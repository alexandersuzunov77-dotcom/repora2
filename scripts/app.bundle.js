/* AUTO-GENERATED by build.js — do not edit. Bundles scripts/ for file:// use. */
(function(){
  var __mods = {}, __cache = {};
  function __def(id, fn){ __mods[id] = fn; }
  function __require(id){
    if (__cache[id]) return __cache[id].exports;
    var m = { exports: {} };
    __cache[id] = m;
    if (!__mods[id]) throw new Error('module not found: ' + id);
    __mods[id](m, m.exports, __require);
    return m.exports;
  }
__def("scripts/app.js", function(module, exports, __require){
const { getState, subscribe } = __require("scripts/store.js");
const { applyTheme } = __require("scripts/theme.js");
const { registerRoute, initRouter, onNavigate, getCurrentPath } = __require("scripts/router.js");
const { renderSidebar } = __require("scripts/components/sidebar.js");
const { renderTweaks } = __require("scripts/components/tweaks.js");

const planner = __require("scripts/pages/planner.js");
const dashboard = __require("scripts/pages/dashboard.js");
const logEntry = __require("scripts/pages/logEntry.js");
const calendar = __require("scripts/pages/calendar.js");
const builder = __require("scripts/pages/builder.js");
const library = __require("scripts/pages/library.js");
const measurements = __require("scripts/pages/measurements.js");
const photos = __require("scripts/pages/photos.js");

function isActive(itemPath, current) {
  if (itemPath === '/') return current === '/';
  return current.startsWith(itemPath);
}

function boot() {
  applyTheme(getState().settings);
  subscribe((s) => applyTheme(s.settings));

  const shell = document.getElementById('app-shell');
  const aside = renderSidebar(shell);
  renderTweaks(aside.querySelector('#tweaks-mount'));

  const main = document.createElement('main');
  main.id = 'main-content';
  shell.appendChild(main);

  registerRoute('/', planner.render);
  registerRoute('/dashboard', dashboard.render);
  registerRoute('/log', logEntry.render);
  registerRoute('/log/:date/:idx', logEntry.render);
  registerRoute('/calendar', calendar.render);
  registerRoute('/calendar/:year/:month', calendar.render);
  registerRoute('/builder', builder.render);
  registerRoute('/library', library.render);
  registerRoute('/library/:id', library.render);
  registerRoute('/measurements', measurements.render);
  registerRoute('/photos', photos.render);

  onNavigate((path) => {
    aside.querySelectorAll('.nav-item').forEach((btn) => {
      btn.classList.toggle('active', isActive(btn.dataset.path, path));
    });
  });

  initRouter(main);
}

document.addEventListener('DOMContentLoaded', boot);


});

__def("scripts/components/dom.js", function(module, exports, __require){
function escapeHtml(str) {
  if (str == null) return '';
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

function h(html) {
  const tpl = document.createElement('template');
  tpl.innerHTML = html.trim();
  return tpl.content.firstElementChild;
}

// Page modules call bindClicks/bindInputs on every render(), often reusing the
// same persistent container across re-renders. Track the previously attached
// delegate per root+kind so re-binding replaces it instead of stacking another
// listener (which would multi-fire actions after repeated in-place renders).
const attached = new WeakMap();

function rebind(root, kind, eventNames, listener) {
  let byKind = attached.get(root);
  if (!byKind) { byKind = {}; attached.set(root, byKind); }
  const prev = byKind[kind];
  if (prev) prev.events.forEach((evt) => root.removeEventListener(evt, prev.listener));
  eventNames.forEach((evt) => root.addEventListener(evt, listener));
  byKind[kind] = { events: eventNames, listener };
}

/** Delegate clicks within `root` for elements matching `[data-action]`.
 *  handlers: { actionName: (el, event) => void } */
function bindClicks(root, handlers) {
  const listener = (e) => {
    const target = e.target.closest('[data-action]');
    if (!target || !root.contains(target)) return;
    const action = target.dataset.action;
    if (handlers[action]) handlers[action](target, e);
  };
  rebind(root, 'click', ['click'], listener);
}

/** Delegate input/change within `root` for elements matching `[data-field]`. */
function bindInputs(root, handler, events = ['input', 'change']) {
  const listener = (e) => {
    const target = e.target.closest('[data-field]');
    if (!target || !root.contains(target)) return;
    handler(target, e);
  };
  rebind(root, 'input:' + events.join(','), events, listener);
}

exports.escapeHtml = escapeHtml;
exports.h = h;
exports.bindClicks = bindClicks;
exports.bindInputs = bindInputs;
});

__def("scripts/components/exerciseArt.js", function(module, exports, __require){
// Demo images for the Exercise Library — illustrated silhouette figures.
// Every exercise maps to one of ~38 movement-pattern poses rendered as a
// weighted-limb silhouette (capsule limbs, filled torso/head, dimmed far side)
// with detailed equipment and a floor shadow. Self-contained inline SVG.

const BODY = '#cfd8e6';
const DIM = '#727c8c';
const METAL = '#8b95a5';
const IRON = '#454e5c';
const IRON_RIM = '#5a6473';

const W = { torso: 13, leg: 10, arm: 8 };

const pline = (pts, stroke, w) =>
  `<polyline points="${pts.map((p) => p.join(',')).join(' ')}" fill="none" stroke="${stroke}" stroke-width="${w}" stroke-linecap="round" stroke-linejoin="round"/>`;
const ln = (x1, y1, x2, y2, stroke = IRON, w = 4, dash = '') =>
  `<line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${stroke}" stroke-width="${w}" stroke-linecap="round"${dash ? ` stroke-dasharray="${dash}"` : ''}/>`;
const ci = (x, y, r, stroke = METAL, fill = 'none', w = 4) =>
  `<circle cx="${x}" cy="${y}" r="${r}" stroke="${stroke}" fill="${fill}" stroke-width="${w}"/>`;
const box = (x, y, w, h, fill = IRON, rx = 2.5) => `<rect x="${x}" y="${y}" width="${w}" height="${h}" rx="${rx}" fill="${fill}"/>`;
const shadow = (cx, rx) => `<ellipse cx="${cx}" cy="141" rx="${rx}" ry="4.5" fill="rgba(0,0,0,.45)"/>`;
const ground = () => ln(26, 142, 214, 142, '#20262f', 2.5);

// limb chain helpers — L = front (body color), B = far side (dim)
const L = (pts, w = W.leg) => pline(pts, BODY, w);
const A = (pts) => pline(pts, BODY, W.arm);
const Lb = (pts, w = W.leg) => pline(pts, DIM, w);
const Ab = (pts) => pline(pts, DIM, W.arm);
const T = (pts) => pline(pts, BODY, W.torso);
const head = (x, y) => `<circle cx="${x}" cy="${y}" r="7.5" fill="${BODY}"/>`;

// ---- equipment ----
function grip(eq, x, y, vertical = false) {
  if (eq === 'db') {
    return vertical
      ? ln(x, y - 5, x, y + 5, METAL, 3.5) + box(x - 4, y - 11, 8, 7, IRON) + box(x - 4, y + 4, 8, 7, IRON)
      : ln(x - 5, y, x + 5, y, METAL, 3.5) + box(x - 11, y - 4, 7, 8, IRON) + box(x + 4, y - 4, 7, 8, IRON);
  }
  if (eq === 'kb') return `<circle cx="${x}" cy="${y + 8}" r="7" fill="${IRON}" stroke="${IRON_RIM}" stroke-width="2"/>` + pline([[x - 4.5, y + 2], [x, y - 3], [x + 4.5, y + 2]], METAL, 3.5);
  if (eq === 'plate') return ci(x, y, 8.5, IRON_RIM, IRON, 3) + ci(x, y, 2.5, METAL, METAL, 2);
  if (eq === 'ball') return ci(x, y, 9.5, IRON_RIM, IRON, 3);
  return '';
}
// side-view loaded barbell (plate + hub + bar stub)
const barSide = (x, y) => ln(x - 12, y, x + 12, y, METAL, 3.5) + ci(x, y, 10.5, IRON_RIM, IRON, 3) + ci(x, y, 3, METAL, METAL, 2);
// front-view barbell with stacked plates
const barFront = (x1, x2, y) =>
  ln(x1 - 14, y, x2 + 14, y, METAL, 4) +
  box(x1 - 4, y - 11, 7, 22) + box(x1 - 12, y - 8, 6, 16) +
  box(x2 - 3, y - 11, 7, 22) + box(x2 + 6, y - 8, 6, 16);
const pulley = (x, y) => ci(x, y, 3.5, METAL, '#2a303c', 2.5);
const cable = (ax, ay, hx, hy) => ln(ax, ay, hx, hy, '#7b8494', 1.8) + pulley(ax, ay);
const benchPad = (x, y, w) => box(x, y, w, 8, IRON, 4) + ln(x + 3, y + 1.5, x + w - 3, y + 1.5, IRON_RIM, 2);

// standing figure base
const stand = (cx) => [
  shadow(cx, 30),
  Lb([[cx, 88], [cx - 6, 112], [cx - 8, 135], [cx - 14, 136]]),
  L([[cx, 88], [cx + 6, 112], [cx + 8, 135], [cx + 15, 136]]),
  T([[cx, 92], [cx, 52]]),
  head(cx, 42),
].join('');

// hinged/bent-over figure base
const hinged = () => [
  shadow(122, 38),
  Lb([[104, 88], [106, 114], [100, 136], [94, 137]]),
  L([[104, 88], [114, 112], [110, 136], [117, 137]]),
  T([[102, 92], [140, 62]]),
  head(150, 55),
].join('');

const POSES = {
  overheadPress(eq) {
    const p = [stand(120), Ab([[120, 58], [106, 44], [102, 28]]), A([[120, 58], [134, 44], [138, 28]])];
    if (eq === 'ball') p.push(ci(120, 19, 10.5, IRON_RIM, IRON, 3));
    else if (eq === 'db' || eq === 'kb') p.push(grip(eq, 102, 25), grip(eq, 138, 25));
    else p.push(barFront(94, 146, 25));
    return p.join('');
  },
  lateralRaise(eq) {
    const p = [stand(120), Ab([[120, 58], [94, 56], [72, 52]]), A([[120, 58], [146, 56], [168, 52]])];
    if (eq === 'cable') p.push(cable(36, 138, 168, 52));
    else if (eq === 'db') p.push(grip('db', 72, 52, true), grip('db', 168, 52, true));
    return p.join('');
  },
  frontRaise(eq) {
    return [stand(120), Ab([[120, 58], [112, 74], [110, 90]]), A([[120, 58], [142, 54], [164, 50]]),
      grip(eq === 'plate' ? 'plate' : eq, 167, 50, true)].join('');
  },
  curl(eq) {
    const p = [stand(120), Ab([[120, 58], [108, 76], [104, 58]]), A([[120, 58], [132, 76], [136, 58]])];
    if (eq === 'cable') p.push(cable(174, 138, 138, 58), ln(126, 57, 148, 57, METAL, 4));
    else if (eq === 'db' || eq === 'kb' || eq === 'plate') p.push(grip(eq, 104, 57), grip(eq, 136, 57));
    else p.push(ln(90, 57, 150, 57, METAL, 4), box(84, 48, 7, 18), box(149, 48, 7, 18));
    return p.join('');
  },
  pushdown() {
    return [stand(112), Ab([[112, 58], [126, 72], [128, 90]]), A([[112, 58], [130, 74], [134, 92]]),
      cable(152, 14, 134, 90), ln(120, 92, 150, 88, METAL, 4.5)].join('');
  },
  overheadTriExt(eq) {
    return [stand(120), Ab([[120, 58], [106, 54], [102, 70]]), A([[120, 58], [134, 40], [118, 28]]),
      eq === 'cable' ? cable(200, 126, 120, 30) : grip('db', 114, 24, true)].join('');
  },
  shrug(eq) {
    const p = [stand(120), Ab([[120, 58], [108, 76], [106, 94]]), A([[120, 58], [132, 76], [134, 94]])];
    if (eq === 'db' || eq === 'kb') p.push(grip(eq, 106, 98), grip(eq, 134, 98));
    else if (eq === 'cable') p.push(cable(60, 138, 106, 96), cable(180, 138, 134, 96));
    else p.push(barFront(98, 142, 97));
    return p.join('');
  },
  squat(eq) {
    const p = [
      shadow(126, 34),
      Lb([[110, 96], [128, 108], [122, 136], [130, 137]]),
      L([[110, 96], [136, 104], [132, 136], [140, 137]]),
      T([[108, 98], [118, 62]]),
      head(124, 49),
    ];
    if (eq === 'none') p.push(Ab([[118, 64], [140, 62], [158, 60]]));
    else if (eq === 'db' || eq === 'kb' || eq === 'plate' || eq === 'ball') {
      p.push(A([[118, 64], [132, 72], [136, 80]]), grip(eq === 'db' ? 'kb' : eq, 138, 84));
    } else p.push(A([[118, 64], [132, 68], [124, 58]]), barSide(111, 56));
    return p.join('');
  },
  hinge(eq) {
    const p = [hinged(), Ab([[136, 66], [140, 88], [142, 104]]), A([[138, 64], [144, 86], [146, 104]])];
    if (eq === 'db' || eq === 'kb') p.push(grip(eq, 146, 109));
    else p.push(barSide(146, 109));
    return p.join('');
  },
  kbSwing(eq) {
    return [hinged(), Ab([[136, 66], [154, 74], [172, 82]]), A([[138, 64], [158, 72], [176, 80]]),
      eq === 'cable' ? cable(32, 136, 176, 80) : '', grip('kb', 179, 84)].join('');
  },
  rowBent(eq) {
    const p = [hinged(), Ab([[134, 66], [136, 84], [130, 94]]), A([[138, 64], [142, 84], [136, 96]])];
    if (eq === 'cable') p.push(cable(210, 122, 136, 98), ln(126, 98, 148, 96, METAL, 4.5));
    else if (eq === 'db' || eq === 'kb') p.push(grip(eq, 136, 101));
    else p.push(barSide(136, 101));
    return p.join('');
  },
  benchPress(eq) {
    const p = [
      shadow(112, 52),
      benchPad(60, 96, 94), ln(72, 104, 72, 136, IRON, 5), ln(142, 104, 142, 136, IRON, 5),
      head(74, 87),
      T([[86, 92], [124, 94]]),
      L([[124, 94], [142, 106], [148, 134], [156, 135]]),
      Ab([[88, 92], [90, 72], [93, 55]]),
      A([[92, 92], [95, 72], [98, 54]]),
    ];
    p.push(eq === 'db' ? grip('db', 98, 50) : barSide(98, 49));
    return p.join('');
  },
  inclinePress(eq) {
    const p = [
      shadow(120, 48),
      ln(84, 72, 134, 104, IRON, 8), ln(110, 104, 110, 138, IRON, 5), ln(134, 104, 148, 104, IRON, 6),
      head(89, 59),
      T([[98, 68], [128, 96]]),
      L([[128, 96], [148, 102], [152, 134], [160, 135]]),
      Ab([[102, 70], [108, 52], [113, 38]]),
      A([[104, 70], [112, 52], [118, 36]]),
    ];
    p.push(eq === 'db' ? grip('db', 118, 32) : barSide(119, 31));
    return p.join('');
  },
  cableCross() {
    return [stand(104), Ab([[104, 58], [124, 64], [144, 68]]), A([[104, 58], [126, 60], [148, 62]]),
      cable(208, 18, 148, 62), cable(208, 116, 144, 68)].join('');
  },
  rearDelt(eq) {
    return [hinged(),
      Ab([[134, 66], [116, 74], [100, 80]]),
      A([[138, 64], [154, 74], [170, 80]]),
      eq === 'db' ? grip('db', 170, 83, true) + grip('db', 100, 83, true) : '',
      eq === 'cable' ? cable(208, 20, 170, 80) : ''].join('');
  },
  pushUp() {
    return [shadow(116, 52), head(64, 97),
      T([[76, 103], [114, 111]]),
      L([[114, 111], [136, 122], [156, 137]]),
      Ab([[78, 104], [70, 122], [78, 138]]),
      A([[80, 104], [74, 122], [84, 138]])].join('');
  },
  plank() {
    return [shadow(116, 52), head(66, 101),
      T([[80, 107], [118, 110]]),
      L([[118, 110], [140, 122], [160, 137]]),
      A([[80, 107], [70, 131], [92, 135]])].join('');
  },
  quadruped(eq) {
    return [shadow(112, 46), head(70, 89),
      T([[84, 95], [126, 94]]),
      A([[84, 95], [82, 116], [80, eq === 'wheel' ? 124 : 136]]),
      Lb([[126, 94], [134, 116], [142, 137]]),
      L([[126, 94], [140, 114], [150, 137]]),
      eq === 'wheel' ? ci(80, 132, 8, IRON_RIM, IRON, 3) + ln(74, 132, 86, 132, METAL, 3) : ''].join('');
  },
  dip() {
    return [shadow(120, 40),
      ln(88, 78, 88, 140, IRON, 6), ln(152, 78, 152, 140, IRON, 6),
      ln(74, 78, 102, 78, METAL, 5), ln(138, 78, 166, 78, METAL, 5),
      head(120, 41),
      T([[120, 50], [120, 90]]),
      Ab([[120, 54], [104, 66], [90, 77]]),
      A([[120, 54], [136, 66], [150, 77]]),
      Lb([[120, 90], [112, 112], [120, 126]]),
      L([[120, 90], [128, 112], [136, 126]])].join('');
  },
  pullUp() {
    return [shadow(120, 30),
      ln(60, 26, 180, 26, METAL, 5),
      head(120, 47),
      T([[120, 56], [120, 96]]),
      Ab([[120, 58], [104, 44], [98, 28]]),
      A([[120, 58], [136, 44], [142, 28]]),
      Lb([[120, 96], [112, 116], [108, 132]]),
      L([[120, 96], [128, 116], [124, 132]])].join('');
  },
  hangingLegRaise() {
    return [shadow(130, 30),
      ln(60, 26, 180, 26, METAL, 5),
      head(120, 47),
      T([[120, 56], [120, 94]]),
      Ab([[120, 58], [106, 44], [102, 28]]),
      A([[120, 58], [134, 44], [138, 28]]),
      Lb([[120, 94], [144, 86], [166, 84]]),
      L([[120, 94], [146, 92], [168, 90]])].join('');
  },
  pulldown() {
    return [shadow(120, 40),
      benchPad(92, 106, 38), ln(108, 114, 108, 138, IRON, 5),
      head(106, 45),
      T([[112, 100], [109, 58]]),
      L([[112, 98], [140, 96], [142, 134], [150, 135]]),
      Ab([[110, 60], [96, 46], [92, 32]]),
      A([[110, 60], [124, 44], [130, 30]]),
      ln(78, 30, 146, 26, METAL, 4.5),
      cable(126, 8, 112, 28)].join('');
  },
  seatedRow() {
    return [shadow(122, 44),
      benchPad(82, 104, 52),
      head(96, 49),
      T([[104, 102], [100, 62]]),
      L([[104, 100], [136, 92], [160, 102]]),
      box(160, 84, 9, 24),
      Ab([[100, 64], [118, 78], [132, 76]]),
      A([[100, 64], [122, 74], [136, 72]]),
      ln(210, 72, 140, 72, '#7b8494', 1.8), pulley(210, 72),
      ln(136, 65, 140, 81, METAL, 4.5)].join('');
  },
  legPress() {
    return [shadow(110, 46),
      ln(58, 74, 94, 108, IRON, 9),
      benchPad(84, 108, 40),
      head(61, 63),
      T([[72, 80], [96, 104]]),
      Lb([[96, 104], [120, 90], [142, 74]]),
      L([[96, 104], [124, 84], [146, 66]]),
      ln(142, 50, 166, 84, IRON, 8),
      Ab([[76, 84], [90, 96], [96, 92]])].join('');
  },
  legExt() {
    return [shadow(116, 42),
      benchPad(88, 94, 48), ln(88, 60, 92, 94, IRON, 7), ln(112, 102, 112, 138, IRON, 5),
      head(98, 45),
      T([[106, 94], [102, 58]]),
      Lb([[106, 92], [134, 94], [150, 100]]),
      L([[106, 92], [138, 92], [164, 80]]),
      ci(158, 84, 5.5, IRON_RIM, IRON, 3),
      Ab([[103, 62], [114, 78], [110, 92]])].join('');
  },
  legCurl() {
    return [shadow(118, 52),
      benchPad(64, 100, 108), ln(82, 108, 82, 136, IRON, 5), ln(152, 108, 152, 136, IRON, 5),
      head(62, 91),
      T([[74, 97], [126, 99]]),
      L([[126, 99], [150, 101], [144, 74]]),
      ci(147, 79, 5.5, IRON_RIM, IRON, 3),
      Ab([[78, 99], [84, 116], [92, 124]])].join('');
  },
  lunge(eq) {
    const p = [
      shadow(118, 44),
      Lb([[116, 90], [100, 118], [84, 135], [78, 133]]),
      L([[116, 90], [144, 104], [144, 136], [152, 137]]),
      T([[116, 92], [116, 54]]),
      head(116, 43),
      Ab([[116, 58], [104, 76], [102, 92]]),
      A([[116, 58], [128, 76], [130, 92]]),
    ];
    if (eq === 'bar') p.push(barSide(113, 51));
    else p.push(grip(eq, 102, 97), grip(eq, 130, 97));
    return p.join('');
  },
  calfRaise(eq) {
    const p = [
      shadow(120, 34),
      box(94, 130, 54, 11, IRON, 3),
      Lb([[120, 80], [114, 104], [110, 127]]),
      L([[120, 80], [122, 104], [118, 127]]),
      T([[120, 84], [120, 48]]),
      head(120, 37),
      Ab([[120, 50], [108, 68], [106, 84]]),
      A([[120, 50], [132, 68], [134, 84]]),
    ];
    if (eq === 'bar' || eq === 'machine') p.push(barSide(117, 44));
    else if (eq === 'db' || eq === 'kb') p.push(grip(eq, 134, 88), grip(eq, 106, 88));
    return p.join('');
  },
  crunch() {
    return [shadow(116, 44), head(84, 109),
      T([[92, 117], [112, 130]]),
      L([[112, 130], [134, 108], [148, 133], [156, 133]]),
      Ab([[94, 119], [110, 111], [120, 107]])].join('');
  },
  bridge(eq) {
    return [shadow(110, 48),
      benchPad(52, 88, 34), ln(58, 96, 58, 126, IRON, 5), ln(80, 96, 80, 126, IRON, 5),
      head(61, 83),
      T([[76, 90], [118, 84]]),
      Lb([[118, 84], [136, 100], [132, 137]]),
      L([[118, 84], [142, 98], [142, 137]]),
      eq === 'bar' || eq === 'machine' ? barSide(114, 76) : ''].join('');
  },
  backExt() {
    return [shadow(118, 44),
      benchPad(96, 94, 38), ln(112, 102, 112, 138, IRON, 6),
      Lb([[112, 96], [142, 100], [166, 108]]),
      T([[114, 96], [84, 78]]),
      head(74, 69),
      Ab([[88, 82], [96, 92]])].join('');
  },
  twist(eq) {
    return [shadow(120, 40), head(90, 79),
      T([[98, 91], [116, 124]]),
      L([[116, 124], [140, 106], [158, 112]]),
      A([[102, 96], [124, 96], [138, 100]]),
      grip(eq && eq !== 'none' && eq !== 'cable' ? eq : 'plate', 144, 100)].join('');
  },
  carry(eq) {
    const kind = eq && eq !== 'none' && eq !== 'machine' ? eq : 'kb';
    return [
      shadow(120, 36),
      Lb([[120, 88], [104, 112], [92, 136], [86, 134]]),
      L([[120, 88], [134, 112], [146, 135], [154, 136]]),
      T([[120, 92], [120, 52]]),
      head(120, 42),
      Ab([[120, 58], [108, 76], [106, 94]]),
      A([[120, 58], [132, 76], [134, 94]]),
      grip(kind, 106, 99), grip(kind, 134, 99)].join('');
  },
  olympic(eq) {
    const p = [
      shadow(124, 36),
      Lb([[112, 96], [128, 108], [122, 136], [130, 137]]),
      L([[112, 96], [136, 104], [132, 136], [140, 137]]),
      T([[110, 98], [118, 60]]),
      head(122, 48),
      Ab([[118, 62], [108, 42], [106, 24]]),
      A([[118, 62], [128, 42], [130, 24]]),
    ];
    if (eq === 'kb') p.push(grip('kb', 130, 20));
    else if (eq === 'db') p.push(grip('db', 106, 21), grip('db', 130, 21));
    else p.push(barFront(90, 148, 21));
    return p.join('');
  },
  wristCurl(eq) {
    return [shadow(116, 40),
      benchPad(86, 100, 48), ln(94, 108, 94, 136, IRON, 5), ln(124, 108, 124, 136, IRON, 5),
      head(96, 49),
      T([[104, 100], [100, 62]]),
      L([[106, 98], [136, 94], [138, 134], [146, 135]]),
      Ab([[101, 64], [118, 80]]),
      A([[118, 80], [142, 84]]),
      eq === 'cable' ? cable(206, 130, 144, 84) : grip('db', 147, 83, true)].join('');
  },
  abduction(eq) {
    return [
      shadow(126, 38),
      L([[120, 88], [118, 112], [116, 135], [123, 136]]),
      Lb([[120, 88], [146, 96], [164, 110], [171, 107]]),
      T([[120, 92], [120, 52]]),
      head(120, 42),
      Ab([[120, 58], [132, 70], [126, 82]]),
      eq === 'band' ? ln(118, 130, 164, 110, METAL, 3, '6 5') : '',
      eq === 'cable' ? cable(210, 132, 164, 110) : ''].join('');
  },
  neck() {
    return [shadow(116, 30),
      `<circle cx="116" cy="58" r="14" fill="${BODY}"/>`,
      T([[116, 72], [116, 96]]),
      pline([[92, 100], [140, 100]], DIM, 11),
      ln(132, 58, 152, 58, '#7b8494', 2),
      grip('plate', 161, 58)].join('');
  },
  kneelOverhead(eq) {
    return [
      shadow(116, 40),
      Lb([[112, 98], [104, 133], [84, 137]]),
      L([[112, 98], [138, 106], [138, 136], [146, 137]]),
      T([[112, 100], [118, 62]]),
      head(120, 50),
      Ab([[118, 64], [128, 82], [134, 96]]),
      A([[118, 62], [114, 42], [112, 26]]),
      grip(eq === 'db' ? 'db' : 'kb', 112, 20)].join('');
  },
};

// Ordered movement-pattern classifier — first matching rule wins.
const RULES = [
  [/push-up|burpee/, 'pushUp'],
  [/toes-to-bar|hanging (leg|knee)/, 'hangingLegRaise'],
  [/dead hang|pull-up|chin-up|inverted row/, 'pullUp'],
  [/behind-the-neck/, 'overheadPress'],
  [/neck/, 'neck'],
  [/leg curl|nordic|glute-ham/, 'legCurl'],
  [/wrist curl|wrist roller/, 'wristCurl'],
  [/plate pinch|farmer|carry/, 'carry'],
  [/leg extension/, 'legExt'],
  [/hack squat|leg press/, 'legPress'],
  [/calf|tibialis/, 'calfRaise'],
  [/rear delt|reverse pec/, 'rearDelt'],
  [/crossover|cable flye|pec deck|face pull|pallof|woodchopper/, 'cableCross'],
  [/svend|front raise/, 'frontRaise'],
  [/lateral raise|y-raise/, 'lateralRaise'],
  [/lateral walk|abduction|kickback \(glute\)/, 'abduction'],
  [/shrug/, 'shrug'],
  [/upright row/, 'curl'],
  [/overhead (cable|dumbbell) extension/, 'overheadTriExt'],
  [/straight-arm pulldown|pushdown|kickback|triceps extension/, 'pushdown'],
  [/pulldown/, 'pulldown'],
  [/seated cable row|machine row|chest-supported/, 'seatedRow'],
  [/renegade|mountain climber|bear crawl|ab wheel/, 'quadruped'],
  [/row/, 'rowBent'],
  [/get-up/, 'kneelOverhead'],
  [/clean & press|thruster|wall ball|shoulder press|arnold|seated barbell|overhead barbell|military|landmine|pike|handstand/, 'overheadPress'],
  [/clean|snatch|devil/, 'olympic'],
  [/swing|pull-through/, 'kbSwing'],
  [/deadlift|rdl|rack pull|good morning|stiff-leg/, 'hinge'],
  [/hip thrust|glute bridge|bridge|frog pump/, 'bridge'],
  [/back extension|hyperextension/, 'backExt'],
  [/bulgarian|split squat|lunge|step-up/, 'lunge'],
  [/squat/, 'squat'],
  [/dip/, 'dip'],
  [/incline.*(press|bench|flye)/, 'inclinePress'],
  [/bench press|floor press|chest press|decline press|jm press|tate|skull|close-grip/, 'benchPress'],
  [/flye/, 'benchPress'],
  [/curl|21s/, 'curl'],
  [/press/, 'overheadPress'],
  [/plank|hollow/, 'plank'],
  [/crunch|sit-up|dead bug/, 'crunch'],
  [/leg raise/, 'crunch'],
  [/twist/, 'twist'],
];

/** Returns the pose key for an exercise (or name string); null = no match. */
function poseKeyFor(exOrName) {
  const n = (typeof exOrName === 'string' ? exOrName : exOrName.name).toLowerCase();
  for (const [re, key] of RULES) if (re.test(n)) return key;
  return null;
}

function eqKind(equipment) {
  const e = (equipment || '').toLowerCase();
  if (e.includes('barbell')) return 'bar';
  if (e.includes('dumbbell')) return 'db';
  if (e.includes('kettlebell')) return 'kb';
  if (e.includes('cable')) return 'cable';
  if (e.includes('machine')) return 'machine';
  if (e.includes('plate')) return 'plate';
  if (e.includes('band')) return 'band';
  if (e.includes('wheel')) return 'wheel';
  if (e.includes('ball')) return 'ball';
  return 'none';
}

const escapeAttr = (s) => String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;');

/** Full demo-image SVG for a library exercise. */
function exerciseArtSvg(ex) {
  const key = poseKeyFor(ex) || 'carry';
  const body = POSES[key](eqKind(ex.equipment));
  return `<svg viewBox="0 0 240 160" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="${escapeAttr(ex.name)} demo" preserveAspectRatio="xMidYMid meet">${ground()}${body}</svg>`;
}

exports.poseKeyFor = poseKeyFor;
exports.exerciseArtSvg = exerciseArtSvg;
});

__def("scripts/components/exerciseMedia.js", function(module, exports, __require){
// Resolves the demo media for an exercise: real demonstration photos where we
// have them (two frames, cross-faded to show the movement — see ATTRIBUTION.md),
// falling back to the procedurally drawn silhouette for the few exercises the
// photo dataset doesn't cover. User-uploaded photos always take precedence
// upstream (imageSlot).

const { IMAGES_BY_NAME } = __require("scripts/data/exerciseImages.js");
const { ANIMATIONS_BY_NAME } = __require("scripts/data/exerciseAnimations.js");
const { exerciseArtSvg } = __require("scripts/components/exerciseArt.js");

const escapeAttr = (s) => String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/</g, '&lt;');

function hasPhoto(ex) {
  return Boolean(IMAGES_BY_NAME[ex.name]);
}

/** Full-size demo media HTML: licensed animation if present, else the
 *  animated two-frame photo demo, else the silhouette SVG. */
function exerciseMediaHtml(ex) {
  const anim = ANIMATIONS_BY_NAME[ex.name];
  if (anim) {
    return `<div class="photo-demo"><img src="${escapeAttr(anim)}" alt="${escapeAttr(ex.name)} demo"></div>`;
  }
  const frames = IMAGES_BY_NAME[ex.name];
  if (!frames) return exerciseArtSvg(ex);
  const alt = escapeAttr(ex.name);
  return `
    <div class="photo-demo">
      <img src="${frames[0]}" alt="${alt} — start position" loading="lazy">
      ${frames[1] ? `<img class="frame2" src="${frames[1]}" alt="${alt} — end position" loading="lazy">` : ''}
    </div>`;
}

const FRAME_LABELS = ['START POSITION', 'END POSITION'];

/** Whether this exercise has a distinct demonstration photo for frame `i`. */
function hasFrame(ex, i) {
  const frames = IMAGES_BY_NAME[ex.name];
  return Boolean(frames && frames[i]);
}

/** Single-frame demo HTML for box `i` (0 = start, 1 = end). Photo if we have
 *  it, otherwise the silhouette drawing as a fallback. */
function exerciseFrameHtml(ex, i) {
  const frames = IMAGES_BY_NAME[ex.name];
  if (frames && frames[i]) {
    return `<div class="photo-demo"><img src="${escapeAttr(frames[i])}" alt="${escapeAttr(ex.name)} — ${i === 0 ? 'start' : 'end'} position" loading="lazy"></div>`;
  }
  return exerciseArtSvg(ex);
}

/** Small static thumbnail HTML (first frame only), or silhouette SVG. */
function exerciseThumbHtml(ex) {
  const frames = IMAGES_BY_NAME[ex.name];
  if (!frames) return exerciseArtSvg(ex);
  return `<img class="photo-thumb" src="${frames[0]}" alt="" loading="lazy">`;
}

exports.hasPhoto = hasPhoto;
exports.exerciseMediaHtml = exerciseMediaHtml;
exports.FRAME_LABELS = FRAME_LABELS;
exports.hasFrame = hasFrame;
exports.exerciseFrameHtml = exerciseFrameHtml;
exports.exerciseThumbHtml = exerciseThumbHtml;
});

__def("scripts/components/exercisePicker.js", function(module, exports, __require){
const { getState } = __require("scripts/store.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");
const { exerciseThumbHtml } = __require("scripts/components/exerciseMedia.js");

const MUSCLE_GROUPS = [
  'Chest', 'Back', 'Shoulders', 'Biceps', 'Triceps', 'Forearms & Grip',
  'Quadriceps', 'Hamstrings', 'Glutes', 'Calves', 'Abs & Core', 'Traps & Neck',
  'Full-Body & Compound',
];

/**
 * Opens a searchable modal over the whole library for picking an exercise.
 * @param {object} opts
 * @param {(exercise: object) => void} opts.onSelect
 * @param {string} [opts.title]
 */
function openExercisePicker({ onSelect, title = 'Add Exercise' }) {
  let query = '';
  let group = 'All';

  const overlay = document.createElement('div');
  overlay.className = 'modal-overlay';
  document.body.appendChild(overlay);

  function close() {
    document.removeEventListener('keydown', onKey);
    overlay.remove();
  }

  function onKey(e) {
    if (e.key === 'Escape') close();
  }
  document.addEventListener('keydown', onKey);

  overlay.addEventListener('mousedown', (e) => {
    if (e.target === overlay) close();
  });

  function paint(focusSearch) {
    const lib = getState().library;
    const filtered = lib.filter((ex) => {
      const groupOk = group === 'All' || ex.muscle === group;
      const q = query.trim().toLowerCase();
      const queryOk = !q || ex.name.toLowerCase().includes(q) || ex.equipment.toLowerCase().includes(q);
      return groupOk && queryOk;
    });

    overlay.innerHTML = `
      <div class="modal-box">
        <div class="modal-head">
          <div class="section-label">${escapeHtml(title).toUpperCase()}</div>
          <button class="icon-btn" data-action="close">✕</button>
        </div>
        <div class="flex gap-10" style="align-items:center;background:var(--surface);border:1px solid var(--chip-border);border-radius:9px;padding:10px 14px;margin:14px 0 10px;">
          <span class="muted" style="font-size:15px;">⌕</span>
          <input type="text" data-field="picker-search" placeholder="Search ${lib.length} exercises…" value="${escapeHtml(query)}" class="mono" style="border:none;background:none;flex:1;color:var(--text-body);font-size:14px;padding:0;">
        </div>
        <div class="flex" style="flex-wrap:wrap;gap:6px;margin-bottom:12px;">
          <button class="pill ${group === 'All' ? '' : 'outline'}" style="cursor:pointer;border:none;" data-action="picker-group" data-group="All">All</button>
          ${MUSCLE_GROUPS.map((g) => `<button class="pill ${g === group ? '' : 'outline'}" style="cursor:pointer;border:none;" data-action="picker-group" data-group="${escapeHtml(g)}">${escapeHtml(g)}</button>`).join('')}
        </div>
        <div class="muted mono" style="font-size:11px;margin-bottom:6px;">${filtered.length} result${filtered.length === 1 ? '' : 's'}</div>
        <div class="modal-list">
          ${filtered.length ? filtered.map((ex) => `
            <div class="modal-list-row" data-action="pick" data-id="${ex.id}">
              <div class="flex gap-10" style="align-items:center;min-width:0;">
                <div class="art-thumb">${exerciseThumbHtml(ex)}</div>
                <div style="min-width:0;">
                  <div style="color:var(--text-body);font-weight:600;font-size:14px;">${escapeHtml(ex.name)}</div>
                  <div class="muted mono" style="font-size:11px;margin-top:1px;">${escapeHtml(ex.muscle)} · ${escapeHtml(ex.equipment)}</div>
                </div>
              </div>
              <span class="pill outline">${escapeHtml(ex.level)}</span>
            </div>
          `).join('') : `<div style="padding:24px;text-align:center;color:var(--text-ghost);">No exercises match.</div>`}
        </div>
      </div>
    `;

    bindClicks(overlay, {
      close: () => close(),
      'picker-group': (el) => { group = el.dataset.group; paint(false); },
      pick: (el) => {
        const ex = lib.find((x) => x.id === el.dataset.id);
        close();
        onSelect(ex);
      },
    });
    bindInputs(overlay, (el) => {
      if (el.dataset.field === 'picker-search') { query = el.value; paint(true); }
    }, ['input']);

    if (focusSearch) {
      const input = overlay.querySelector('[data-field="picker-search"]');
      input.focus();
      input.setSelectionRange(input.value.length, input.value.length);
    }
  }

  paint(true);
}

exports.MUSCLE_GROUPS = MUSCLE_GROUPS;
exports.openExercisePicker = openExercisePicker;
});

__def("scripts/components/imageSlot.js", function(module, exports, __require){
// Reusable drop/click image slot. Resizes+compresses images client-side
// before handing back a dataURL, to keep localStorage usage sane.

function compressImage(file, maxDim = 900, quality = 0.78) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onerror = reject;
    reader.onload = () => {
      const img = new Image();
      img.onerror = reject;
      img.onload = () => {
        let { width, height } = img;
        if (width > height && width > maxDim) {
          height = Math.round((height * maxDim) / width);
          width = maxDim;
        } else if (height > maxDim) {
          width = Math.round((width * maxDim) / height);
          height = maxDim;
        }
        const canvas = document.createElement('canvas');
        canvas.width = width;
        canvas.height = height;
        const ctx = canvas.getContext('2d');
        ctx.drawImage(img, 0, 0, width, height);
        resolve(canvas.toDataURL('image/jpeg', quality));
      };
      img.src = reader.result;
    };
    reader.readAsDataURL(file);
  });
}

/**
 * @param {object} opts
 * @param {string|null} opts.image - current dataURL, if any
 * @param {string} opts.placeholder - placeholder text
 * @param {string} [opts.placeholderSvg] - inline SVG art shown when no image is set
 * @param {(dataUrl: string|null) => void} opts.onChange
 * @param {boolean} [opts.removable]
 */
function createImageSlot({ image, placeholder, placeholderSvg, onChange, removable = true }) {
  const el = document.createElement('div');
  el.className = 'image-slot';

  const input = document.createElement('input');
  input.type = 'file';
  input.accept = 'image/*';
  input.style.display = 'none';

  function paint() {
    el.innerHTML = '';
    if (image) {
      const img = document.createElement('img');
      img.src = image;
      el.appendChild(img);
      if (removable) {
        const rm = document.createElement('button');
        rm.className = 'remove-btn';
        rm.textContent = '✕';
        rm.title = 'Remove photo';
        rm.addEventListener('click', (e) => {
          e.stopPropagation();
          image = null;
          onChange(null);
          paint();
        });
        el.appendChild(rm);
      }
    } else if (placeholderSvg) {
      const art = document.createElement('div');
      art.className = 'art-ph';
      art.innerHTML = placeholderSvg;
      el.appendChild(art);
      const hint = document.createElement('div');
      hint.className = 'art-hint';
      hint.textContent = placeholder || 'Click or drop a photo to replace';
      el.appendChild(hint);
    } else {
      const p = document.createElement('div');
      p.className = 'placeholder-text';
      p.textContent = placeholder || 'Click or drop image';
      el.appendChild(p);
    }
    el.appendChild(input);
  }

  async function handleFile(file) {
    if (!file || !file.type.startsWith('image/')) return;
    const dataUrl = await compressImage(file);
    image = dataUrl;
    onChange(dataUrl);
    paint();
  }

  el.addEventListener('click', () => input.click());
  input.addEventListener('change', () => handleFile(input.files[0]));

  el.addEventListener('dragover', (e) => { e.preventDefault(); el.classList.add('drag-over'); });
  el.addEventListener('dragleave', () => el.classList.remove('drag-over'));
  el.addEventListener('drop', (e) => {
    e.preventDefault();
    el.classList.remove('drag-over');
    const file = e.dataTransfer.files && e.dataTransfer.files[0];
    handleFile(file);
  });

  paint();
  return el;
}

exports.createImageSlot = createImageSlot;
});

__def("scripts/components/sidebar.js", function(module, exports, __require){
const { getCurrentPath, navigate } = __require("scripts/router.js");

const NAV_GROUPS = [
  { label: 'Workout', items: [
    { path: '/', label: 'Weekly Planner' },
    { path: '/log', label: 'Log Entry' },
    { path: '/calendar', label: 'Calendar' },
    { path: '/dashboard', label: 'Dashboard' },
  ] },
  { label: 'Builder', items: [
    { path: '/builder', label: 'Program Builder' },
    { path: '/library', label: 'Exercise Library' },
  ] },
  { label: 'Client', items: [
    { path: '/measurements', label: 'Measurements' },
    { path: '/photos', label: 'Progress Photos' },
  ] },
];

function isActive(itemPath, current) {
  if (itemPath === '/') return current === '/';
  return current.startsWith(itemPath);
}

function renderSidebar(container) {
  const aside = document.createElement('aside');
  aside.className = 'sidebar';

  const current = getCurrentPath() || '/';

  aside.innerHTML = `
    <div class="sidebar-brand">
      <div class="logo">REP<span>ORA</span></div>
      <div class="tagline">TRAINING SYSTEM</div>
    </div>
    <nav class="sidebar-nav">
      ${NAV_GROUPS.map((g) => `
        <div class="group-label">${g.label}</div>
        ${g.items.map((it) => `
          <button class="nav-item ${isActive(it.path, current) ? 'active' : ''}" data-path="${it.path}">${it.label}</button>
        `).join('')}
      `).join('')}
    </nav>
    <div class="sidebar-footer" id="tweaks-mount"></div>
  `;

  aside.querySelectorAll('.nav-item').forEach((btn) => {
    btn.addEventListener('click', () => navigate(btn.dataset.path));
  });

  container.appendChild(aside);
  return aside;
}

exports.renderSidebar = renderSidebar;
});

__def("scripts/components/tweaks.js", function(module, exports, __require){
const { getState, update } = __require("scripts/store.js");
const { ACCENT_OPTIONS, DENSITY_OPTIONS, ENERGY_OPTIONS, applyTheme } = __require("scripts/theme.js");

let panelOpen = false;

function renderTweaks(mount) {
  const settings = getState().settings;

  mount.innerHTML = `
    <button class="tweaks-toggle" data-action="toggle-tweaks">⚙ Feel</button>
  `;

  mount.querySelector('[data-action="toggle-tweaks"]').addEventListener('click', () => {
    panelOpen = !panelOpen;
    renderPanel();
  });

  renderPanel();

  function renderPanel() {
    let panel = document.getElementById('tweaks-panel');
    if (panel) panel.remove();
    if (!panelOpen) return;

    const s = getState().settings;
    panel = document.createElement('div');
    panel.id = 'tweaks-panel';
    panel.className = 'tweaks-panel';
    panel.innerHTML = `
      <h4>Feel</h4>
      <div class="tweaks-field">
        <label>Accent</label>
        <div class="swatch-row">
          ${ACCENT_OPTIONS.map((c) => `
            <button class="swatch ${c === s.accent ? 'active' : ''}" style="background:${c}" data-action="set-accent" data-value="${c}" title="${c}"></button>
          `).join('')}
        </div>
      </div>
      <div class="tweaks-field">
        <label>Log density</label>
        <div class="seg-row">
          ${DENSITY_OPTIONS.map((d) => `
            <button class="seg-btn ${d === s.density ? 'active' : ''}" data-action="set-density" data-value="${d}">${d}</button>
          `).join('')}
        </div>
      </div>
      <div class="tweaks-field">
        <label>Energy</label>
        <div class="seg-row">
          ${ENERGY_OPTIONS.map((en) => `
            <button class="seg-btn ${en === s.energy ? 'active' : ''}" data-action="set-energy" data-value="${en}">${en}</button>
          `).join('')}
        </div>
      </div>
    `;
    document.body.appendChild(panel);

    panel.querySelectorAll('[data-action="set-accent"]').forEach((btn) => {
      btn.addEventListener('click', () => {
        update((d) => { d.settings.accent = btn.dataset.value; });
        applyTheme(getState().settings);
        renderPanel();
      });
    });
    panel.querySelectorAll('[data-action="set-density"]').forEach((btn) => {
      btn.addEventListener('click', () => {
        update((d) => { d.settings.density = btn.dataset.value; });
        applyTheme(getState().settings);
        renderPanel();
      });
    });
    panel.querySelectorAll('[data-action="set-energy"]').forEach((btn) => {
      btn.addEventListener('click', () => {
        update((d) => { d.settings.energy = btn.dataset.value; });
        applyTheme(getState().settings);
        renderPanel();
      });
    });
  }
}

exports.renderTweaks = renderTweaks;
});

__def("scripts/data/cues.js", function(module, exports, __require){
// Coaching cues for every exercise in the reference database.
// Keyed by exact exercise name; consumed at seed time and backfilled into
// existing localStorage libraries by store.js's reconcile pass.

const CUES_BY_NAME = {
  // ---- CHEST ----
  'Push-Up': [
    'Hands under shoulders, slightly wider; body one straight line.',
    'Lower chest to the floor with elbows tracking ~45°.',
    'Squeeze glutes and brace — no sagging hips.',
  ],
  'Knee Push-Up': [
    'Knees down, but hips stay locked in line with the shoulders.',
    'Lower the chest between the hands, not the face.',
    'Press the floor away without letting the low back sag.',
  ],
  'Incline Push-Up (hands elevated)': [
    'Hands on a bench or bar, body rigid from head to heels.',
    'Lower the chest to the edge under control.',
    'The taller the incline, the easier — lower it as you get stronger.',
  ],
  'Decline Push-Up (feet elevated)': [
    'Feet elevated, hands under the shoulders.',
    'Keep the neck neutral — don’t dive head-first.',
    'Brace hard; hips sag faster in the decline position.',
  ],
  'Barbell Bench Press (flat)': [
    'Retract & depress the scapula — pin the shoulders into the bench.',
    'Lower the bar to the sternum, elbows tucked to ~45°.',
    'Drive the feet into the floor and press in a slight backward arc.',
    'Keep wrists stacked over the elbows through lockout.',
  ],
  'Barbell Incline Bench Press': [
    'Set the bench to 30–45°.',
    'Touch the bar just below the collarbones.',
    'Keep elbows under the bar — don’t flare at the bottom.',
  ],
  'Barbell Decline Bench Press': [
    'Slight decline (15–30°) is plenty.',
    'Bar path travels to the lower chest.',
    'Shorter stroke — control the touch, never bounce.',
  ],
  'Dumbbell Bench Press (flat)': [
    'Bells over the chest, palms turned slightly in.',
    'Take a deep stretch at the bottom without the shoulders rolling forward.',
    'Press up and slightly together at the top.',
  ],
  'Dumbbell Incline Press': [
    'Bench at ~30°; start with the bells at the outer chest.',
    'Drive up without banging the bells together.',
    'Lower to a full stretch under control.',
  ],
  'Dumbbell Decline Press': [
    'Press to the lower-chest line.',
    'Keep the feet hooked and anchored.',
    'Control the eccentric — the stretch position is heavily loaded.',
  ],
  'Dumbbell Flye (flat)': [
    'Hold a soft elbow bend and keep it constant.',
    'Open the arms until you feel a chest stretch, not shoulder pain.',
    'Hug the arc back up like wrapping your arms around a barrel.',
  ],
  'Incline Dumbbell Flye': [
    'Same fixed-elbow arc on a 30° bench.',
    'Feel the stretch across the upper chest.',
    'Don’t turn it into a press — the elbow angle never changes.',
  ],
  'Machine Chest Press': [
    'Set the seat so the handles sit mid-chest.',
    'Pin the shoulder blades to the pad.',
    'Press fully without the shoulders rolling forward at lockout.',
  ],
  'Incline Machine Press': [
    'Handles start just below the collarbone.',
    'Drive up and slightly in.',
    'Pause a beat in the stretch each rep.',
  ],
  'Pec Deck / Machine Flye': [
    'Elbows slightly below shoulder height.',
    'Squeeze the pads together and hold for a second.',
    'Open only to a comfortable stretch — no yanking.',
  ],
  'Cable Crossover (high to low)': [
    'Split stance, slight forward lean.',
    'Sweep the hands down and in to the hip line.',
    'Squeeze, then resist the return — don’t get pulled open.',
  ],
  'Cable Crossover (low to high)': [
    'Pulleys low; sweep up to eye line.',
    'Finish thumbs-up with the hands together.',
    'Upper-chest focus — keep the shoulders down.',
  ],
  'Cable Flye (mid)': [
    'Pulleys at chest height, slight forward lean.',
    'Hands meet at the sternum with soft elbows.',
    'Constant tension — control both directions.',
  ],
  'Chest Dip': [
    'Lean the torso forward ~30° with elbows slightly out.',
    'Lower to a stretch across the chest.',
    'Keep the neck long — don’t bury the chin.',
  ],
  'Weighted Chest Dip': [
    'Same forward lean with the belt weight hanging still.',
    'Control the descent — no bounce at the bottom.',
    'Keep depth honest: upper arm to about parallel.',
  ],
  'Svend Press': [
    'Squeeze the plates together hard — that squeeze IS the exercise.',
    'Press straight out at chest height.',
    'Return slowly without losing plate pressure.',
  ],
  'Landmine Press': [
    'Staggered stance, bar racked at the shoulder.',
    'Press up and forward along the bar’s natural arc.',
    'Brace the ribs down — don’t lean away from the bar.',
  ],
  'Smith Machine Bench Press': [
    'Set the safeties first.',
    'The bar path is fixed — position your body so elbows track ~45°.',
    'Treat it like paused reps; the machine hides momentum.',
  ],
  'Floor Press': [
    'Let the upper arms settle to the floor each rep — a dead pause.',
    'Press without bouncing the hips.',
    'Short range, big lockout focus.',
  ],

  // ---- BACK ----
  'Pull-Up': [
    'Start from a dead hang, avoid kipping.',
    'Drive the elbows down and back, chest to the bar.',
    'Control the eccentric — 2–3 seconds down.',
  ],
  'Chin-Up (supinated)': [
    'Supinated, shoulder-width grip.',
    'Lead with the chest to the bar.',
    'Ribs down — no wild swinging.',
  ],
  'Assisted Pull-Up (band/machine)': [
    'Same strict form as an unassisted pull-up.',
    'Pick assistance that allows 6–10 clean reps.',
    'Fight the negative — that’s where the strength builds.',
  ],
  'Lat Pulldown (wide)': [
    'Grip a little wider than the shoulders.',
    'Pull the bar to the upper chest, elbows driving to the hips.',
    'Don’t lean way back to cheat the weight down.',
  ],
  'Lat Pulldown (close/neutral)': [
    'Neutral close grip, elbows tight in front.',
    'Take a big stretch at the top of every rep.',
    'Pull to the collarbone, pause, control up.',
  ],
  'Straight-Arm Pulldown': [
    'Arms nearly straight, slight hip hinge.',
    'Sweep the bar to the thighs with the lats, not the triceps.',
    'Keep the shoulders away from the ears throughout.',
  ],
  'Barbell Bent-Over Row': [
    'Hinge to ~45° torso angle, flat back throughout.',
    'Pull to the lower ribcage, elbows close to the body.',
    'Avoid using momentum — control the negative.',
  ],
  'Pendlay Row': [
    'The bar starts dead on the floor every rep.',
    'Torso parallel to the floor, back set hard.',
    'Explode to the lower chest, then set it back down flat.',
  ],
  'T-Bar Row': [
    'Chest tall over the bar, hips back.',
    'Pull the handle to the sternum with elbows tight.',
    'No rebound out of the bottom.',
  ],
  'Dumbbell Single-Arm Row': [
    'Knee and hand braced on the bench, back flat.',
    'Pull to the hip pocket, not the armpit.',
    'Zero torso twist — the shoulders stay square.',
  ],
  'Chest-Supported Dumbbell Row': [
    'Chest glued to the pad — it never leaves.',
    'Row the elbows back and squeeze the blades.',
    'The pad kills momentum: embrace the strictness.',
  ],
  'Seated Cable Row (wide)': [
    'Pull to the upper chest with the elbows out.',
    'Hold the squeeze for a full second.',
    'Torso vertical — minimal rocking.',
  ],
  'Seated Cable Row (close)': [
    'Handle to the belly button, elbows tight.',
    'Stretch forward at the start while keeping the low back neutral.',
    'Drive the chest up to meet the handle.',
  ],
  'Machine Row (Hammer Strength)': [
    'Chest on the pad, feet planted.',
    'Let the blades protract for a full stretch.',
    'Row and pause at the pocket every rep.',
  ],
  'Inverted Row / Bodyweight Row': [
    'Rigid plank line from hips to heels.',
    'Pull the chest to the bar.',
    'Elevate the feet to make it harder.',
  ],
  'Meadows Row': [
    'Staggered stance beside the landmine.',
    'Wide grip on the bar sleeve, row to the hip.',
    'Take the big stretch at the bottom — that’s the magic.',
  ],
  'Deadlift (conventional)': [
    'Bar over mid-foot, shins close before the pull.',
    'Take the slack out of the bar, then drive the floor away.',
    'Keep the bar in contact with the legs on the way up.',
    'Lock out with glutes, not a lumbar hyperextension.',
  ],
  'Rack Pull': [
    'Set the pins just below the knee.',
    'Wedge the hips in exactly like a deadlift.',
    'Drag the bar up the thighs and finish with the glutes.',
  ],
  'Back Extension (hyperextension)': [
    'Hinge at the hips over the pad, not the spine.',
    'Rise to a straight line — no hyperextension at the top.',
    'Squeeze the glutes to finish.',
  ],
  'Weighted Back Extension': [
    'Hold the plate at the chest.',
    'Take three slow seconds down.',
    'Same straight-line finish — no whipping up.',
  ],
  'Face Pull': [
    'Set the rope at face height.',
    'Pull toward the eyes with the elbows high.',
    'Finish thumbs-back — the external rotation is the point.',
  ],
  'Shrug-to-Row Combo': [
    'Shrug straight up, then flow into a row with the elbows back.',
    'Keep the neck long and the chin level.',
    'Light weight, crisp tempo.',
  ],
  'Renegade Row': [
    'Push-up plank on the bells, feet wide.',
    'Row one bell without letting the hips twist.',
    'The anti-rotation is the exercise — slow it down.',
  ],

  // ---- SHOULDERS ----
  'Overhead Barbell Press (standing)': [
    'Brace the core and glutes to avoid a lumbar arch.',
    'Bar path travels straight up; move the head through at lockout.',
    'Finish with the bar stacked over mid-foot.',
  ],
  'Seated Barbell Press': [
    'Sit tall — don’t over-arch off the pad.',
    'Lower to chin height under control.',
    'Press to lockout directly over the ears.',
  ],
  'Dumbbell Shoulder Press': [
    'Bells at the shoulders, forearms vertical.',
    'Press up and slightly in.',
    'Lower to ear level under control.',
  ],
  'Arnold Press': [
    'Start palms facing you at chin height.',
    'Rotate outward as you press.',
    'Reverse the spiral on the way down.',
  ],
  'Machine Shoulder Press': [
    'Set the seat so the handles start at shoulder height.',
    'Drive up without shrugging into the ears.',
    'Stop just short of a harsh lockout.',
  ],
  'Dumbbell Lateral Raise': [
    'Lead with the elbows, slight fixed bend.',
    'Raise to shoulder height with the pinkie slightly up.',
    'No swinging — go lighter than your ego says.',
  ],
  'Cable Lateral Raise': [
    'Cable runs behind the body from the low pulley.',
    'Constant tension from bottom to top.',
    'Pause for a beat at parallel.',
  ],
  'Machine Lateral Raise': [
    'Pads at the elbows, seat adjusted so shoulders line up with the cam.',
    'Push out and up to parallel.',
    'Take the negative slow.',
  ],
  'Dumbbell Front Raise': [
    'Raise to eye line with the thumb slightly up.',
    'Don’t rock the torso for momentum.',
    'Alternate arms to stay strict.',
  ],
  'Plate Front Raise': [
    'Grip the plate at 3 and 9 o’clock.',
    'Raise to eye level with soft elbows.',
    'Lower under control — no dropping.',
  ],
  'Rear Delt Dumbbell Flye': [
    'Hinge flat, bells hanging under the chest.',
    'Open wide with the pinkies leading up.',
    'Stop at the shoulder line — no trap shrug.',
  ],
  'Reverse Pec Deck': [
    'Arms nearly straight, push the handles back.',
    'Lead with the rear delts, not a blade pinch.',
    'Pause one second at the back.',
  ],
  'Cable Rear Delt Flye': [
    'Cross the cables and pull out and back.',
    'Elbows soft, hands at shoulder height.',
    'Slow return — stay tensioned.',
  ],
  'Upright Row (barbell)': [
    'Grip just inside shoulder width.',
    'Pull the elbows — not the hands — to shoulder height.',
    'Stop the range if the wrists or shoulders pinch.',
  ],
  'Upright Row (cable)': [
    'Same elbow lead as the barbell version.',
    'Keep the bar close to the body.',
    'Constant tension — no jerking off the stack.',
  ],
  'Landmine Press (shoulder)': [
    'Half-kneeling or staggered stance.',
    'Press along the bar’s arc, not straight up.',
    'Ribs down — resist arching away from it.',
  ],
  'Pike Push-Up': [
    'Hips high, torso as vertical as possible.',
    'Lower the head between the hands toward the floor.',
    'Press back up to the pike — don’t drift forward.',
  ],
  'Handstand Push-Up': [
    'Kick to the wall with the body stacked.',
    'Lower the head to the mat with elbows ~45°.',
    'Press to full lockout — hollow body, no banana back.',
  ],
  'Behind-the-Neck Press': [
    'Only with proven shoulder mobility.',
    'Lower just to ear level, not all the way down.',
    'Strict tempo and moderate loads.',
  ],
  'Cable Y-Raise': [
    'Raise the arms up and out into a Y.',
    'Thumbs up, shoulders away from the ears.',
    'Go light and feel the lower traps do the work.',
  ],

  // ---- BICEPS ----
  'Barbell Curl': [
    'Pin the elbows at your sides.',
    'Curl without leaning back.',
    'Lower for 2–3 seconds to a fully straight arm.',
  ],
  'EZ-Bar Curl': [
    'Hands on the angled grips — easier on the wrists.',
    'Same pinned elbows as a straight bar.',
    'Squeeze hard at the top of every rep.',
  ],
  'Dumbbell Curl (alternating)': [
    'Supinate as you curl — pinkie turns up.',
    'One arm at a time, no rocking.',
    'Full stretch at the bottom of each rep.',
  ],
  'Hammer Curl': [
    'Neutral grip the whole way.',
    'Optionally curl slightly across the body.',
    'The slow lowering builds the forearms too.',
  ],
  'Incline Dumbbell Curl': [
    'Lie back 45–60° so the arms hang behind the body.',
    'That stretch is huge — start lighter than usual.',
    'Keep the shoulders pinned to the bench.',
  ],
  'Concentration Curl': [
    'Brace the elbow inside the thigh.',
    'Curl to the shoulder without the upper arm moving.',
    'Hold the peak squeeze for a second.',
  ],
  'Preacher Curl (barbell)': [
    'Armpits tight to the top of the pad.',
    'Never bounce out of the full stretch.',
    'Curl to the chin and lower slow.',
  ],
  'Preacher Curl (machine)': [
    'Adjust the seat so the armpits meet the pad.',
    'Use the full range in both directions.',
    'Let the machine keep you honest — no shortcuts.',
  ],
  'Cable Curl (straight bar)': [
    'Step back from the stack for tension at the bottom.',
    'Elbows stay at your sides.',
    'Constant tension — no resting at top or bottom.',
  ],
  'Cable Rope Hammer Curl': [
    'Neutral grip on the rope ends.',
    'Pull the rope slightly apart as you curl.',
    'Keep the wrists dead straight.',
  ],
  'Spider Curl': [
    'Chest down on an incline bench, arms hanging vertical.',
    'Strict curl — swinging is impossible here.',
    'Squeeze hard at the top.',
  ],
  'Zottman Curl': [
    'Curl up palms-up.',
    'Rotate palms-down at the top.',
    'Lower slowly pronated — the way down is the point.',
  ],
  'Chin-Up (biceps focus)': [
    'Shoulder-width supinated grip.',
    'Think “pull the hands to the shoulders.”',
    'Slow negatives grow arms.',
  ],
  '21s (barbell)': [
    'Seven bottom-half reps, seven top-half, seven full.',
    'No rest between the segments.',
    'Go lighter than your normal curl weight.',
  ],
  'Bayesian Cable Curl': [
    'Face away from the low pulley, arm trailing behind.',
    'Curl from that deep stretch position.',
    'Stay strict — the stretch does the work.',
  ],

  // ---- TRICEPS ----
  'Triceps Pushdown (rope)': [
    'Elbows pinned; split the rope at the bottom.',
    'Full lockout with the knuckles turning down.',
    'Don’t lean your bodyweight over the cable.',
  ],
  'Triceps Pushdown (bar)': [
    'Elbows at the sides the entire set.',
    'Wrists straight — push with the palms.',
    'Control back past 90° without the elbows drifting up.',
  ],
  'Overhead Cable Extension': [
    'Face away from the stack with arms overhead.',
    'Extend without flaring the elbows wide.',
    'Sink into a deep stretch behind the head each rep.',
  ],
  'Overhead Dumbbell Extension': [
    'Both palms under one bell.',
    'Lower behind the head to a full stretch.',
    'Elbows point forward, ribs stay down.',
  ],
  'Skull Crusher (EZ-bar)': [
    'Lower the bar to the hairline or just behind the head.',
    'Elbows fixed — no turning it into a press.',
    'End the set when the elbows complain, not after.',
  ],
  'Close-Grip Bench Press': [
    'Grip just inside shoulder width.',
    'Elbows tucked tight to the sides.',
    'Touch the low chest and press without flaring.',
  ],
  'Triceps Dip (bench)': [
    'Hands behind you on the bench, feet out front.',
    'Keep the hips close to the bench edge.',
    'Shoulders down — shrugging here is how they get hurt.',
  ],
  'Parallel Bar Dip': [
    'Upright torso keeps it on the triceps.',
    'Lock out completely at the top.',
    'Bend to ~90° — no deeper if the shoulders bark.',
  ],
  'Dumbbell Kickback': [
    'Torso parallel, upper arm frozen level with the back.',
    'Extend to dead straight and pause.',
    'Light weight, real lockout.',
  ],
  'Cable Kickback': [
    'Upper arm pinned to the side.',
    'Strict extension with constant cable tension.',
    'Pause at full lockout each rep.',
  ],
  'Diamond Push-Up': [
    'Hands form a triangle under the chest.',
    'Elbows track straight back.',
    'Same rigid plank rules as any push-up.',
  ],
  'Machine Triceps Extension': [
    'Elbows set on the pad at the right depth.',
    'Extend fully, return under control.',
    'Don’t let the shoulders creep up as you fatigue.',
  ],
  'JM Press': [
    'Hybrid path — the bar tracks to the chin/neck line.',
    'Elbows stay tucked and high.',
    'Moderate load; the groove matters more than weight.',
  ],
  'Tate Press': [
    'Bells over the chest; lower the plates inward to the sternum.',
    'Elbows stay out — it should look like a butterfly closing.',
    'Press back up along the same path, slow and controlled.',
  ],

  // ---- FOREARMS & GRIP ----
  'Wrist Curl (barbell)': [
    'Forearms on the thighs or bench, palms up.',
    'Curl the wrist through its full range.',
    'Let the bar roll to the fingertips for an extra stretch.',
  ],
  'Reverse Wrist Curl': [
    'Palms down, forearms braced.',
    'Lift the knuckles up slowly.',
    'Much lighter than regular wrist curls — respect that.',
  ],
  'Reverse Barbell Curl': [
    'Palms-down grip, elbows pinned.',
    'Wrists straight — no flicking at the top.',
    'Builds the brachioradialis; expect humbling weights.',
  ],
  "Farmer's Carry": [
    'Heavy handles, shoulders packed down and back.',
    'Walk tall with the ribs stacked over the hips.',
    'Grip to near-failure but never to a stagger.',
  ],
  'Dead Hang': [
    'Full grip on the bar, shoulders slightly engaged.',
    'Breathe and accumulate time — build toward 60s+.',
    'Finish a hang with a few active scap pulls.',
  ],
  'Plate Pinch': [
    'Pinch smooth plates between fingers and thumb.',
    'Stand tall; time the holds.',
    'Add seconds before you add weight.',
  ],
  'Wrist Roller': [
    'Arms out, roll the weight all the way up.',
    'Wind it both directions — flexors and extensors.',
    'A small plate is brutal; start there.',
  ],
  'Cable Wrist Curl': [
    'Low pulley, forearm braced on a bench.',
    'Move only the wrist.',
    'Constant cable tension beats free weights here.',
  ],

  // ---- QUADRICEPS ----
  'Bodyweight Squat': [
    'Feet shoulder-width, toes slightly out.',
    'Sit down between the hips with the chest tall.',
    'Full depth with the heels planted.',
  ],
  'Barbell Back Squat': [
    'Brace hard before the descent — 360° of air pressure.',
    'Break at the hips and knees together, track knees over toes.',
    'Hit depth (hip crease below knee) without losing the arch.',
    'Drive up through the whole foot, not just the heels.',
  ],
  'Barbell Front Squat': [
    'Bar rides on the front delts with elbows high.',
    'Torso stays vertical the whole rep.',
    'Drop the hips straight down between the heels.',
  ],
  'Goblet Squat': [
    'Hold the bell tight to the chest.',
    'Elbows track inside the knees at the bottom.',
    'The best depth teacher there is — pause down there.',
  ],
  'Hack Squat (machine)': [
    'Back flat on the pad, feet mid-platform.',
    'Lower to a deep knee bend.',
    'Drive through the whole foot; don’t slam the lockout.',
  ],
  'Leg Press': [
    'Feet shoulder width, mid-platform.',
    'Lower until the knees reach ~90° without the low back rounding off the pad.',
    'Drive through the full foot; avoid slamming the knees at lockout.',
  ],
  'Leg Extension': [
    'Pad just above the ankle.',
    'Extend to full lockout and squeeze for a second.',
    'Lower slow — never let the stack slam.',
  ],
  'Bulgarian Split Squat': [
    'Rear foot laced on the bench behind you.',
    'Drop the back knee straight down.',
    'Front heel drives; a slight forward lean shifts it to the glute.',
  ],
  'Walking Lunge': [
    'Long strides with the knee tracking over the toes.',
    'Back knee kisses the floor.',
    'Push off the front heel into the next step.',
  ],
  'Reverse Lunge': [
    'Step back and drop the knee under the hip.',
    'Front shin stays vertical.',
    'Easier on the knees than forward lunges.',
  ],
  'Step-Up': [
    'Whole foot on a knee-height box.',
    'Drive through the top leg only — no push off the floor.',
    'Control the way down; don’t drop.',
  ],
  'Smith Machine Squat': [
    'Set the feet slightly forward of the bar.',
    'Sit into the fixed path.',
    'Great for burnout work — not a back-squat replacement.',
  ],
  'Sissy Squat': [
    'Knees travel far forward while the hips stay extended.',
    'Body forms one line from knees to head.',
    'Hold support and keep the range short at first — the quad stretch is intense.',
  ],
  'Box Squat': [
    'Sit fully to the box and pause — no rocking.',
    'Shins vertical, hips pushed back.',
    'Explode up without leaning into momentum.',
  ],
  'Pause Squat': [
    'Two to three seconds dead in the hole.',
    'Stay braced — never relax down there.',
    'Drive up with zero bounce.',
  ],

  // ---- HAMSTRINGS ----
  'Romanian Deadlift (barbell)': [
    'Soft knee bend, hinge from the hips.',
    'Bar stays close; feel the hamstring stretch at mid-shin.',
    'Squeeze the glutes to reverse — don’t round the low back.',
  ],
  'Dumbbell RDL': [
    'Same hinge with the bells tracing the thighs.',
    'Bells stay against the legs, never drifting forward.',
    'Stretch to mid-shin at most.',
  ],
  'Stiff-Leg Deadlift': [
    'Knees nearly straight — just soft.',
    'More forward reach means more stretch.',
    'Lighter than RDLs, slower tempo.',
  ],
  'Lying Leg Curl': [
    'Hips pressed into the pad the whole set.',
    'Curl the heels to the glutes.',
    'Three seconds down — don’t let the hips pop up.',
  ],
  'Seated Leg Curl': [
    'Back tight to the pad, thighs clamped.',
    'Curl under and hold the squeeze.',
    'Better stretch than the lying version — use all of it.',
  ],
  'Nordic Hamstring Curl': [
    'Knees anchored; fall forward as slowly as possible.',
    'Catch with the hands and push back lightly.',
    'Fight for every degree of the descent.',
  ],
  'Good Morning': [
    'Bar high on the back; hinge to ~45° or deeper.',
    'Knees soft, hips shooting back.',
    'Return by driving the hips forward, not lifting the chest.',
  ],
  'Glute-Ham Raise': [
    'Toes pressed into the plate, knees on the pad.',
    'Curl the body up with the hamstrings.',
    'Hips stay extended — no jackknifing.',
  ],
  'Cable Pull-Through': [
    'Face away with the rope between the legs.',
    'Hinge — don’t squat.',
    'Snap the hips through and squeeze the glutes.',
  ],
  'Single-Leg RDL': [
    'Hinge on one leg with the rear leg reaching back.',
    'Keep the hips square to the floor.',
    'Touch the weight to mid-shin and drive through the heel.',
  ],
  'Kettlebell Swing': [
    'It’s a hinge — not a squat.',
    'Snap the hips; the arms are just ropes.',
    'The bell floats to chest height and rides back high between the legs.',
  ],

  // ---- GLUTES ----
  'Barbell Hip Thrust': [
    'Shoulder blades on the bench, drive through the heels.',
    'Chin tucked and ribs down at the top.',
    'Full lockout with a one-second glute squeeze.',
  ],
  'Glute Bridge (bodyweight)': [
    'Heels close to the hips.',
    'Push the floor away and squeeze at the top.',
    'Don’t arch the lower back to fake extra range.',
  ],
  'Machine Hip Thrust': [
    'Pad across the hips, back set on the bench.',
    'Reach full extension every rep.',
    'Slow negatives keep the tension on.',
  ],
  'Cable Kickback (glute)': [
    'Slight hinge, kick the leg back and up.',
    'Squeeze the glute — no lower-back arch.',
    'Small strict arc beats a big sloppy one.',
  ],
  'Hip Abduction Machine': [
    'Sit tall (lean forward slightly for upper glute).',
    'Push the knees apart and pause.',
    'Slow return — never let it slam shut.',
  ],
  'Banded Lateral Walk': [
    'Band at the knees or ankles, quarter-squat height.',
    'Step wide and keep band tension always.',
    'Don’t drag the trailing foot.',
  ],
  'Sumo Deadlift': [
    'Wide stance, toes out, shins vertical.',
    'Wedge the hips low with the chest tall.',
    'Push the floor apart and drive the hips through.',
  ],
  'Curtsy Lunge': [
    'Step behind and across the body.',
    'Front knee keeps tracking over the toes.',
    'Feel the outer glute — go light.',
  ],
  'Frog Pump': [
    'Soles of the feet together, knees flared out.',
    'Pump the hips up with a hard glute squeeze.',
    'High reps, constant tension.',
  ],
  'Single-Leg Hip Thrust': [
    'One foot planted, the other leg held up.',
    'Hips stay square — no dipping to one side.',
    'Full lockout every rep.',
  ],
  'Step-Up (glute focus)': [
    'Use a higher box with a slight forward lean.',
    'Drive through the heel.',
    'Squeeze the glute standing fully tall at the top.',
  ],

  // ---- CALVES ----
  'Standing Calf Raise (machine)': [
    'Pause in the full stretch at the bottom.',
    'Rise to tiptoe and hold for a second.',
    'Straight knees keep it on the gastrocnemius.',
  ],
  'Seated Calf Raise': [
    'The bent knee targets the soleus.',
    'Same rules: pause the stretch, pause the top.',
    'Slow beats heavy on calves.',
  ],
  'Standing Calf Raise (dumbbell)': [
    'One hand for support, the bell in the other.',
    'Single leg on a step for full range.',
    'Strict pauses at both ends.',
  ],
  'Leg Press Calf Raise': [
    'Balls of the feet low on the platform.',
    'Knees nearly straight throughout.',
    'Never let the heels crash downward.',
  ],
  'Single-Leg Calf Raise': [
    'Full bodyweight on one side.',
    'Use the wall for balance only, not assistance.',
    'Add a dumbbell once 15+ reps are clean.',
  ],
  'Smith Machine Calf Raise': [
    'Bar on the shoulders, toes on a block.',
    'Deep stretch to a tall finish.',
    'Keep the knee position consistent rep to rep.',
  ],
  'Donkey Calf Raise': [
    'Hinge over the pad with the hips back.',
    'The hip position adds stretch — use it.',
    'A classic for a reason: pause at the bottom.',
  ],
  'Tibialis Raise': [
    'Heels on the ground or a step edge; lift the toes.',
    'Slow reps until the shins burn.',
    'Balances out all the calf raise volume.',
  ],

  // ---- ABS & CORE ----
  'Plank': [
    'Stack the shoulders over the elbows; brace like taking a punch.',
    'Straight line from head to heels — squeeze glutes and quads.',
    'End the set when the hips sag, not the clock.',
  ],
  'Side Plank': [
    'Elbow under the shoulder, feet stacked.',
    'Lift the hips to one straight line.',
    'Top arm up or on the hip — don’t roll forward.',
  ],
  'Crunch': [
    'Curl the ribs toward the hips, not the neck to the chest.',
    'Exhale on the way up and hold the squeeze.',
    'Lower one vertebra at a time.',
  ],
  'Bicycle Crunch': [
    'Opposite elbow to knee, slowly.',
    'Fully extend the other leg each time.',
    'Rotate from the ribs — not flapping elbows.',
  ],
  'Hanging Leg Raise': [
    'Raise straight legs to parallel or higher.',
    'Kill the swing between reps.',
    'Curl the pelvis up at the top for the lower abs.',
  ],
  'Hanging Knee Raise': [
    'Knees to the chest with the pelvis curling.',
    'Lower slowly to a dead hang.',
    'The stepping stone to straight-leg raises.',
  ],
  'Cable Crunch': [
    'Kneel with the rope held behind the head.',
    'Crunch the ribs to the pelvis — the hips stay still.',
    'Flex the spine; don’t just sit back.',
  ],
  'Ab Wheel Rollout': [
    'Start from the knees with the hips pushed forward.',
    'Roll out only as far as the back stays flat.',
    'Pull back with the abs, not the arms.',
  ],
  'Russian Twist': [
    'Lean back 45° with a proud chest.',
    'Rotate the shoulders fully side to side.',
    'Feet down is easier; feet up is harder.',
  ],
  'Mountain Climber': [
    'The plank stays rigid throughout.',
    'Drive the knees fast without bouncing the hips.',
    'Shoulders stay stacked over the wrists.',
  ],
  'Dead Bug': [
    'Press the low back into the floor and keep it there.',
    'Opposite arm and leg reach out slowly.',
    'Exhale as the limbs extend.',
  ],
  'Leg Raise (lying)': [
    'Hands under the hips if needed.',
    'Legs to vertical, then lower slow.',
    'Stop the descent where the back starts to arch.',
  ],
  'Decline Sit-Up': [
    'Anchor the feet, arms crossed on the chest.',
    'Curl up smoothly — never yank the neck.',
    'Hold a plate at the chest to progress.',
  ],
  'Pallof Press': [
    'Cable at chest height; press straight out.',
    'Resist the rotation — that resistance is the rep.',
    'Hold three seconds pressed, controlled return.',
  ],
  'Toes-to-Bar': [
    'Hang with active shoulders, lats pulling down.',
    'Lift the toes all the way to the bar.',
    'Small kip or strict — but always controlled.',
  ],
  'Woodchopper (cable)': [
    'Rotate high-to-low across the body.',
    'Arms stay mostly straight; pivot the back foot.',
    'Power comes from the hips, not the arms.',
  ],
  'Hollow Body Hold': [
    'Weld the low back to the floor.',
    'Arms and legs long, ribs pulled down.',
    'Scale by tucking the knees in.',
  ],

  // ---- TRAPS & NECK ----
  'Barbell Shrug': [
    'Shoulders straight up toward the ears.',
    'Hold the top for a full second.',
    'No rolling — straight up, straight down.',
  ],
  'Dumbbell Shrug': [
    'Bells at the sides.',
    'Shrug up and slightly back.',
    'Long neck, level chin.',
  ],
  'Cable Shrug': [
    'Constant tension from the low pulley.',
    'Strict pause at the top.',
    'Stretch at the bottom without slouching forward.',
  ],
  'Trap Bar Shrug': [
    'Neutral handles put the load in a natural line.',
    'Big weights are fine — form stays strict.',
    'Hold the peak for one to two seconds.',
  ],
  "Farmer's Carry (trap)": [
    'Heavy carry — the traps load isometrically.',
    'Walk tall; no active shrugging needed.',
    'Work to time or distance targets.',
  ],
  'Neck Curl (plate)': [
    'Lie face-up with a towel-wrapped plate on the forehead.',
    'Nod the chin toward the chest slowly.',
    'Tiny range, tiny loads, maximum care.',
  ],
  'Neck Extension': [
    'Face-down with the plate on the back of the head.',
    'Lift the head slowly to neutral and slightly past.',
    'Never jerk — build this up gradually.',
  ],

  // ---- FULL-BODY & COMPOUND ----
  'Clean & Press': [
    'Clean the bar to the front rack and stand fully.',
    'Dip-drive into the press overhead.',
    'Reset each rep — no rushed cycling.',
  ],
  'Power Clean': [
    'Bar close; explode through the hips.',
    'Catch high on the shoulders with the elbows whipping through.',
    'Stand completely before dropping the bar.',
  ],
  'Snatch': [
    'Wide grip; the bar travels overhead in one motion.',
    'Punch under and catch in the squat.',
    'Mobility and coaching first — this is the most technical lift.',
  ],
  'Thruster': [
    'Front squat flowing straight into a press.',
    'The legs power the bar — arms finish it.',
    'Breathe at the top of every rep.',
  ],
  'Turkish Get-Up': [
    'Eyes on the bell the entire way up.',
    'Each position is a checkpoint — own it before moving.',
    'Slow is the point.',
  ],
  'Burpee': [
    'Chest to the floor, snap the hips back to the feet.',
    'Jump and open the hips fully at the top.',
    'A steady rhythm beats sprint-and-die.',
  ],
  'Kettlebell Clean': [
    'The bell spirals around the wrist — no banging.',
    'Elbow lands tight to the ribs in the rack.',
    'Hips power it; the arm just guides.',
  ],
  "Devil's Press": [
    'A dumbbell burpee into a double swing-snatch overhead.',
    'Brace before every lift-off the floor.',
    'Pick a pace you can actually sustain.',
  ],
  'Trap Bar Deadlift': [
    'Stand centered with a neutral grip.',
    'Push the floor away — hips and knees extend together.',
    'The friendliest deadlift to learn; still brace hard.',
  ],
  'Bear Crawl': [
    'Knees hover two inches off the floor.',
    'Opposite hand and foot move together.',
    'Hips stay level like a tabletop.',
  ],
  'Wall Ball': [
    'Squat to full depth with the ball at the chest.',
    'Drive up and throw to the target in one flow.',
    'Catch soft and sink straight into the next rep.',
  ],
};

exports.CUES_BY_NAME = CUES_BY_NAME;
});

__def("scripts/data/exerciseAnimations.js", function(module, exports, __require){
// Optional animated demos (GIF/WebP/APNG), keyed by exact exercise name.
//
// The 3D-rendered animated exercise demos used by commercial fitness apps
// (the white-background anatomical style) are © GymVisual (gymvisual.com)
// and require a paid license to redistribute — they are NOT bundled here.
//
// If you license them (or any other animated demos):
//   1. Put the files in  app/images/anim/
//   2. Add one entry per exercise below, e.g.
//        'Barbell Bench Press (flat)': 'images/anim/barbell-bench-press.gif',
//   3. Reload — the app automatically prefers an animation listed here over
//      the built-in two-frame photo demo. User photo uploads still override.

const ANIMATIONS_BY_NAME = {};

exports.ANIMATIONS_BY_NAME = ANIMATIONS_BY_NAME;
});

__def("scripts/data/exerciseImages.js", function(module, exports, __require){
// Demo photo manifest — real demonstration photos for each exercise,
// sourced from the public-domain free-exercise-db (see ATTRIBUTION.md).
// Two frames per exercise: start and end position.

const IMAGES_BY_NAME = {
  "21s (barbell)": ["images/exercises/Barbell_Curl/0.jpg", "images/exercises/Barbell_Curl/1.jpg"],
  "Ab Wheel Rollout": ["images/exercises/Ab_Roller/0.jpg", "images/exercises/Ab_Roller/1.jpg"],
  "Arnold Press": ["images/exercises/Arnold_Dumbbell_Press/0.jpg", "images/exercises/Arnold_Dumbbell_Press/1.jpg"],
  "Assisted Pull-Up (band/machine)": ["images/exercises/Band_Assisted_Pull-Up/0.jpg", "images/exercises/Band_Assisted_Pull-Up/1.jpg"],
  "Back Extension (hyperextension)": ["images/exercises/Hyperextensions_Back_Extensions/0.jpg", "images/exercises/Hyperextensions_Back_Extensions/1.jpg"],
  "Barbell Back Squat": ["images/exercises/Barbell_Squat/0.jpg", "images/exercises/Barbell_Squat/1.jpg"],
  "Barbell Bench Press (flat)": ["images/exercises/Barbell_Bench_Press_-_Medium_Grip/0.jpg", "images/exercises/Barbell_Bench_Press_-_Medium_Grip/1.jpg"],
  "Barbell Bent-Over Row": ["images/exercises/Bent_Over_Barbell_Row/0.jpg", "images/exercises/Bent_Over_Barbell_Row/1.jpg"],
  "Barbell Curl": ["images/exercises/Barbell_Curl/0.jpg", "images/exercises/Barbell_Curl/1.jpg"],
  "Barbell Decline Bench Press": ["images/exercises/Decline_Barbell_Bench_Press/0.jpg", "images/exercises/Decline_Barbell_Bench_Press/1.jpg"],
  "Barbell Front Squat": ["images/exercises/Front_Barbell_Squat/0.jpg", "images/exercises/Front_Barbell_Squat/1.jpg"],
  "Barbell Hip Thrust": ["images/exercises/Barbell_Hip_Thrust/0.jpg", "images/exercises/Barbell_Hip_Thrust/1.jpg"],
  "Barbell Incline Bench Press": ["images/exercises/Barbell_Incline_Bench_Press_-_Medium_Grip/0.jpg", "images/exercises/Barbell_Incline_Bench_Press_-_Medium_Grip/1.jpg"],
  "Barbell Shrug": ["images/exercises/Barbell_Shrug/0.jpg", "images/exercises/Barbell_Shrug/1.jpg"],
  "Bayesian Cable Curl": ["images/exercises/Standing_One-Arm_Cable_Curl/0.jpg", "images/exercises/Standing_One-Arm_Cable_Curl/1.jpg"],
  "Bear Crawl": ["images/exercises/Bear_Crawl_Sled_Drags/0.jpg", "images/exercises/Bear_Crawl_Sled_Drags/1.jpg"],
  "Behind-the-Neck Press": ["images/exercises/Standing_Barbell_Press_Behind_Neck/0.jpg", "images/exercises/Standing_Barbell_Press_Behind_Neck/1.jpg"],
  "Bicycle Crunch": ["images/exercises/Air_Bike/0.jpg", "images/exercises/Air_Bike/1.jpg"],
  "Bodyweight Squat": ["images/exercises/Bodyweight_Squat/0.jpg", "images/exercises/Bodyweight_Squat/1.jpg"],
  "Box Squat": ["images/exercises/Box_Squat/0.jpg", "images/exercises/Box_Squat/1.jpg"],
  "Bulgarian Split Squat": ["images/exercises/One_Leg_Barbell_Squat/0.jpg", "images/exercises/One_Leg_Barbell_Squat/1.jpg"],
  "Cable Crossover (high to low)": ["images/exercises/Cable_Crossover/0.jpg", "images/exercises/Cable_Crossover/1.jpg"],
  "Cable Crossover (low to high)": ["images/exercises/Cable_Crossover/0.jpg", "images/exercises/Cable_Crossover/1.jpg"],
  "Cable Crunch": ["images/exercises/Cable_Crunch/0.jpg", "images/exercises/Cable_Crunch/1.jpg"],
  "Cable Curl (straight bar)": ["images/exercises/Standing_Biceps_Cable_Curl/0.jpg", "images/exercises/Standing_Biceps_Cable_Curl/1.jpg"],
  "Cable Flye (mid)": ["images/exercises/Flat_Bench_Cable_Flyes/0.jpg", "images/exercises/Flat_Bench_Cable_Flyes/1.jpg"],
  "Cable Kickback": ["images/exercises/Standing_Low-Pulley_One-Arm_Triceps_Extension/0.jpg", "images/exercises/Standing_Low-Pulley_One-Arm_Triceps_Extension/1.jpg"],
  "Cable Kickback (glute)": ["images/exercises/One-Legged_Cable_Kickback/0.jpg", "images/exercises/One-Legged_Cable_Kickback/1.jpg"],
  "Cable Lateral Raise": ["images/exercises/Cable_Seated_Lateral_Raise/0.jpg", "images/exercises/Cable_Seated_Lateral_Raise/1.jpg"],
  "Cable Pull-Through": ["images/exercises/Pull_Through/0.jpg", "images/exercises/Pull_Through/1.jpg"],
  "Cable Rear Delt Flye": ["images/exercises/Cable_Rear_Delt_Fly/0.jpg", "images/exercises/Cable_Rear_Delt_Fly/1.jpg"],
  "Cable Rope Hammer Curl": ["images/exercises/Cable_Hammer_Curls_-_Rope_Attachment/0.jpg", "images/exercises/Cable_Hammer_Curls_-_Rope_Attachment/1.jpg"],
  "Cable Shrug": ["images/exercises/Cable_Shrugs/0.jpg", "images/exercises/Cable_Shrugs/1.jpg"],
  "Cable Wrist Curl": ["images/exercises/Cable_Wrist_Curl/0.jpg", "images/exercises/Cable_Wrist_Curl/1.jpg"],
  "Cable Y-Raise": ["images/exercises/Front_Cable_Raise/0.jpg", "images/exercises/Front_Cable_Raise/1.jpg"],
  "Chest Dip": ["images/exercises/Dips_-_Chest_Version/0.jpg", "images/exercises/Dips_-_Chest_Version/1.jpg"],
  "Chest-Supported Dumbbell Row": ["images/exercises/Dumbbell_Incline_Row/0.jpg", "images/exercises/Dumbbell_Incline_Row/1.jpg"],
  "Chin-Up (biceps focus)": ["images/exercises/Chin-Up/0.jpg", "images/exercises/Chin-Up/1.jpg"],
  "Chin-Up (supinated)": ["images/exercises/Chin-Up/0.jpg", "images/exercises/Chin-Up/1.jpg"],
  "Clean & Press": ["images/exercises/Clean_and_Press/0.jpg", "images/exercises/Clean_and_Press/1.jpg"],
  "Close-Grip Bench Press": ["images/exercises/Close-Grip_Barbell_Bench_Press/0.jpg", "images/exercises/Close-Grip_Barbell_Bench_Press/1.jpg"],
  "Concentration Curl": ["images/exercises/Concentration_Curls/0.jpg", "images/exercises/Concentration_Curls/1.jpg"],
  "Crunch": ["images/exercises/Crunches/0.jpg", "images/exercises/Crunches/1.jpg"],
  "Curtsy Lunge": ["images/exercises/Crossover_Reverse_Lunge/0.jpg", "images/exercises/Crossover_Reverse_Lunge/1.jpg"],
  "Dead Bug": ["images/exercises/Dead_Bug/0.jpg", "images/exercises/Dead_Bug/1.jpg"],
  "Dead Hang": ["images/exercises/Pullups/0.jpg", "images/exercises/Pullups/1.jpg"],
  "Deadlift (conventional)": ["images/exercises/Barbell_Deadlift/0.jpg", "images/exercises/Barbell_Deadlift/1.jpg"],
  "Decline Push-Up (feet elevated)": ["images/exercises/Decline_Push-Up/0.jpg", "images/exercises/Decline_Push-Up/1.jpg"],
  "Decline Sit-Up": ["images/exercises/Decline_Crunch/0.jpg", "images/exercises/Decline_Crunch/1.jpg"],
  "Devil's Press": ["images/exercises/Double_Kettlebell_Snatch/0.jpg", "images/exercises/Double_Kettlebell_Snatch/1.jpg"],
  "Diamond Push-Up": ["images/exercises/Push-Ups_-_Close_Triceps_Position/0.jpg", "images/exercises/Push-Ups_-_Close_Triceps_Position/1.jpg"],
  "Donkey Calf Raise": ["images/exercises/Donkey_Calf_Raises/0.jpg", "images/exercises/Donkey_Calf_Raises/1.jpg"],
  "Dumbbell Bench Press (flat)": ["images/exercises/Dumbbell_Bench_Press/0.jpg", "images/exercises/Dumbbell_Bench_Press/1.jpg"],
  "Dumbbell Curl (alternating)": ["images/exercises/Dumbbell_Alternate_Bicep_Curl/0.jpg", "images/exercises/Dumbbell_Alternate_Bicep_Curl/1.jpg"],
  "Dumbbell Decline Press": ["images/exercises/Decline_Dumbbell_Bench_Press/0.jpg", "images/exercises/Decline_Dumbbell_Bench_Press/1.jpg"],
  "Dumbbell Flye (flat)": ["images/exercises/Dumbbell_Flyes/0.jpg", "images/exercises/Dumbbell_Flyes/1.jpg"],
  "Dumbbell Front Raise": ["images/exercises/Front_Dumbbell_Raise/0.jpg", "images/exercises/Front_Dumbbell_Raise/1.jpg"],
  "Dumbbell Incline Press": ["images/exercises/Incline_Dumbbell_Press/0.jpg", "images/exercises/Incline_Dumbbell_Press/1.jpg"],
  "Dumbbell Kickback": ["images/exercises/Tricep_Dumbbell_Kickback/0.jpg", "images/exercises/Tricep_Dumbbell_Kickback/1.jpg"],
  "Dumbbell Lateral Raise": ["images/exercises/Side_Lateral_Raise/0.jpg", "images/exercises/Side_Lateral_Raise/1.jpg"],
  "Dumbbell RDL": ["images/exercises/Stiff-Legged_Dumbbell_Deadlift/0.jpg", "images/exercises/Stiff-Legged_Dumbbell_Deadlift/1.jpg"],
  "Dumbbell Shoulder Press": ["images/exercises/Dumbbell_Shoulder_Press/0.jpg", "images/exercises/Dumbbell_Shoulder_Press/1.jpg"],
  "Dumbbell Shrug": ["images/exercises/Dumbbell_Shrug/0.jpg", "images/exercises/Dumbbell_Shrug/1.jpg"],
  "Dumbbell Single-Arm Row": ["images/exercises/One-Arm_Dumbbell_Row/0.jpg", "images/exercises/One-Arm_Dumbbell_Row/1.jpg"],
  "EZ-Bar Curl": ["images/exercises/EZ-Bar_Curl/0.jpg", "images/exercises/EZ-Bar_Curl/1.jpg"],
  "Face Pull": ["images/exercises/Face_Pull/0.jpg", "images/exercises/Face_Pull/1.jpg"],
  "Farmer's Carry": ["images/exercises/Farmers_Walk/0.jpg", "images/exercises/Farmers_Walk/1.jpg"],
  "Farmer's Carry (trap)": ["images/exercises/Farmers_Walk/0.jpg", "images/exercises/Farmers_Walk/1.jpg"],
  "Floor Press": ["images/exercises/Floor_Press/0.jpg", "images/exercises/Floor_Press/1.jpg"],
  "Frog Pump": ["images/exercises/Butt_Lift_Bridge/0.jpg", "images/exercises/Butt_Lift_Bridge/1.jpg"],
  "Glute Bridge (bodyweight)": ["images/exercises/Butt_Lift_Bridge/0.jpg", "images/exercises/Butt_Lift_Bridge/1.jpg"],
  "Glute-Ham Raise": ["images/exercises/Glute_Ham_Raise/0.jpg", "images/exercises/Glute_Ham_Raise/1.jpg"],
  "Goblet Squat": ["images/exercises/Goblet_Squat/0.jpg", "images/exercises/Goblet_Squat/1.jpg"],
  "Good Morning": ["images/exercises/Good_Morning/0.jpg", "images/exercises/Good_Morning/1.jpg"],
  "Hack Squat (machine)": ["images/exercises/Hack_Squat/0.jpg", "images/exercises/Hack_Squat/1.jpg"],
  "Hammer Curl": ["images/exercises/Hammer_Curls/0.jpg", "images/exercises/Hammer_Curls/1.jpg"],
  "Handstand Push-Up": ["images/exercises/Handstand_Push-Ups/0.jpg", "images/exercises/Handstand_Push-Ups/1.jpg"],
  "Hanging Knee Raise": ["images/exercises/Hanging_Leg_Raise/0.jpg", "images/exercises/Hanging_Leg_Raise/1.jpg"],
  "Hanging Leg Raise": ["images/exercises/Hanging_Leg_Raise/0.jpg", "images/exercises/Hanging_Leg_Raise/1.jpg"],
  "Hip Abduction Machine": ["images/exercises/Thigh_Abductor/0.jpg", "images/exercises/Thigh_Abductor/1.jpg"],
  "Incline Dumbbell Curl": ["images/exercises/Incline_Dumbbell_Curl/0.jpg", "images/exercises/Incline_Dumbbell_Curl/1.jpg"],
  "Incline Dumbbell Flye": ["images/exercises/Incline_Dumbbell_Flyes/0.jpg", "images/exercises/Incline_Dumbbell_Flyes/1.jpg"],
  "Incline Machine Press": ["images/exercises/Leverage_Incline_Chest_Press/0.jpg", "images/exercises/Leverage_Incline_Chest_Press/1.jpg"],
  "Incline Push-Up (hands elevated)": ["images/exercises/Incline_Push-Up/0.jpg", "images/exercises/Incline_Push-Up/1.jpg"],
  "Inverted Row / Bodyweight Row": ["images/exercises/Inverted_Row/0.jpg", "images/exercises/Inverted_Row/1.jpg"],
  "JM Press": ["images/exercises/JM_Press/0.jpg", "images/exercises/JM_Press/1.jpg"],
  "Kettlebell Clean": ["images/exercises/One-Arm_Kettlebell_Clean/0.jpg", "images/exercises/One-Arm_Kettlebell_Clean/1.jpg"],
  "Kettlebell Swing": ["images/exercises/One-Arm_Kettlebell_Swings/0.jpg", "images/exercises/One-Arm_Kettlebell_Swings/1.jpg"],
  "Knee Push-Up": ["images/exercises/Pushups/0.jpg", "images/exercises/Pushups/1.jpg"],
  "Landmine Press": ["images/exercises/Landmine_Linear_Jammer/0.jpg", "images/exercises/Landmine_Linear_Jammer/1.jpg"],
  "Landmine Press (shoulder)": ["images/exercises/Landmine_Linear_Jammer/0.jpg", "images/exercises/Landmine_Linear_Jammer/1.jpg"],
  "Lat Pulldown (close/neutral)": ["images/exercises/Close-Grip_Front_Lat_Pulldown/0.jpg", "images/exercises/Close-Grip_Front_Lat_Pulldown/1.jpg"],
  "Lat Pulldown (wide)": ["images/exercises/Wide-Grip_Lat_Pulldown/0.jpg", "images/exercises/Wide-Grip_Lat_Pulldown/1.jpg"],
  "Leg Extension": ["images/exercises/Leg_Extensions/0.jpg", "images/exercises/Leg_Extensions/1.jpg"],
  "Leg Press": ["images/exercises/Leg_Press/0.jpg", "images/exercises/Leg_Press/1.jpg"],
  "Leg Press Calf Raise": ["images/exercises/Calf_Press_On_The_Leg_Press_Machine/0.jpg", "images/exercises/Calf_Press_On_The_Leg_Press_Machine/1.jpg"],
  "Leg Raise (lying)": ["images/exercises/Flat_Bench_Lying_Leg_Raise/0.jpg", "images/exercises/Flat_Bench_Lying_Leg_Raise/1.jpg"],
  "Lying Leg Curl": ["images/exercises/Lying_Leg_Curls/0.jpg", "images/exercises/Lying_Leg_Curls/1.jpg"],
  "Machine Chest Press": ["images/exercises/Leverage_Chest_Press/0.jpg", "images/exercises/Leverage_Chest_Press/1.jpg"],
  "Machine Hip Thrust": ["images/exercises/Barbell_Hip_Thrust/0.jpg", "images/exercises/Barbell_Hip_Thrust/1.jpg"],
  "Machine Lateral Raise": ["images/exercises/Seated_Side_Lateral_Raise/0.jpg", "images/exercises/Seated_Side_Lateral_Raise/1.jpg"],
  "Machine Row (Hammer Strength)": ["images/exercises/Leverage_Iso_Row/0.jpg", "images/exercises/Leverage_Iso_Row/1.jpg"],
  "Machine Shoulder Press": ["images/exercises/Machine_Shoulder_Military_Press/0.jpg", "images/exercises/Machine_Shoulder_Military_Press/1.jpg"],
  "Machine Triceps Extension": ["images/exercises/Machine_Triceps_Extension/0.jpg", "images/exercises/Machine_Triceps_Extension/1.jpg"],
  "Meadows Row": ["images/exercises/One-Arm_Dumbbell_Row/0.jpg", "images/exercises/One-Arm_Dumbbell_Row/1.jpg"],
  "Mountain Climber": ["images/exercises/Mountain_Climbers/0.jpg", "images/exercises/Mountain_Climbers/1.jpg"],
  "Neck Curl (plate)": ["images/exercises/Lying_Face_Up_Plate_Neck_Resistance/0.jpg", "images/exercises/Lying_Face_Up_Plate_Neck_Resistance/1.jpg"],
  "Neck Extension": ["images/exercises/Lying_Face_Down_Plate_Neck_Resistance/0.jpg", "images/exercises/Lying_Face_Down_Plate_Neck_Resistance/1.jpg"],
  "Nordic Hamstring Curl": ["images/exercises/Natural_Glute_Ham_Raise/0.jpg", "images/exercises/Natural_Glute_Ham_Raise/1.jpg"],
  "Overhead Barbell Press (standing)": ["images/exercises/Standing_Military_Press/0.jpg", "images/exercises/Standing_Military_Press/1.jpg"],
  "Overhead Cable Extension": ["images/exercises/Cable_Rope_Overhead_Triceps_Extension/0.jpg", "images/exercises/Cable_Rope_Overhead_Triceps_Extension/1.jpg"],
  "Overhead Dumbbell Extension": ["images/exercises/Standing_Dumbbell_Triceps_Extension/0.jpg", "images/exercises/Standing_Dumbbell_Triceps_Extension/1.jpg"],
  "Pallof Press": ["images/exercises/Pallof_Press/0.jpg", "images/exercises/Pallof_Press/1.jpg"],
  "Parallel Bar Dip": ["images/exercises/Parallel_Bar_Dip/0.jpg", "images/exercises/Parallel_Bar_Dip/1.jpg"],
  "Pause Squat": ["images/exercises/Barbell_Full_Squat/0.jpg", "images/exercises/Barbell_Full_Squat/1.jpg"],
  "Pec Deck / Machine Flye": ["images/exercises/Butterfly/0.jpg", "images/exercises/Butterfly/1.jpg"],
  "Pendlay Row": ["images/exercises/Bent_Over_Barbell_Row/0.jpg", "images/exercises/Bent_Over_Barbell_Row/1.jpg"],
  "Pike Push-Up": ["images/exercises/Handstand_Push-Ups/0.jpg", "images/exercises/Handstand_Push-Ups/1.jpg"],
  "Plank": ["images/exercises/Plank/0.jpg", "images/exercises/Plank/1.jpg"],
  "Plate Front Raise": ["images/exercises/Front_Plate_Raise/0.jpg", "images/exercises/Front_Plate_Raise/1.jpg"],
  "Plate Pinch": ["images/exercises/Plate_Pinch/0.jpg", "images/exercises/Plate_Pinch/1.jpg"],
  "Power Clean": ["images/exercises/Power_Clean/0.jpg", "images/exercises/Power_Clean/1.jpg"],
  "Preacher Curl (barbell)": ["images/exercises/Preacher_Curl/0.jpg", "images/exercises/Preacher_Curl/1.jpg"],
  "Preacher Curl (machine)": ["images/exercises/Machine_Preacher_Curls/0.jpg", "images/exercises/Machine_Preacher_Curls/1.jpg"],
  "Pull-Up": ["images/exercises/Pullups/0.jpg", "images/exercises/Pullups/1.jpg"],
  "Push-Up": ["images/exercises/Pushups/0.jpg", "images/exercises/Pushups/1.jpg"],
  "Rack Pull": ["images/exercises/Rack_Pulls/0.jpg", "images/exercises/Rack_Pulls/1.jpg"],
  "Rear Delt Dumbbell Flye": ["images/exercises/Seated_Bent-Over_Rear_Delt_Raise/0.jpg", "images/exercises/Seated_Bent-Over_Rear_Delt_Raise/1.jpg"],
  "Renegade Row": ["images/exercises/Alternating_Renegade_Row/0.jpg", "images/exercises/Alternating_Renegade_Row/1.jpg"],
  "Reverse Barbell Curl": ["images/exercises/Reverse_Barbell_Curl/0.jpg", "images/exercises/Reverse_Barbell_Curl/1.jpg"],
  "Reverse Lunge": ["images/exercises/Dumbbell_Rear_Lunge/0.jpg", "images/exercises/Dumbbell_Rear_Lunge/1.jpg"],
  "Reverse Pec Deck": ["images/exercises/Reverse_Machine_Flyes/0.jpg", "images/exercises/Reverse_Machine_Flyes/1.jpg"],
  "Reverse Wrist Curl": ["images/exercises/Seated_Palms-Down_Barbell_Wrist_Curl/0.jpg", "images/exercises/Seated_Palms-Down_Barbell_Wrist_Curl/1.jpg"],
  "Romanian Deadlift (barbell)": ["images/exercises/Romanian_Deadlift/0.jpg", "images/exercises/Romanian_Deadlift/1.jpg"],
  "Russian Twist": ["images/exercises/Russian_Twist/0.jpg", "images/exercises/Russian_Twist/1.jpg"],
  "Seated Barbell Press": ["images/exercises/Seated_Barbell_Military_Press/0.jpg", "images/exercises/Seated_Barbell_Military_Press/1.jpg"],
  "Seated Cable Row (close)": ["images/exercises/Seated_Cable_Rows/0.jpg", "images/exercises/Seated_Cable_Rows/1.jpg"],
  "Seated Cable Row (wide)": ["images/exercises/Seated_Cable_Rows/0.jpg", "images/exercises/Seated_Cable_Rows/1.jpg"],
  "Seated Calf Raise": ["images/exercises/Seated_Calf_Raise/0.jpg", "images/exercises/Seated_Calf_Raise/1.jpg"],
  "Seated Leg Curl": ["images/exercises/Seated_Leg_Curl/0.jpg", "images/exercises/Seated_Leg_Curl/1.jpg"],
  "Shrug-to-Row Combo": ["images/exercises/Middle_Back_Shrug/0.jpg", "images/exercises/Middle_Back_Shrug/1.jpg"],
  "Side Plank": ["images/exercises/Side_Bridge/0.jpg", "images/exercises/Side_Bridge/1.jpg"],
  "Single-Leg Calf Raise": ["images/exercises/Calf_Raise_On_A_Dumbbell/0.jpg", "images/exercises/Calf_Raise_On_A_Dumbbell/1.jpg"],
  "Single-Leg Hip Thrust": ["images/exercises/Single_Leg_Glute_Bridge/0.jpg", "images/exercises/Single_Leg_Glute_Bridge/1.jpg"],
  "Single-Leg RDL": ["images/exercises/Kettlebell_One-Legged_Deadlift/0.jpg", "images/exercises/Kettlebell_One-Legged_Deadlift/1.jpg"],
  "Sissy Squat": ["images/exercises/Weighted_Sissy_Squat/0.jpg", "images/exercises/Weighted_Sissy_Squat/1.jpg"],
  "Skull Crusher (EZ-bar)": ["images/exercises/EZ-Bar_Skullcrusher/0.jpg", "images/exercises/EZ-Bar_Skullcrusher/1.jpg"],
  "Smith Machine Bench Press": ["images/exercises/Smith_Machine_Bench_Press/0.jpg", "images/exercises/Smith_Machine_Bench_Press/1.jpg"],
  "Smith Machine Calf Raise": ["images/exercises/Smith_Machine_Calf_Raise/0.jpg", "images/exercises/Smith_Machine_Calf_Raise/1.jpg"],
  "Smith Machine Squat": ["images/exercises/Smith_Machine_Squat/0.jpg", "images/exercises/Smith_Machine_Squat/1.jpg"],
  "Snatch": ["images/exercises/Snatch/0.jpg", "images/exercises/Snatch/1.jpg"],
  "Spider Curl": ["images/exercises/Spider_Curl/0.jpg", "images/exercises/Spider_Curl/1.jpg"],
  "Standing Calf Raise (dumbbell)": ["images/exercises/Standing_Dumbbell_Calf_Raise/0.jpg", "images/exercises/Standing_Dumbbell_Calf_Raise/1.jpg"],
  "Standing Calf Raise (machine)": ["images/exercises/Standing_Calf_Raises/0.jpg", "images/exercises/Standing_Calf_Raises/1.jpg"],
  "Step-Up": ["images/exercises/Dumbbell_Step_Ups/0.jpg", "images/exercises/Dumbbell_Step_Ups/1.jpg"],
  "Step-Up (glute focus)": ["images/exercises/Barbell_Step_Ups/0.jpg", "images/exercises/Barbell_Step_Ups/1.jpg"],
  "Stiff-Leg Deadlift": ["images/exercises/Stiff-Legged_Barbell_Deadlift/0.jpg", "images/exercises/Stiff-Legged_Barbell_Deadlift/1.jpg"],
  "Straight-Arm Pulldown": ["images/exercises/Straight-Arm_Pulldown/0.jpg", "images/exercises/Straight-Arm_Pulldown/1.jpg"],
  "Sumo Deadlift": ["images/exercises/Sumo_Deadlift/0.jpg", "images/exercises/Sumo_Deadlift/1.jpg"],
  "Svend Press": ["images/exercises/Svend_Press/0.jpg", "images/exercises/Svend_Press/1.jpg"],
  "T-Bar Row": ["images/exercises/Lying_T-Bar_Row/0.jpg", "images/exercises/Lying_T-Bar_Row/1.jpg"],
  "Tate Press": ["images/exercises/Tate_Press/0.jpg", "images/exercises/Tate_Press/1.jpg"],
  "Thruster": ["images/exercises/Kettlebell_Thruster/0.jpg", "images/exercises/Kettlebell_Thruster/1.jpg"],
  "Toes-to-Bar": ["images/exercises/Hanging_Pike/0.jpg", "images/exercises/Hanging_Pike/1.jpg"],
  "Trap Bar Deadlift": ["images/exercises/Trap_Bar_Deadlift/0.jpg", "images/exercises/Trap_Bar_Deadlift/1.jpg"],
  "Trap Bar Shrug": ["images/exercises/Leverage_Shrug/0.jpg", "images/exercises/Leverage_Shrug/1.jpg"],
  "Triceps Dip (bench)": ["images/exercises/Bench_Dips/0.jpg", "images/exercises/Bench_Dips/1.jpg"],
  "Triceps Pushdown (bar)": ["images/exercises/Triceps_Pushdown/0.jpg", "images/exercises/Triceps_Pushdown/1.jpg"],
  "Triceps Pushdown (rope)": ["images/exercises/Triceps_Pushdown_-_Rope_Attachment/0.jpg", "images/exercises/Triceps_Pushdown_-_Rope_Attachment/1.jpg"],
  "Turkish Get-Up": ["images/exercises/Kettlebell_Turkish_Get-Up_Lunge_style/0.jpg", "images/exercises/Kettlebell_Turkish_Get-Up_Lunge_style/1.jpg"],
  "Upright Row (barbell)": ["images/exercises/Upright_Barbell_Row/0.jpg", "images/exercises/Upright_Barbell_Row/1.jpg"],
  "Upright Row (cable)": ["images/exercises/Upright_Cable_Row/0.jpg", "images/exercises/Upright_Cable_Row/1.jpg"],
  "Walking Lunge": ["images/exercises/Bodyweight_Walking_Lunge/0.jpg", "images/exercises/Bodyweight_Walking_Lunge/1.jpg"],
  "Wall Ball": ["images/exercises/Medicine_Ball_Scoop_Throw/0.jpg", "images/exercises/Medicine_Ball_Scoop_Throw/1.jpg"],
  "Weighted Back Extension": ["images/exercises/Weighted_Ball_Hyperextension/0.jpg", "images/exercises/Weighted_Ball_Hyperextension/1.jpg"],
  "Weighted Chest Dip": ["images/exercises/Dips_-_Chest_Version/0.jpg", "images/exercises/Dips_-_Chest_Version/1.jpg"],
  "Woodchopper (cable)": ["images/exercises/Standing_Cable_Wood_Chop/0.jpg", "images/exercises/Standing_Cable_Wood_Chop/1.jpg"],
  "Wrist Curl (barbell)": ["images/exercises/Seated_Palm-Up_Barbell_Wrist_Curl/0.jpg", "images/exercises/Seated_Palm-Up_Barbell_Wrist_Curl/1.jpg"],
  "Wrist Roller": ["images/exercises/Wrist_Roller/0.jpg", "images/exercises/Wrist_Roller/1.jpg"],
  "Zottman Curl": ["images/exercises/Zottman_Curl/0.jpg", "images/exercises/Zottman_Curl/1.jpg"],
};

exports.IMAGES_BY_NAME = IMAGES_BY_NAME;
});

__def("scripts/pages/builder.js", function(module, exports, __require){
const { getState, update, activeProgram, newProgramDays } = __require("scripts/store.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");
const { uid } = __require("scripts/utils.js");
const { openExercisePicker } = __require("scripts/components/exercisePicker.js");

function render(container) {
  const s = getState();
  const programs = s.programs;
  const p = activeProgram(s);

  container.innerHTML = `
    <div style="margin-bottom:18px;">
      <div class="eyebrow">PROGRAM BUILDER</div>
      <div class="flex" style="flex-wrap:wrap;gap:8px;align-items:center;margin-top:10px;">
        ${programs.map((prog) => `
          <button data-action="select-program" data-id="${prog.id}" class="pill ${prog.id === p.id ? '' : 'outline'}" style="cursor:pointer;border:none;font-size:12px;padding:8px 15px;">${escapeHtml(prog.name)}</button>
        `).join('')}
        <button data-action="new-program" class="btn" style="padding:7px 12px;">+ New program</button>
      </div>
    </div>

    <!-- Active program: rename GUI + delete -->
    <div class="card" style="padding:14px 16px;margin-bottom:16px;display:flex;align-items:center;gap:14px;flex-wrap:wrap;">
      <label class="stat-label" style="margin:0;">PROGRAM NAME</label>
      <input type="text" data-field="prog-name" value="${escapeHtml(p.name)}" placeholder="Name this program…"
        style="flex:1;min-width:220px;font-family:'Oswald';font-size:20px;font-weight:700;color:var(--text-heading);background:var(--surface);border:1px solid var(--chip-border);border-radius:8px;padding:9px 13px;">
      <button class="btn" data-action="delete-program" ${programs.length <= 1 ? 'disabled style="opacity:.4;cursor:default;"' : ''}>Delete program</button>
    </div>

    <!-- Day rows (each day laid out horizontally) -->
    <div class="flex-col gap-8">
      ${p.days.map((day, di) => `
        <div class="card" style="overflow:hidden;display:flex;align-items:stretch;">
          <div style="flex:0 0 152px;padding:10px 12px;border-right:1px solid #1c212b;display:flex;flex-direction:column;justify-content:center;gap:5px;">
            <div style="font-family:'Oswald';font-weight:600;font-size:11px;letter-spacing:1.5px;color:var(--text-ghost);">${day.day}</div>
            <input type="text" data-field="day-focus" data-day="${di}" value="${escapeHtml(day.focus)}" placeholder="Workout name"
              title="Rename this day's workout"
              style="width:100%;font-family:'Oswald';font-weight:700;font-size:14px;color:var(--text-heading);background:var(--surface);border:1px solid var(--surface-border);border-radius:6px;padding:5px 8px;">
          </div>
          <div style="flex:1;min-width:0;padding:10px;display:flex;flex-wrap:wrap;align-items:center;gap:6px;">
            ${day.exercises.map((it, ei) => `
              <div class="chip" style="cursor:pointer;position:relative;display:flex;align-items:center;gap:6px;max-width:230px;" data-action="edit-exercise" data-day="${di}" data-ex="${ei}">
                <span style="min-width:0;">${escapeHtml(it.name)}</span>
                <button class="chip-del" title="Delete exercise" data-action="delete-exercise" data-day="${di}" data-ex="${ei}">✕</button>
              </div>
            `).join('')}
            ${day.off ? `<span class="mono" style="color:var(--text-disabled);font-size:11px;padding:4px 8px;">— rest day —</span>` : ''}
            <button class="mono" style="color:var(--text-ghost);font-size:11px;padding:5px 9px;background:none;border:1px dashed var(--dash-border);border-radius:6px;cursor:pointer;" data-action="add-exercise" data-day="${di}">+ add</button>
            <button class="mono" style="color:var(--text-ghost);font-size:10px;padding:4px 6px;background:none;border:none;cursor:pointer;text-decoration:underline;margin-left:auto;" data-action="toggle-off" data-day="${di}">${day.off ? 'make training day' : 'mark rest day'}</button>
          </div>
        </div>
      `).join('')}
    </div>
  `;

  bindClicks(container, {
    'select-program': (el) => {
      update((draft) => { draft.activeProgramId = el.dataset.id; });
      render(container);
    },
    'new-program': () => {
      const id = uid('prog');
      update((draft) => {
        draft.programs.push({ id, name: `Program ${draft.programs.length + 1}`, days: newProgramDays() });
        draft.activeProgramId = id;
      });
      render(container);
    },
    'delete-program': () => {
      if (s.programs.length <= 1) return;
      if (!confirm(`Delete “${p.name}”? This can’t be undone.`)) return;
      update((draft) => {
        draft.programs = draft.programs.filter((prog) => prog.id !== draft.activeProgramId);
        draft.activeProgramId = draft.programs[0].id;
      });
      render(container);
    },
    'edit-exercise': (el) => {
      const di = parseInt(el.dataset.day, 10);
      const ei = parseInt(el.dataset.ex, 10);
      const current = p.days[di].exercises[ei];
      const val = prompt('Exercise (blank to remove):', current.name);
      if (val === null) return;
      update((draft) => {
        const days = activeProgram(draft).days;
        if (val.trim() === '') days[di].exercises.splice(ei, 1);
        else days[di].exercises[ei].name = val.trim();
      });
      render(container);
    },
    'delete-exercise': (el, e) => {
      e.stopPropagation();
      const di = parseInt(el.dataset.day, 10);
      const ei = parseInt(el.dataset.ex, 10);
      update((draft) => { activeProgram(draft).days[di].exercises.splice(ei, 1); });
      render(container);
    },
    'add-exercise': (el) => {
      const di = parseInt(el.dataset.day, 10);
      openExercisePicker({
        title: `Add exercise · ${p.days[di].day}`,
        onSelect: (ex) => {
          update((draft) => {
            const day = activeProgram(draft).days[di];
            day.exercises.push({
              id: uid('ex'), name: ex.name, target: `${ex.muscle} · ${ex.equipment}`,
              weight: '0 lb', sets: '3', reps: '8', rpe: '',
            });
            day.off = false;
          });
          render(container);
        },
      });
    },
    'toggle-off': (el) => {
      const di = parseInt(el.dataset.day, 10);
      update((draft) => {
        const day = activeProgram(draft).days[di];
        day.off = !day.off;
      });
      render(container);
    },
  });

  // Live edits: write on every keystroke without re-rendering so the field
  // keeps focus (and you can tab across all seven day inputs freely).
  bindInputs(container, (el, e) => {
    const field = el.dataset.field;
    if (field === 'prog-name') {
      update((draft) => { activeProgram(draft).name = el.value; });
      // Re-render on blur/enter so the program tabs pick up the new name.
      if (e.type === 'change') render(container);
    } else if (field === 'day-focus') {
      const di = parseInt(el.dataset.day, 10);
      update((draft) => { activeProgram(draft).days[di].focus = el.value; });
    }
  });
}

exports.render = render;
});

__def("scripts/pages/calendar.js", function(module, exports, __require){
const { getState, update, helpers, activeProgram } = __require("scripts/store.js");
const { navigate } = __require("scripts/router.js");
const { bindClicks } = __require("scripts/components/dom.js");

const { isoDate } = helpers;

const TAG_CYCLE = [null, 'PUSH', 'PULL', 'LEGS', 'CARDIO', 'REST'];
const TAG_MIX = { PUSH: 52, PULL: 84, LEGS: 38, CARDIO: 65, REST: 0 };
const MONTH_NAMES = ['January', 'February', 'March', 'April', 'May', 'June', 'July', 'August', 'September', 'October', 'November', 'December'];

function tagFor(dateIso) {
  const s = getState();
  const wd = (new Date(dateIso + 'T00:00:00').getDay() + 6) % 7;
  const override = s.calendarOverrides[dateIso];
  if (override !== undefined) return override;
  return activeProgram(s).days[wd].tag;
}

function render(container, params = {}) {
  const today = new Date();
  const year = params.year ? parseInt(params.year, 10) : today.getFullYear();
  const month = params.month ? parseInt(params.month, 10) : today.getMonth();
  const todayIso = isoDate(today);

  const firstOfMonth = new Date(year, month, 1);
  const startOffset = (firstOfMonth.getDay() + 6) % 7; // days before month start (Mon-first)
  const daysInMonth = new Date(year, month + 1, 0).getDate();
  const totalCells = Math.ceil((startOffset + daysInMonth) / 7) * 7;

  const cells = [];
  for (let i = 0; i < totalCells; i++) {
    const dayNum = i - startOffset + 1;
    const cellDate = new Date(year, month, dayNum);
    const dateIso = isoDate(cellDate);
    const inMonth = dayNum >= 1 && dayNum <= daysInMonth;
    cells.push({
      dateIso,
      label: cellDate.getDate(),
      inMonth,
      isToday: dateIso === todayIso,
      tag: inMonth ? tagFor(dateIso) : null,
    });
  }

  container.innerHTML = `
    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:20px;">
      <div class="flex gap-14" style="align-items:baseline;">
        <div class="page-title page-title-sm">${MONTH_NAMES[month].toUpperCase()} ${year}</div>
        <div class="flex gap-6">
          <button class="icon-btn" data-action="prev-month">◀</button>
          <button class="icon-btn" data-action="next-month">▶</button>
          <button class="btn" data-action="today">TODAY</button>
        </div>
      </div>
      <div class="flex gap-14" style="align-items:center;">
        ${Object.entries(TAG_MIX).map(([tag, mix]) => `
          <div class="flex gap-6" style="align-items:center;">
            <span style="width:12px;height:12px;border-radius:3px;background:${mix === 0 ? '#141821' : `color-mix(in srgb, var(--accent) ${mix}%, var(--surface))`};"></span>
            <span class="muted" style="font-size:12px;font-family:'Oswald';letter-spacing:1px;">${tag}</span>
          </div>
        `).join('')}
      </div>
    </div>
    <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:8px;margin-bottom:8px;">
      ${['MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT', 'SUN'].map((w) => `<div class="center muted" style="font-family:'Oswald';font-weight:600;font-size:11px;letter-spacing:2px;">${w}</div>`).join('')}
    </div>
    <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:8px;">
      ${cells.map((c) => {
        if (!c.inMonth) {
          return `<div style="height:84px;border-radius:9px;padding:8px 10px;color:var(--text-disabled);"><span style="font-family:'Oswald';font-weight:600;font-size:14px;">${c.label}</span></div>`;
        }
        const mix = TAG_MIX[c.tag] ?? 0;
        const bg = mix === 0 ? '#141821' : `color-mix(in srgb, var(--accent) ${mix}%, var(--surface))`;
        const fg = mix === 0 ? 'var(--text-ghost)' : (mix >= 70 ? '#0c0e13' : 'var(--text-body2)');
        const border = c.isToday ? 'var(--accent)' : 'var(--row-border)';
        const ring = c.isToday ? 'inset 0 0 0 2px var(--accent)' : 'none';
        return `
          <button data-action="cycle-tag" data-date="${c.dateIso}" style="height:84px;border-radius:9px;padding:8px 10px;display:flex;flex-direction:column;justify-content:space-between;background:${bg};border:1px solid ${border};box-shadow:${ring};cursor:pointer;text-align:left;">
            <span style="font-family:'Oswald';font-weight:600;font-size:14px;color:${fg};">${c.label}</span>
            <span style="font-family:'Oswald';font-weight:600;font-size:11px;letter-spacing:1px;color:${fg};">${c.tag || ''}</span>
          </button>
        `;
      }).join('')}
    </div>
  `;

  bindClicks(container, {
    'prev-month': () => { const d = new Date(year, month - 1, 1); navigate(`/calendar/${d.getFullYear()}/${d.getMonth()}`); },
    'next-month': () => { const d = new Date(year, month + 1, 1); navigate(`/calendar/${d.getFullYear()}/${d.getMonth()}`); },
    today: () => navigate('/calendar'),
    'cycle-tag': (el) => {
      const dateIso = el.dataset.date;
      const current = tagFor(dateIso);
      const idx = TAG_CYCLE.indexOf(current);
      const next = TAG_CYCLE[(idx + 1) % TAG_CYCLE.length];
      update((draft) => {
        if (next === null) delete draft.calendarOverrides[dateIso];
        else draft.calendarOverrides[dateIso] = next;
      });
      render(container, params);
    },
  });
}

exports.render = render;
});

__def("scripts/pages/dashboard.js", function(module, exports, __require){
const { getState, helpers } = __require("scripts/store.js");
const { bindClicks, escapeHtml } = __require("scripts/components/dom.js");
const { bestEst1RM, earliestEst1RM, weeklyVolumeBuckets, adherenceInWindow } = __require("scripts/utils.js");

const { isoDate } = helpers;

const WINDOWS = [{ key: '8', label: '8 WEEKS', weeks: 8 }, { key: '12', label: '12 WEEKS', weeks: 12 }, { key: 'all', label: 'ALL', weeks: null }];
const TOP_LIFT_NAMES = ['Deadlift', 'Back Squat', 'Barbell Bench Press', 'Overhead Press'];

let windowKey = '8';

function render(container) {
  const s = getState();
  const win = WINDOWS.find((w) => w.key === windowKey);
  const buckets = weeklyVolumeBuckets(win.weeks);
  const totalVolume = buckets.reduce((a, b) => a + b.volume, 0);
  const maxVol = Math.max(1, ...buckets.map((b) => b.volume));
  const { done: sessionsDone, pct: adherencePct } = adherenceInWindow(win.weeks);

  const benchNow = bestEst1RM('Barbell Bench Press');
  const benchEarliest = earliestEst1RM('Barbell Bench Press');
  const benchDelta = benchEarliest != null ? benchNow - benchEarliest : null;

  const bwLog = [...s.bodyweightLog].sort((a, b) => a.date.localeCompare(b.date));
  const bwNow = bwLog.length ? bwLog[bwLog.length - 1].weight : null;
  const bwFirst = bwLog.length ? bwLog[0].weight : null;
  const bwDelta = bwNow != null && bwFirst != null ? bwNow - bwFirst : null;

  const lifts = TOP_LIFT_NAMES.map((name) => ({ name: shortName(name), val: bestEst1RM(name) }));
  const maxLift = Math.max(1, ...lifts.map((l) => l.val));

  container.innerHTML = `
    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:20px;">
      <div class="page-title page-title-sm">PROGRESS DASHBOARD</div>
      <div class="flex gap-6">
        ${WINDOWS.map((w) => `<button class="pill ${w.key === windowKey ? '' : 'outline'}" data-action="set-window" data-key="${w.key}" style="cursor:pointer;border:none;">${w.label}</button>`).join('')}
      </div>
    </div>

    <div class="stat-strip">
      <div class="stat-box">
        <div class="stat-label">TOTAL VOLUME</div>
        <div style="font-family:'Oswald';font-weight:700;font-size:32px;color:var(--text-heading);">${formatK(totalVolume)}</div>
        <div style="color:var(--positive);font-size:12px;font-weight:600;">${buckets.length} wk${buckets.length === 1 ? '' : 's'} tracked</div>
      </div>
      <div class="stat-box">
        <div class="stat-label">SESSIONS</div>
        <div style="font-family:'Oswald';font-weight:700;font-size:32px;color:var(--text-heading);">${sessionsDone}</div>
        <div style="color:var(--positive);font-size:12px;font-weight:600;">${adherencePct}% adherence</div>
      </div>
      <div class="stat-box">
        <div class="stat-label">EST. 1RM · BENCH</div>
        <div style="font-family:'Oswald';font-weight:700;font-size:32px;color:var(--accent);">${benchNow || '—'}</div>
        <div style="color:${benchDelta == null ? 'var(--text-ghost)' : (benchDelta >= 0 ? 'var(--positive)' : '#e0704a')};font-size:12px;font-weight:600;">${benchDelta == null ? 'no data yet' : (benchDelta >= 0 ? '▲ ' : '▼ ') + Math.abs(benchDelta) + ' lbs'}</div>
      </div>
      <div class="stat-box">
        <div class="stat-label">BODYWEIGHT</div>
        <div style="font-family:'Oswald';font-weight:700;font-size:32px;color:var(--text-heading);">${bwNow ?? '—'}</div>
        <div style="color:${bwDelta == null ? 'var(--text-ghost)' : (bwDelta <= 0 ? 'var(--positive)' : '#e0704a')};font-size:12px;font-weight:600;">${bwDelta == null ? 'no data yet' : (bwDelta <= 0 ? '▼ ' : '▲ ') + Math.abs(bwDelta).toFixed(1) + ' lbs'}</div>
      </div>
    </div>

    <div style="display:grid;grid-template-columns:1.6fr 1fr;gap:14px;">
      <div class="stat-box">
        <div class="stat-label" style="margin-bottom:16px;">WEEKLY TRAINING VOLUME (LBS)</div>
        <div class="bar-chart" style="height:150px;">
          ${buckets.length ? buckets.map((b, i) => `
            <div class="bar-col">
              <div class="bar-track" style="height:100%;"><div class="bar-fill" style="height:${Math.max(4, Math.round((b.volume / maxVol) * 100))}%;background:color-mix(in srgb, var(--accent) ${30 + Math.round((i / Math.max(1, buckets.length - 1)) * 70)}%, var(--surface));"></div></div>
              <span class="bar-label">${b.label}</span>
            </div>
          `).join('') : `<div class="muted" style="width:100%;text-align:center;">Log a session to see volume history.</div>`}
        </div>
      </div>
      <div class="stat-box">
        <div class="stat-label" style="margin-bottom:14px;">TOP LIFTS — EST. 1RM</div>
        ${lifts.map((l) => `
          <div style="margin-bottom:14px;">
            <div class="flex" style="justify-content:space-between;margin-bottom:6px;"><span style="color:var(--text-body2);font-size:13px;font-weight:600;">${escapeHtml(l.name)}</span><span class="mono" style="color:var(--accent);font-size:13px;">${l.val ? l.val + ' lb' : '—'}</span></div>
            <div style="height:7px;background:#1c212b;border-radius:4px;overflow:hidden;"><div style="height:100%;background:var(--accent);box-shadow:var(--glow);width:${Math.round((l.val / maxLift) * 100)}%;"></div></div>
          </div>
        `).join('')}
      </div>
    </div>
  `;

  bindClicks(container, {
    'set-window': (el) => { windowKey = el.dataset.key; render(container); },
  });
}

function shortName(name) {
  return name.replace('Barbell ', '').replace('Back ', '');
}

function formatK(n) {
  if (n >= 1000) return (n / 1000).toFixed(n >= 10000 ? 0 : 1) + 'k';
  return String(Math.round(n));
}

exports.render = render;
});

__def("scripts/pages/library.js", function(module, exports, __require){
const { getState, update } = __require("scripts/store.js");
const { navigate } = __require("scripts/router.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");
const { uid, bestEst1RM, bestSetFor, lastDoneFor, relativeDateLabel } = __require("scripts/utils.js");
const { createImageSlot } = __require("scripts/components/imageSlot.js");
const { MUSCLE_GROUPS } = __require("scripts/components/exercisePicker.js");
const { exerciseFrameHtml, exerciseThumbHtml, FRAME_LABELS } = __require("scripts/components/exerciseMedia.js");

const GROUPS = ['All', ...MUSCLE_GROUPS];

let searchQuery = '';
let activeGroup = 'All';

function render(container, params = {}) {
  const s = getState();
  const lib = s.library;

  const filtered = lib.filter((ex) => {
    const matchesGroup = activeGroup === 'All' || ex.muscle === activeGroup;
    const matchesSearch = !searchQuery || ex.name.toLowerCase().includes(searchQuery.toLowerCase());
    return matchesGroup && matchesSearch;
  });

  const selectedId = params.id || filtered[0]?.id || lib[0]?.id;
  const selected = lib.find((ex) => ex.id === selectedId);

  container.innerHTML = `
    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:18px;">
      <div class="page-title page-title-sm">EXERCISE LIBRARY</div>
      <button class="btn primary" data-action="add-exercise">+ New exercise</button>
    </div>
    <div style="display:grid;grid-template-columns:1fr 1.15fr;gap:16px;">
      <div>
        <div class="flex gap-10" style="align-items:center;background:var(--surface);border:1px solid var(--chip-border);border-radius:9px;padding:10px 14px;margin-bottom:12px;">
          <span class="muted" style="font-size:15px;">⌕</span>
          <input type="text" data-field="search" placeholder="Search exercises…" value="${escapeHtml(searchQuery)}" class="mono" style="border:none;background:none;flex:1;color:var(--text-body);font-size:14px;padding:0;">
        </div>
        <div class="flex" style="flex-wrap:wrap;gap:6px;margin-bottom:14px;">
          ${GROUPS.map((g) => `<button class="pill ${g === activeGroup ? '' : 'outline'}" style="cursor:pointer;border:none;" data-action="set-group" data-group="${g}">${g}</button>`).join('')}
        </div>
        <div class="grid-table">
          ${filtered.length ? filtered.map((ex) => `
            <div style="display:flex;align-items:center;justify-content:space-between;padding:9px 15px 9px 11px;border-bottom:1px solid var(--row-border);border-left:2px solid ${ex.id === selectedId ? 'var(--accent)' : 'transparent'};background:${ex.id === selectedId ? `color-mix(in srgb, var(--accent) 14%, var(--surface))` : 'transparent'};cursor:pointer;" data-action="select" data-id="${ex.id}">
              <div class="flex gap-10" style="align-items:center;min-width:0;">
                <div class="art-thumb">${exerciseThumbHtml(ex)}</div>
                <div style="min-width:0;">
                  <div style="color:var(--text-body);font-weight:600;font-size:14px;">${escapeHtml(ex.name)}</div>
                  <div class="muted mono" style="font-size:11px;margin-top:1px;">${escapeHtml(ex.group)}</div>
                </div>
              </div>
              <span class="muted" style="font-size:16px;">›</span>
            </div>
          `).join('') : `<div style="padding:20px;text-align:center;color:var(--text-ghost);">No exercises match.</div>`}
        </div>
      </div>
      <div class="stat-box" id="detail-panel"></div>
    </div>
  `;

  const detail = container.querySelector('#detail-panel');
  renderDetail(detail, selected);

  bindClicks(container, {
    'set-group': (el) => { activeGroup = el.dataset.group; render(container, params); },
    select: (el) => navigate(`/library/${el.dataset.id}`),
    'add-exercise': () => {
      const name = prompt('Exercise name?');
      if (!name) return;
      const id = uid('lib');
      update((draft) => {
        draft.library.push({
          id, name, muscle: 'Full-Body & Compound', equipment: 'Custom', level: 'Beginner',
          group: 'Full-Body & Compound · Custom', cues: [], image: null,
        });
      });
      navigate(`/library/${id}`);
    },
  });

  bindInputs(container, (el) => {
    if (el.dataset.field === 'search') { searchQuery = el.value; render(container, params); }
  }, ['input']);
}

function renderDetail(mount, ex) {
  if (!ex) {
    mount.innerHTML = `<div class="muted" style="padding:20px;text-align:center;">Select an exercise.</div>`;
    return;
  }
  const est1RM = bestEst1RM(ex.name);
  const best = bestSetFor(ex.name);
  const lastDone = lastDoneFor(ex.name);
  const stats = {
    EQUIPMENT: ex.equipment,
    LEVEL: ex.level,
    'EST. 1RM': est1RM ? `${est1RM} lb` : '—',
    'LAST DONE': lastDone ? relativeDateLabel(lastDone) : '—',
  };

  mount.innerHTML = `
    <div style="font-family:'Oswald';font-weight:700;font-size:22px;color:var(--text-heading);line-height:1;">${escapeHtml(ex.name)}</div>
    <div class="mono" style="color:var(--accent);font-size:12px;margin-top:4px;">${escapeHtml(ex.muscle).toUpperCase()}</div>
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0;">
      <div>
        <div class="stat-label" style="font-size:10px;margin-bottom:6px;">${FRAME_LABELS[0]}</div>
        <div id="lib-image-start" style="width:100%;height:170px;border-radius:9px;overflow:hidden;border:1px solid var(--surface-border);position:relative;"></div>
      </div>
      <div>
        <div class="stat-label" style="font-size:10px;margin-bottom:6px;">${FRAME_LABELS[1]}</div>
        <div id="lib-image-end" style="width:100%;height:170px;border-radius:9px;overflow:hidden;border:1px solid var(--surface-border);position:relative;"></div>
      </div>
    </div>
    <div style="display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:16px;">
      ${Object.entries(stats).map(([k, v]) => `
        <div class="card" style="padding:10px 12px;"><div class="stat-label" style="font-size:10px;">${k}</div><div style="font-family:'Oswald';font-weight:700;font-size:18px;color:var(--text-heading);line-height:1.2;">${escapeHtml(String(v))}</div></div>
      `).join('')}
    </div>
    ${best ? `<div class="muted mono" style="font-size:11px;margin:-10px 0 16px;">Best set: ${best.weight} × ${best.reps}</div>` : ''}
    <div class="stat-label" style="margin-bottom:8px;">COACHING CUES</div>
    <div class="flex-col gap-8" id="cues-list">
      ${ex.cues.map((cue, i) => `
        <div class="flex gap-8" style="align-items:flex-start;">
          <span style="width:6px;height:6px;border-radius:2px;background:var(--accent);margin-top:6px;flex-shrink:0;"></span>
          <span style="color:var(--text-muted);font-size:13px;line-height:1.45;flex:1;">${escapeHtml(cue)}</span>
          <button data-action="remove-cue" data-idx="${i}" style="background:none;border:none;color:var(--text-ghost);cursor:pointer;font-size:11px;">✕</button>
        </div>
      `).join('')}
      <button data-action="add-cue" style="background:none;border:none;color:var(--text-ghost);cursor:pointer;font-size:12px;text-align:left;padding:2px 0;">+ add coaching cue</button>
    </div>
  `;

  // Two demo boxes: START and END, each pre-filled with the corresponding
  // demonstration photo and independently replaceable by the user's own photo.
  mount.querySelector('#lib-image-start').appendChild(createImageSlot({
    image: ex.imageStart ?? ex.image ?? null,
    placeholder: 'Replace start photo',
    placeholderSvg: exerciseFrameHtml(ex, 0),
    onChange: (dataUrl) => update((draft) => { draft.library.find((l) => l.id === ex.id).imageStart = dataUrl; }),
  }));
  mount.querySelector('#lib-image-end').appendChild(createImageSlot({
    image: ex.imageEnd ?? null,
    placeholder: 'Replace end photo',
    placeholderSvg: exerciseFrameHtml(ex, 1),
    onChange: (dataUrl) => update((draft) => { draft.library.find((l) => l.id === ex.id).imageEnd = dataUrl; }),
  }));

  bindClicks(mount, {
    'add-cue': () => {
      const cue = prompt('New coaching cue:');
      if (!cue) return;
      update((draft) => { draft.library.find((l) => l.id === ex.id).cues.push(cue); });
      renderDetail(mount, { ...ex, cues: [...ex.cues, cue] });
    },
    'remove-cue': (el) => {
      const i = parseInt(el.dataset.idx, 10);
      update((draft) => { draft.library.find((l) => l.id === ex.id).cues.splice(i, 1); });
      const next = { ...ex, cues: ex.cues.filter((_, n) => n !== i) };
      renderDetail(mount, next);
    },
  });
}

exports.render = render;
});

__def("scripts/pages/logEntry.js", function(module, exports, __require){
const { getState, update, helpers } = __require("scripts/store.js");
const { navigate } = __require("scripts/router.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");
const { ensureSession, exerciseDone, previousExerciseInstance, epley1RM } = __require("scripts/utils.js");

const { isoDate } = helpers;

let timer = { seconds: 150, running: false, intervalId: null };

function resetTimerIfNeeded(key) {
  if (timer.key !== key) {
    clearInterval(timer.intervalId);
    timer = { seconds: 150, running: false, intervalId: null, key };
  }
}

function fmtClock(totalSeconds) {
  const s = Math.max(0, totalSeconds);
  const m = Math.floor(s / 60);
  const sec = s % 60;
  return `${m}:${String(sec).padStart(2, '0')}`;
}

function render(container, params = {}) {
  const s = getState();
  const date = params.date || isoDate(new Date());
  const session = ensureSession(date);
  let idx = params.idx != null ? parseInt(params.idx, 10) : session.exercises.findIndex((ex) => !exerciseDone(ex));
  if (idx < 0) idx = 0;
  if (idx >= session.exercises.length) idx = session.exercises.length - 1;

  resetTimerIfNeeded(`${date}:${idx}`);

  if (!session.exercises.length) {
    container.innerHTML = `<div class="card" style="padding:40px;text-align:center;color:var(--text-ghost);">No exercises logged for ${escapeHtml(date)}. Head back to the <a href="#/">Weekly Planner</a> to add one.</div>`;
    return;
  }

  const ex = session.exercises[idx];
  const prev = previousExerciseInstance(date, ex.name);
  const doneSets = ex.setLogs.filter((sl) => sl.done);
  const setVolume = doneSets.reduce((a, sl) => a + sl.weight * sl.reps, 0);
  const bestDoneSet = doneSets.reduce((best, sl) => (sl.weight > (best?.weight ?? -1) ? sl : best), null);
  const est1RM = bestDoneSet ? Math.round(epley1RM(bestDoneSet.weight, bestDoneSet.reps)) : 0;

  container.innerHTML = `
    <div class="page-head">
      <div>
        <div class="eyebrow">LOG ENTRY · ${escapeHtml(session.focus)} · EX ${idx + 1} / ${session.exercises.length}</div>
        <div class="page-title">${escapeHtml(ex.name).toUpperCase()}</div>
        <div class="muted mono" style="font-size:13px;margin-top:6px;">${escapeHtml(ex.target)} — target ${escapeHtml(ex.sets)} × ${escapeHtml(ex.reps)} @ RPE ${escapeHtml(ex.rpe)}</div>
      </div>
      <div class="flex gap-8">
        <button class="btn" data-action="prev" ${idx === 0 ? 'disabled style="opacity:.4;cursor:default;"' : ''}>◀ PREV</button>
        <button class="btn accent-outline" data-action="next" ${idx === session.exercises.length - 1 ? 'disabled style="opacity:.4;cursor:default;"' : ''}>NEXT ▶</button>
      </div>
    </div>

    <div style="display:grid;grid-template-columns:1.9fr 1fr;gap:14px;">
      <div class="grid-table">
        <div class="grid-head" style="grid-template-columns:.6fr 1fr 1fr .9fr .8fr .5fr .4fr;">
          <div>SET</div><div>PREV</div><div class="center">WEIGHT</div><div class="center">REPS</div><div class="center">RPE</div><div class="center">✓</div><div></div>
        </div>
        ${ex.setLogs.map((sl, i) => `
          <div class="grid-row" style="grid-template-columns:.6fr 1fr 1fr .9fr .8fr .5fr .4fr;">
            <div><span style="width:22px;height:22px;border-radius:6px;background:color-mix(in srgb, var(--accent) 16%, var(--surface));color:var(--accent);font-family:'Oswald';font-weight:700;font-size:13px;display:flex;align-items:center;justify-content:center;">${i + 1}</span></div>
            <div class="mono muted" style="font-size:13px;">${prev && prev.setLogs[i] ? `${prev.setLogs[i].weight} × ${prev.setLogs[i].reps}` : '—'}</div>
            <div class="center"><input class="field" type="number" data-field="weight" data-idx="${i}" value="${sl.weight}"></div>
            <div class="center"><input class="field" type="number" data-field="reps" data-idx="${i}" value="${sl.reps}"></div>
            <div class="center"><input class="field mono" style="color:var(--accent);min-width:44px;" type="text" data-field="rpe" data-idx="${i}" value="${escapeHtml(sl.rpe)}"></div>
            <div class="flex" style="justify-content:center;"><button class="${sl.done ? 'check-done' : 'check-pending'}" data-action="toggle-set" data-idx="${i}">${sl.done ? '✓' : ''}</button></div>
            <div class="flex" style="justify-content:center;">${ex.setLogs.length > 1 ? `<button class="icon-btn" style="width:22px;height:22px;font-size:11px;" data-action="remove-set" data-idx="${i}">✕</button>` : ''}</div>
          </div>
        `).join('')}
        <button class="add-row" data-action="add-set"><span class="plus">+</span>Add set</button>
      </div>

      <div class="flex-col gap-14">
        <div class="stat-box" style="text-align:center;">
          <div class="stat-label">REST TIMER</div>
          <div id="timer-display" style="font-family:'Oswald';font-weight:700;font-size:54px;color:var(--accent);text-shadow:var(--glow);line-height:1.1;letter-spacing:1px;">${fmtClock(timer.seconds)}</div>
          <div class="flex gap-8" style="justify-content:center;margin-top:8px;">
            <button class="btn" data-action="timer-sub">−15s</button>
            <button class="btn primary" data-action="timer-toggle" id="timer-toggle-btn">${timer.running ? 'PAUSE' : 'START'}</button>
            <button class="btn" data-action="timer-add">+15s</button>
          </div>
        </div>
        <div class="stat-box grow">
          <div class="stat-label" style="margin-bottom:10px;">SESSION NOTES</div>
          <textarea data-field="notes" rows="4" style="width:100%;resize:vertical;line-height:1.6;">${escapeHtml(ex.notes)}</textarea>
        </div>
        <div style="background:color-mix(in srgb, var(--accent) 12%, var(--surface));border:1px solid color-mix(in srgb, var(--accent) 30%, var(--panel-border));border-radius:10px;padding:14px 16px;display:flex;align-items:center;justify-content:space-between;">
          <div><div class="stat-label">SET VOLUME</div><div style="font-family:'Oswald';font-weight:700;font-size:24px;color:var(--text-heading);line-height:1;">${Math.round(setVolume).toLocaleString()} lb</div></div>
          <div style="text-align:right;"><div class="stat-label">EST. 1RM</div><div style="font-family:'Oswald';font-weight:700;font-size:24px;color:var(--accent);line-height:1;">${est1RM || '—'}</div></div>
        </div>
      </div>
    </div>
  `;

  function mutateExercise(fn) {
    update((draft) => { fn(draft.sessions[date].exercises[idx]); });
  }

  bindClicks(container, {
    prev: () => { if (idx > 0) navigate(`/log/${date}/${idx - 1}`); },
    next: () => { if (idx < session.exercises.length - 1) navigate(`/log/${date}/${idx + 1}`); },
    'toggle-set': (el) => {
      const i = parseInt(el.dataset.idx, 10);
      mutateExercise((e) => { e.setLogs[i].done = !e.setLogs[i].done; });
      render(container, params);
    },
    'remove-set': (el) => {
      const i = parseInt(el.dataset.idx, 10);
      mutateExercise((e) => { e.setLogs.splice(i, 1); e.setLogs.forEach((sl, n) => { sl.set = String(n + 1); }); });
      render(container, params);
    },
    'add-set': () => {
      mutateExercise((e) => {
        const last = e.setLogs[e.setLogs.length - 1];
        e.setLogs.push({ set: String(e.setLogs.length + 1), weight: last?.weight ?? 0, reps: last?.reps ?? 8, rpe: '', done: false });
      });
      render(container, params);
    },
    'timer-toggle': () => {
      if (timer.running) {
        clearInterval(timer.intervalId);
        timer.running = false;
      } else {
        timer.running = true;
        timer.intervalId = setInterval(() => {
          timer.seconds = Math.max(0, timer.seconds - 1);
          const disp = document.getElementById('timer-display');
          if (disp) disp.textContent = fmtClock(timer.seconds);
          if (timer.seconds === 0) {
            clearInterval(timer.intervalId);
            timer.running = false;
            const btn = document.getElementById('timer-toggle-btn');
            if (btn) btn.textContent = 'START';
          }
        }, 1000);
      }
      const btn = document.getElementById('timer-toggle-btn');
      if (btn) btn.textContent = timer.running ? 'PAUSE' : 'START';
    },
    'timer-add': () => {
      timer.seconds += 15;
      const disp = document.getElementById('timer-display');
      if (disp) disp.textContent = fmtClock(timer.seconds);
    },
    'timer-sub': () => {
      timer.seconds = Math.max(0, timer.seconds - 15);
      const disp = document.getElementById('timer-display');
      if (disp) disp.textContent = fmtClock(timer.seconds);
    },
  });

  bindInputs(container, (el) => {
    const i = parseInt(el.dataset.idx, 10);
    if (el.dataset.field === 'weight') {
      const v = parseFloat(el.value); if (Number.isFinite(v)) mutateExercise((e) => { e.setLogs[i].weight = v; });
    } else if (el.dataset.field === 'reps') {
      const v = parseInt(el.value, 10); if (Number.isFinite(v)) mutateExercise((e) => { e.setLogs[i].reps = v; });
    } else if (el.dataset.field === 'rpe') {
      mutateExercise((e) => { e.setLogs[i].rpe = el.value; });
    } else if (el.dataset.field === 'notes') {
      mutateExercise((e) => { e.notes = el.value; });
    }
  }, ['change']);
}

exports.render = render;
});

__def("scripts/pages/measurements.js", function(module, exports, __require){
const { getState, update, helpers } = __require("scripts/store.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");

const { isoDate } = helpers;

const METRICS = [
  { key: 'weight', label: 'WEIGHT', unit: 'lb', goodDown: true },
  { key: 'bodyFat', label: 'BODY FAT', unit: '%', goodDown: true },
  { key: 'waist', label: 'WAIST', unit: 'in', goodDown: true },
  { key: 'chest', label: 'CHEST', unit: 'in', goodDown: false },
  { key: 'arms', label: 'ARMS', unit: 'in', goodDown: false },
  { key: 'thighs', label: 'THIGHS', unit: 'in', goodDown: false },
];

let showForm = false;

function fmtShort(dateIso) {
  return new Date(dateIso + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
}

function render(container) {
  const s = getState();
  const rows = [...s.measurements].sort((a, b) => a.date.localeCompare(b.date));
  const latest = rows[rows.length - 1];
  const prev = rows[rows.length - 2];

  const trend = rows.slice(-8);
  const maxW = Math.max(1, ...trend.map((r) => r.weight));
  const minW = Math.min(...trend.map((r) => r.weight));

  const historyCols = rows.slice(-4);

  container.innerHTML = `
    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:18px;">
      <div class="page-title page-title-sm">MEASUREMENTS</div>
      <button class="btn primary" data-action="toggle-form">${showForm ? 'Cancel' : '+ Log measurement'}</button>
    </div>

    ${showForm ? `
      <div class="card" style="padding:16px;margin-bottom:16px;">
        <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:10px;align-items:end;">
          <div><label class="stat-label" style="display:block;margin-bottom:6px;">DATE</label><input type="date" data-field="m-date" value="${isoDate(new Date())}"></div>
          ${METRICS.map((m) => `<div><label class="stat-label" style="display:block;margin-bottom:6px;">${m.label}</label><input type="number" step="0.1" data-field="m-${m.key}" value="${latest ? latest[m.key] : ''}" style="width:100%;"></div>`).join('')}
        </div>
        <button class="btn primary" data-action="save-measurement" style="margin-top:12px;">Save entry</button>
      </div>
    ` : ''}

    <div style="display:grid;grid-template-columns:repeat(6,1fr);gap:10px;margin-bottom:16px;">
      ${METRICS.map((m) => {
        const val = latest ? latest[m.key] : null;
        const delta = latest && prev ? latest[m.key] - prev[m.key] : null;
        const isGood = delta == null ? null : (m.goodDown ? delta <= 0 : delta >= 0);
        return `
          <div class="stat-box">
            <div class="stat-label">${m.label}</div>
            <div style="font-family:'Oswald';font-weight:700;font-size:26px;color:var(--text-heading);line-height:1.1;">${val ?? '—'} <span style="font-size:12px;color:var(--text-ghost);font-weight:400;">${m.unit}</span></div>
            <div style="font-size:12px;font-weight:600;margin-top:2px;color:${delta == null ? 'var(--text-ghost)' : (isGood ? 'var(--positive)' : '#e0704a')};">${delta == null ? 'no history' : (delta <= 0 ? '▼ ' : '▲ ') + Math.abs(delta).toFixed(1)}</div>
          </div>
        `;
      }).join('')}
    </div>

    <div style="display:grid;grid-template-columns:1.3fr 1fr;gap:14px;">
      <div class="stat-box">
        <div class="stat-label" style="margin-bottom:16px;">BODYWEIGHT TREND (LB)</div>
        <div class="bar-chart" style="height:150px;">
          ${trend.length ? trend.map((r) => {
            const range = Math.max(1, maxW - minW);
            const pct = 20 + Math.round(((r.weight - minW) / range) * 80);
            return `
              <div class="bar-col">
                <div class="bar-track" style="height:100%;"><div class="bar-fill" style="height:${pct}%;"></div></div>
                <span class="bar-label">${fmtShort(r.date)}</span>
              </div>
            `;
          }).join('') : `<div class="muted" style="width:100%;text-align:center;">Log an entry to see the trend.</div>`}
        </div>
      </div>
      <div class="grid-table" style="align-self:start;">
        <div class="grid-head" style="grid-template-columns:1.4fr repeat(${Math.max(historyCols.length, 1)}, 1fr);">
          <div>METRIC</div>
          ${historyCols.map((c) => `<div class="center">${fmtShort(c.date)}</div>`).join('')}
        </div>
        ${METRICS.map((m) => `
          <div class="grid-row" style="grid-template-columns:1.4fr repeat(${Math.max(historyCols.length, 1)}, 1fr);">
            <div style="color:var(--text-body2);font-size:13px;font-weight:500;">${m.label} (${m.unit})</div>
            ${historyCols.map((c) => `<div class="center mono" style="color:var(--text-dim);font-size:13px;">${c[m.key] ?? '—'}</div>`).join('')}
          </div>
        `).join('')}
      </div>
    </div>
  `;

  bindClicks(container, {
    'toggle-form': () => { showForm = !showForm; render(container); },
    'save-measurement': () => {
      const date = container.querySelector('[data-field="m-date"]').value || isoDate(new Date());
      const entry = { date };
      METRICS.forEach((m) => {
        const v = parseFloat(container.querySelector(`[data-field="m-${m.key}"]`).value);
        entry[m.key] = Number.isFinite(v) ? v : (latest ? latest[m.key] : 0);
      });
      update((draft) => {
        const existing = draft.measurements.find((r) => r.date === date);
        if (existing) Object.assign(existing, entry); else draft.measurements.push(entry);
        const bw = draft.bodyweightLog.find((r) => r.date === date);
        if (bw) bw.weight = entry.weight; else draft.bodyweightLog.push({ date, weight: entry.weight });
      });
      showForm = false;
      render(container);
    },
  });
}

exports.render = render;
});

__def("scripts/pages/photos.js", function(module, exports, __require){
const { getState, update, helpers } = __require("scripts/store.js");
const { bindClicks, escapeHtml } = __require("scripts/components/dom.js");
const { uid, bodyweightNear } = __require("scripts/utils.js");
const { createImageSlot } = __require("scripts/components/imageSlot.js");

const { isoDate } = helpers;

let viewMode = 'compare';

function fmtDate(dateIso) {
  return new Date(dateIso + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
}

function render(container) {
  const s = getState();
  const timeline = [...s.photos].sort((a, b) => a.date.localeCompare(b.date));
  const withImages = timeline.filter((p) => p.image);
  const before = withImages[0] || null;
  const after = withImages.length > 1 ? withImages[withImages.length - 1] : null;

  container.innerHTML = `
    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:18px;">
      <div class="page-title page-title-sm">PROGRESS PHOTOS</div>
      <div class="flex gap-6">
        <button class="pill ${viewMode === 'compare' ? '' : 'outline'}" style="cursor:pointer;border:none;" data-action="set-view" data-mode="compare">COMPARE</button>
        <button class="pill ${viewMode === 'grid' ? '' : 'outline'}" style="cursor:pointer;border:none;" data-action="set-view" data-mode="grid">GRID</button>
      </div>
    </div>

    ${viewMode === 'compare' ? `
      <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-bottom:16px;">
        <div>
          <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:8px;">
            <span class="stat-label">BEFORE</span>
            <span class="muted mono" style="font-size:12px;">${before ? `${fmtDate(before.date)} · ${bodyweightNear(before.date) ?? '—'} lb` : 'No photo yet'}</span>
          </div>
          <div style="width:100%;height:340px;border-radius:11px;overflow:hidden;border:1px solid var(--surface-border);" id="before-mount"></div>
        </div>
        <div>
          <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:8px;">
            <span class="mono" style="color:var(--accent);font-family:'Oswald';font-weight:600;font-size:12px;letter-spacing:1.5px;">AFTER</span>
            <span class="muted mono" style="font-size:12px;">${after ? `${fmtDate(after.date)} · ${bodyweightNear(after.date) ?? '—'} lb` : 'Add a second photo'}</span>
          </div>
          <div style="width:100%;height:340px;border-radius:11px;overflow:hidden;border:1px solid color-mix(in srgb, var(--accent) 40%, var(--panel-border));" id="after-mount"></div>
        </div>
      </div>
    ` : ''}

    <div class="flex" style="justify-content:space-between;align-items:center;margin-bottom:10px;">
      <div class="stat-label">TIMELINE</div>
      <button class="btn primary" data-action="add-photo">+ Add photo</button>
    </div>
    <div style="display:grid;grid-template-columns:repeat(auto-fill, minmax(160px, 1fr));gap:10px;">
      ${timeline.map((p) => `
        <div>
          <div style="width:100%;height:150px;border-radius:9px;overflow:hidden;border:1px solid var(--surface-border);" data-photo-mount="${p.id}"></div>
          <div class="flex" style="justify-content:space-between;align-items:center;margin-top:6px;">
            <span class="muted" style="font-size:11px;font-family:'Oswald';letter-spacing:1px;">${fmtDate(p.date)}</span>
            <button data-action="remove-photo" data-id="${p.id}" style="background:none;border:none;color:var(--text-ghost);cursor:pointer;font-size:11px;">✕</button>
          </div>
        </div>
      `).join('')}
    </div>
  `;

  if (viewMode === 'compare') {
    container.querySelector('#before-mount').appendChild(createImageSlot({
      image: before?.image ?? null,
      placeholder: 'No before photo — add one below',
      removable: false,
      onChange: () => {},
    }));
    container.querySelector('#after-mount').appendChild(createImageSlot({
      image: after?.image ?? null,
      placeholder: 'No after photo — add one below',
      removable: false,
      onChange: () => {},
    }));
  }

  timeline.forEach((p) => {
    const mount = container.querySelector(`[data-photo-mount="${p.id}"]`);
    mount.appendChild(createImageSlot({
      image: p.image,
      placeholder: 'Add',
      onChange: (dataUrl) => {
        update((draft) => { draft.photos.find((x) => x.id === p.id).image = dataUrl; });
        render(container);
      },
    }));
  });

  bindClicks(container, {
    'set-view': (el) => { viewMode = el.dataset.mode; render(container); },
    'add-photo': () => {
      const id = uid('ph');
      update((draft) => { draft.photos.push({ id, date: isoDate(new Date()), image: null }); });
      render(container);
    },
    'remove-photo': (el) => {
      update((draft) => { draft.photos = draft.photos.filter((p) => p.id !== el.dataset.id); });
      render(container);
    },
  });
}

exports.render = render;
});

__def("scripts/pages/planner.js", function(module, exports, __require){
const { getState, update, helpers, activeProgram } = __require("scripts/store.js");
const { navigate } = __require("scripts/router.js");
const { bindClicks, bindInputs, escapeHtml } = __require("scripts/components/dom.js");
const {
  ensureSession, peekSession, exerciseDone, sessionVolume, sessionCompletionPct,
  isSessionFullyDone, currentWeekDates, uid,
} = __require("scripts/utils.js");

const { isoDate, WEEKDAYS } = helpers;

function fmtWeekRange(dates) {
  const start = new Date(dates[0] + 'T00:00:00');
  const end = new Date(dates[6] + 'T00:00:00');
  const opts = { month: 'short', day: 'numeric' };
  return `${start.toLocaleDateString('en-US', opts)} → ${end.toLocaleDateString('en-US', opts)}`;
}

function render(container) {
  const s = getState();
  const dates = currentWeekDates();
  const todayIso = isoDate(new Date());
  const isToday = dates.includes(todayIso);
  const focusDate = isToday ? todayIso : dates[0];

  const trainingDays = activeProgram(s).days.filter((d) => !d.off).length;
  const weekSessions = dates.map((d) => peekSession(d));
  const sessionsDone = weekSessions.filter((sess) => sess.exercises.length && isSessionFullyDone(sess)).length;
  const weeklyVolume = weekSessions.reduce((a, sess) => a + sessionVolume(sess), 0);
  const completionPct = trainingDays ? Math.round((sessionsDone / trainingDays) * 100) : 0;

  const session = ensureSession(focusDate);
  const estMinutes = session.exercises.length ? session.exercises.length * 9 : 0;
  const rpeCap = session.exercises.reduce((max, ex) => {
    const n = parseFloat(ex.rpe);
    return Number.isFinite(n) ? Math.max(max, n) : max;
  }, 0);

  const bwLog = [...s.bodyweightLog].sort((a, b) => a.date.localeCompare(b.date));
  const current = bwLog.length ? bwLog[bwLog.length - 1].weight : null;
  const weekAgoIso = isoDate(new Date(Date.now() - 7 * 86400000));
  const weekAgoEntry = [...bwLog].reverse().find((e) => e.date <= weekAgoIso);
  const delta = current != null && weekAgoEntry ? (current - weekAgoEntry.weight) : null;

  const dayBars = dates.map((d, i) => {
    const sess = peekSession(d);
    const pct = sess.exercises.length ? Math.round(sessionCompletionPct(sess) * 100) : 0;
    return { label: WEEKDAYS[i], pct };
  });

  container.innerHTML = `
    <div class="page-head">
      <div>
        <div class="eyebrow">${escapeHtml(activeProgram(s).name)}</div>
        <div class="page-title page-title-lg">THIS WEEK</div>
      </div>
      <div style="text-align:right;">
        <div class="stat-label">CLIENT · ${escapeHtml(s.client.name).toUpperCase()}</div>
        <div style="color:var(--text-heading);font-family:'Oswald';font-weight:500;font-size:16px;margin-top:2px;">${fmtWeekRange(dates)}</div>
      </div>
    </div>

    <div class="stat-strip">
      <div class="stat-box"><div class="stat-label">TRAINING DAYS</div><div class="stat-value">${trainingDays} <small>/ 7</small></div></div>
      <div class="stat-box"><div class="stat-label">SESSIONS DONE</div><div class="stat-value accent">${sessionsDone} <small>/ ${trainingDays}</small></div></div>
      <div class="stat-box"><div class="stat-label">WEEKLY VOLUME</div><div class="stat-value">${Math.round(weeklyVolume).toLocaleString()} <small>lbs</small></div></div>
      <div class="stat-box">
        <div class="stat-label">COMPLETION</div>
        <div class="flex" style="align-items:baseline;gap:8px;">
          <div class="stat-value">${completionPct}%</div>
          <div class="progress-track"><div class="progress-fill" style="width:${completionPct}%;"></div></div>
        </div>
      </div>
    </div>

    <div class="flex" style="align-items:center;justify-content:space-between;margin-bottom:10px;">
      <div class="flex gap-12" style="align-items:center;">
        <span class="section-label">${isToday ? 'TODAY' : escapeHtml(dates[0])} — ${escapeHtml(session.focus)}</span>
        ${session.off ? '' : `<span class="pill">${escapeHtml(session.tag)}</span>`}
      </div>
      ${session.off ? '' : `<span class="muted mono" style="font-size:12px;">Est. ${estMinutes} min · RPE cap ${rpeCap || '—'}</span>`}
    </div>

    ${session.off ? `
      <div class="grid-table" style="padding:40px 0;text-align:center;color:var(--text-ghost);font-family:'Oswald';letter-spacing:1px;">— REST DAY —</div>
    ` : `
    <div class="grid-table">
      <div class="grid-head" style="grid-template-columns:2.4fr .9fr .8fr .9fr .8fr .7fr;">
        <div>EXERCISE</div><div class="center">WEIGHT</div><div class="center">SETS</div><div class="center">REPS</div><div class="center">RPE</div><div class="center">✓</div>
      </div>
      ${session.exercises.map((ex, i) => `
        <div class="grid-row" style="grid-template-columns:2.4fr .9fr .8fr .9fr .8fr .7fr;cursor:pointer;" data-action="open-log" data-idx="${i}">
          <div class="flex-col"><span style="color:var(--text-body);font-weight:600;font-size:14px;">${escapeHtml(ex.name)}</span><span class="muted mono" style="font-size:11px;">${escapeHtml(ex.target)}</span></div>
          <div class="center mono" style="color:var(--text-body2);font-size:14px;font-weight:500;">${escapeHtml(ex.weight)}</div>
          <div class="center mono" style="color:var(--text-body2);font-size:14px;">${escapeHtml(ex.sets)}</div>
          <div class="center mono" style="color:var(--text-body2);font-size:14px;">${escapeHtml(ex.reps)}</div>
          <div class="center mono" style="color:var(--accent);font-size:14px;">${escapeHtml(ex.rpe)}</div>
          <div class="flex" style="justify-content:center;">
            <button class="${exerciseDone(ex) ? 'check-done' : 'check-pending'}" data-action="toggle-done" data-idx="${i}">${exerciseDone(ex) ? '✓' : ''}</button>
          </div>
        </div>
      `).join('')}
      <button class="add-row" data-action="add-exercise"><span class="plus">+</span>Add exercise</button>
    </div>
    `}

    <div style="display:grid;grid-template-columns:1fr 1.4fr;gap:14px;margin-top:18px;">
      <div class="stat-box">
        <div class="stat-label" style="margin-bottom:12px;">BODYWEIGHT</div>
        <div class="flex" style="align-items:flex-end;gap:16px;">
          <div><div class="muted" style="font-size:11px;">Current</div><div style="font-family:'Oswald';font-weight:700;font-size:28px;color:var(--text-heading);line-height:1;">${current != null ? current : '—'}</div></div>
          <div><div class="muted" style="font-size:11px;">Target</div><div style="font-family:'Oswald';font-weight:700;font-size:28px;color:var(--accent);line-height:1;">
            <input type="number" data-field="bw-target" value="${s.bodyweightTarget}" style="width:70px;background:none;border:none;color:var(--accent);font-family:'Oswald';font-weight:700;font-size:28px;padding:0;">
          </div></div>
          <div style="margin-left:auto;text-align:right;">
            <div class="muted" style="font-size:11px;">7-day</div>
            <div style="font-family:'Oswald';font-weight:600;font-size:16px;color:${delta == null ? 'var(--text-ghost)' : (delta <= 0 ? 'var(--positive)' : '#e0704a')};line-height:1;">${delta == null ? '—' : (delta <= 0 ? '▼' : '▲') + ' ' + Math.abs(delta).toFixed(1)}</div>
          </div>
        </div>
        <div class="flex gap-8" style="margin-top:12px;">
          <input type="number" step="0.1" placeholder="Log today's weight" data-field="bw-log" style="flex:1;">
          <button class="btn primary" data-action="log-bodyweight">Log</button>
        </div>
      </div>
      <div class="stat-box">
        <div class="stat-label" style="margin-bottom:12px;">DAILY PROGRESS</div>
        <div class="bar-chart" style="height:74px;">
          ${dayBars.map((d) => `
            <div class="bar-col">
              <div class="bar-track" style="height:52px;"><div class="bar-fill" style="height:${Math.max(d.pct, 3)}%;"></div></div>
              <span class="bar-label">${d.label}</span>
            </div>
          `).join('')}
        </div>
      </div>
    </div>
  `;

  bindClicks(container, {
    'open-log': (el) => {
      navigate(`/log/${focusDate}/${el.dataset.idx}`);
    },
    'toggle-done': (el, e) => {
      e.stopPropagation();
      const idx = parseInt(el.dataset.idx, 10);
      const sess = ensureSession(focusDate);
      const ex = sess.exercises[idx];
      const makeDone = !exerciseDone(ex);
      update((draft) => {
        draft.sessions[focusDate].exercises[idx].setLogs.forEach((sl) => { sl.done = makeDone; });
      });
      render(container);
    },
    'add-exercise': () => {
      const name = prompt('Exercise name?');
      if (!name) return;
      const sess = ensureSession(focusDate);
      update((draft) => {
        draft.sessions[focusDate].exercises.push({
          exerciseId: uid('ex'),
          name,
          target: '',
          weight: '0 lb',
          sets: '3',
          reps: '8',
          rpe: '',
          notes: '',
          setLogs: [1, 2, 3].map((n) => ({ set: String(n), weight: 0, reps: 8, rpe: '', done: false })),
        });
      });
      render(container);
    },
    'log-bodyweight': () => {
      const input = container.querySelector('[data-field="bw-log"]');
      const val = parseFloat(input.value);
      if (!Number.isFinite(val)) return;
      update((draft) => {
        const today = isoDate(new Date());
        const existing = draft.bodyweightLog.find((e) => e.date === today);
        if (existing) existing.weight = val; else draft.bodyweightLog.push({ date: today, weight: val });
      });
      render(container);
    },
  });

  bindInputs(container, (el) => {
    if (el.dataset.field === 'bw-target') {
      const val = parseFloat(el.value);
      if (Number.isFinite(val)) update((draft) => { draft.bodyweightTarget = val; });
    }
  }, ['change']);
}

exports.render = render;
});

__def("scripts/router.js", function(module, exports, __require){
const routes = [];
let notFound = null;
let container = null;
let currentPath = null;
const onNavigateListeners = new Set();

function registerRoute(pattern, render) {
  const paramNames = [];
  const regex = new RegExp('^' + pattern.replace(/:[^/]+/g, (m) => {
    paramNames.push(m.slice(1));
    return '([^/]+)';
  }) + '$');
  routes.push({ regex, paramNames, render });
}

function setNotFound(render) {
  notFound = render;
}

function onNavigate(fn) {
  onNavigateListeners.add(fn);
  return () => onNavigateListeners.delete(fn);
}

function currentHashPath() {
  const h = location.hash.replace(/^#/, '');
  return h || '/';
}

function matchRoute(path) {
  for (const r of routes) {
    const m = path.match(r.regex);
    if (m) {
      const params = {};
      r.paramNames.forEach((name, i) => { params[name] = decodeURIComponent(m[i + 1]); });
      return { render: r.render, params };
    }
  }
  return null;
}

function handle() {
  const path = currentHashPath();
  currentPath = path;
  container.innerHTML = '';
  const match = matchRoute(path);
  onNavigateListeners.forEach((fn) => fn(path));
  if (match) {
    match.render(container, match.params);
  } else if (notFound) {
    notFound(container);
  }
}

function initRouter(mountEl) {
  container = mountEl;
  window.addEventListener('hashchange', handle);
  handle();
}

function navigate(path) {
  if (currentHashPath() === path) {
    handle();
  } else {
    location.hash = path;
  }
}

function getCurrentPath() {
  return currentPath;
}

function rerender() {
  handle();
}

exports.registerRoute = registerRoute;
exports.setNotFound = setNotFound;
exports.onNavigate = onNavigate;
exports.initRouter = initRouter;
exports.navigate = navigate;
exports.getCurrentPath = getCurrentPath;
exports.rerender = rerender;
});

__def("scripts/store.js", function(module, exports, __require){
// Iron Ledger — localStorage-backed data store with pub/sub.

const { CUES_BY_NAME } = __require("scripts/data/cues.js");

const STORAGE_KEY = 'ironledger:v1';

function uid(prefix = 'id') {
  return `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 8)}`;
}

function isoDate(d) {
  const y = d.getFullYear();
  const m = String(d.getMonth() + 1).padStart(2, '0');
  const day = String(d.getDate()).padStart(2, '0');
  return `${y}-${m}-${day}`;
}

function startOfWeek(date) {
  const d = new Date(date);
  const dow = (d.getDay() + 6) % 7; // Mon=0
  d.setDate(d.getDate() - dow);
  d.setHours(0, 0, 0, 0);
  return d;
}

const WEEKDAYS = ['MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT', 'SUN'];

function defaultProgramDays() {
  return [
    { day: 'MON', focus: 'PUSH A', tag: 'PUSH', off: false, exercises: [
      { id: uid('ex'), name: 'Barbell Bench Press', target: 'Chest', weight: '185 lb', sets: '4', reps: '8', rpe: '8' },
      { id: uid('ex'), name: 'Overhead Press', target: 'Shoulders', weight: '115 lb', sets: '3', reps: '8-10', rpe: '8' },
      { id: uid('ex'), name: 'Incline DB Press', target: 'Chest', weight: '65 lb', sets: '3', reps: '10-12', rpe: '8' },
      { id: uid('ex'), name: 'Lateral Raise', target: 'Delts', weight: '20 lb', sets: '3', reps: '12-15', rpe: '9' },
      { id: uid('ex'), name: 'Triceps Rope Pushdown', target: 'Triceps', weight: '50 lb', sets: '3', reps: '12-15', rpe: '9' },
    ] },
    { day: 'TUE', focus: 'PULL A', tag: 'PULL', off: false, exercises: [
      { id: uid('ex'), name: 'Deadlift', target: 'Posterior chain', weight: '315 lb', sets: '4', reps: '5', rpe: '8' },
      { id: uid('ex'), name: 'Weighted Pull-Up', target: 'Lats · +45 lb', weight: '+45 lb', sets: '4', reps: '6-8', rpe: '9' },
      { id: uid('ex'), name: 'Barbell Row', target: 'Mid-back', weight: '185 lb', sets: '3', reps: '8-10', rpe: '8' },
      { id: uid('ex'), name: 'Seated Cable Row', target: 'Rhomboids', weight: '160 lb', sets: '3', reps: '10-12', rpe: '7' },
      { id: uid('ex'), name: 'Face Pull', target: 'Rear delts', weight: '50 lb', sets: '3', reps: '15', rpe: '7' },
      { id: uid('ex'), name: 'Incline DB Curl', target: 'Biceps', weight: '35 lb', sets: '3', reps: '10-12', rpe: '8' },
      { id: uid('ex'), name: 'Hammer Curl', target: 'Brachialis', weight: '30 lb', sets: '3', reps: '12', rpe: '8' },
    ] },
    { day: 'WED', focus: 'LEGS A', tag: 'LEGS', off: false, exercises: [
      { id: uid('ex'), name: 'Back Squat', target: 'Quads', weight: '275 lb', sets: '4', reps: '6', rpe: '8' },
      { id: uid('ex'), name: 'Romanian Deadlift', target: 'Hamstrings', weight: '225 lb', sets: '3', reps: '8-10', rpe: '8' },
      { id: uid('ex'), name: 'Leg Press', target: 'Quads', weight: '450 lb', sets: '3', reps: '10-12', rpe: '8' },
      { id: uid('ex'), name: 'Leg Curl', target: 'Hamstrings', weight: '90 lb', sets: '3', reps: '12', rpe: '8' },
      { id: uid('ex'), name: 'Standing Calf Raise', target: 'Calves', weight: '180 lb', sets: '4', reps: '12-15', rpe: '9' },
    ] },
    { day: 'THU', focus: 'PUSH B', tag: 'PUSH', off: false, exercises: [
      { id: uid('ex'), name: 'Incline Barbell Bench', target: 'Upper chest', weight: '155 lb', sets: '4', reps: '8', rpe: '8' },
      { id: uid('ex'), name: 'Arnold Press', target: 'Shoulders', weight: '45 lb', sets: '3', reps: '10', rpe: '8' },
      { id: uid('ex'), name: 'Cable Fly', target: 'Chest', weight: '30 lb', sets: '3', reps: '12-15', rpe: '8' },
      { id: uid('ex'), name: 'Lateral Raise', target: 'Delts', weight: '20 lb', sets: '3', reps: '12-15', rpe: '9' },
      { id: uid('ex'), name: 'Skullcrusher', target: 'Triceps', weight: '60 lb', sets: '3', reps: '10-12', rpe: '8' },
    ] },
    { day: 'FRI', focus: 'PULL B', tag: 'PULL', off: false, exercises: [
      { id: uid('ex'), name: 'Rack Pull', target: 'Posterior chain', weight: '365 lb', sets: '3', reps: '5', rpe: '8' },
      { id: uid('ex'), name: 'Chin-Up', target: 'Lats', weight: 'BW', sets: '4', reps: '8-10', rpe: '9' },
      { id: uid('ex'), name: 'Chest-Supported Row', target: 'Mid-back', weight: '135 lb', sets: '3', reps: '10', rpe: '8' },
      { id: uid('ex'), name: 'Rear Delt Fly', target: 'Rear delts', weight: '15 lb', sets: '3', reps: '15', rpe: '8' },
      { id: uid('ex'), name: 'Hammer Curl', target: 'Brachialis', weight: '30 lb', sets: '3', reps: '12', rpe: '8' },
    ] },
    { day: 'SAT', focus: 'LEGS B', tag: 'LEGS', off: false, exercises: [
      { id: uid('ex'), name: 'Front Squat', target: 'Quads', weight: '205 lb', sets: '4', reps: '6', rpe: '8' },
      { id: uid('ex'), name: 'Hip Thrust', target: 'Glutes', weight: '275 lb', sets: '3', reps: '10', rpe: '8' },
      { id: uid('ex'), name: 'Walking Lunge', target: 'Quads/Glutes', weight: '40 lb', sets: '3', reps: '12/leg', rpe: '8' },
      { id: uid('ex'), name: 'Leg Extension', target: 'Quads', weight: '110 lb', sets: '3', reps: '12-15', rpe: '9' },
      { id: uid('ex'), name: 'Seated Calf Raise', target: 'Calves', weight: '90 lb', sets: '4', reps: '15', rpe: '9' },
    ] },
    { day: 'SUN', focus: 'REST', tag: 'REST', off: true, exercises: [] },
  ];
}

// Full reference catalog, sourced from the uploaded exercise_database.pdf
// (184 exercises across 13 muscle groups, each tagged with equipment + level).
const EXERCISE_DATABASE = [
  { muscle: 'Chest', items: [
    ['Push-Up', 'Bodyweight', 'Beginner'],
    ['Knee Push-Up', 'Bodyweight', 'Beginner'],
    ['Incline Push-Up (hands elevated)', 'Bodyweight', 'Beginner'],
    ['Decline Push-Up (feet elevated)', 'Bodyweight', 'Intermediate'],
    ['Barbell Bench Press (flat)', 'Barbell', 'Intermediate'],
    ['Barbell Incline Bench Press', 'Barbell', 'Intermediate'],
    ['Barbell Decline Bench Press', 'Barbell', 'Intermediate'],
    ['Dumbbell Bench Press (flat)', 'Dumbbell', 'Beginner'],
    ['Dumbbell Incline Press', 'Dumbbell', 'Beginner'],
    ['Dumbbell Decline Press', 'Dumbbell', 'Intermediate'],
    ['Dumbbell Flye (flat)', 'Dumbbell', 'Intermediate'],
    ['Incline Dumbbell Flye', 'Dumbbell', 'Intermediate'],
    ['Machine Chest Press', 'Machine', 'Beginner'],
    ['Incline Machine Press', 'Machine', 'Beginner'],
    ['Pec Deck / Machine Flye', 'Machine', 'Beginner'],
    ['Cable Crossover (high to low)', 'Cable', 'Intermediate'],
    ['Cable Crossover (low to high)', 'Cable', 'Intermediate'],
    ['Cable Flye (mid)', 'Cable', 'Intermediate'],
    ['Chest Dip', 'Bodyweight', 'Intermediate'],
    ['Weighted Chest Dip', 'Bodyweight+', 'Advanced'],
    ['Svend Press', 'Plate', 'Beginner'],
    ['Landmine Press', 'Barbell', 'Intermediate'],
    ['Smith Machine Bench Press', 'Machine', 'Beginner'],
    ['Floor Press', 'Barbell/DB', 'Intermediate'],
  ] },
  { muscle: 'Back', items: [
    ['Pull-Up', 'Bodyweight', 'Intermediate'],
    ['Chin-Up (supinated)', 'Bodyweight', 'Intermediate'],
    ['Assisted Pull-Up (band/machine)', 'Machine', 'Beginner'],
    ['Lat Pulldown (wide)', 'Cable', 'Beginner'],
    ['Lat Pulldown (close/neutral)', 'Cable', 'Beginner'],
    ['Straight-Arm Pulldown', 'Cable', 'Intermediate'],
    ['Barbell Bent-Over Row', 'Barbell', 'Intermediate'],
    ['Pendlay Row', 'Barbell', 'Advanced'],
    ['T-Bar Row', 'Barbell', 'Intermediate'],
    ['Dumbbell Single-Arm Row', 'Dumbbell', 'Beginner'],
    ['Chest-Supported Dumbbell Row', 'Dumbbell', 'Beginner'],
    ['Seated Cable Row (wide)', 'Cable', 'Beginner'],
    ['Seated Cable Row (close)', 'Cable', 'Beginner'],
    ['Machine Row (Hammer Strength)', 'Machine', 'Beginner'],
    ['Inverted Row / Bodyweight Row', 'Bodyweight', 'Beginner'],
    ['Meadows Row', 'Barbell', 'Advanced'],
    ['Deadlift (conventional)', 'Barbell', 'Intermediate'],
    ['Rack Pull', 'Barbell', 'Intermediate'],
    ['Back Extension (hyperextension)', 'Bodyweight', 'Beginner'],
    ['Weighted Back Extension', 'Bodyweight+', 'Intermediate'],
    ['Face Pull', 'Cable', 'Beginner'],
    ['Shrug-to-Row Combo', 'Dumbbell', 'Intermediate'],
    ['Renegade Row', 'Dumbbell', 'Advanced'],
  ] },
  { muscle: 'Shoulders', items: [
    ['Overhead Barbell Press (standing)', 'Barbell', 'Intermediate'],
    ['Seated Barbell Press', 'Barbell', 'Intermediate'],
    ['Dumbbell Shoulder Press', 'Dumbbell', 'Beginner'],
    ['Arnold Press', 'Dumbbell', 'Intermediate'],
    ['Machine Shoulder Press', 'Machine', 'Beginner'],
    ['Dumbbell Lateral Raise', 'Dumbbell', 'Beginner'],
    ['Cable Lateral Raise', 'Cable', 'Intermediate'],
    ['Machine Lateral Raise', 'Machine', 'Beginner'],
    ['Dumbbell Front Raise', 'Dumbbell', 'Beginner'],
    ['Plate Front Raise', 'Plate', 'Beginner'],
    ['Rear Delt Dumbbell Flye', 'Dumbbell', 'Beginner'],
    ['Reverse Pec Deck', 'Machine', 'Beginner'],
    ['Cable Rear Delt Flye', 'Cable', 'Intermediate'],
    ['Upright Row (barbell)', 'Barbell', 'Intermediate'],
    ['Upright Row (cable)', 'Cable', 'Intermediate'],
    ['Landmine Press (shoulder)', 'Barbell', 'Intermediate'],
    ['Pike Push-Up', 'Bodyweight', 'Intermediate'],
    ['Handstand Push-Up', 'Bodyweight', 'Advanced'],
    ['Behind-the-Neck Press', 'Barbell', 'Advanced'],
    ['Cable Y-Raise', 'Cable', 'Intermediate'],
  ] },
  { muscle: 'Biceps', items: [
    ['Barbell Curl', 'Barbell', 'Beginner'],
    ['EZ-Bar Curl', 'Barbell', 'Beginner'],
    ['Dumbbell Curl (alternating)', 'Dumbbell', 'Beginner'],
    ['Hammer Curl', 'Dumbbell', 'Beginner'],
    ['Incline Dumbbell Curl', 'Dumbbell', 'Intermediate'],
    ['Concentration Curl', 'Dumbbell', 'Beginner'],
    ['Preacher Curl (barbell)', 'Barbell', 'Intermediate'],
    ['Preacher Curl (machine)', 'Machine', 'Beginner'],
    ['Cable Curl (straight bar)', 'Cable', 'Beginner'],
    ['Cable Rope Hammer Curl', 'Cable', 'Beginner'],
    ['Spider Curl', 'Dumbbell', 'Intermediate'],
    ['Zottman Curl', 'Dumbbell', 'Intermediate'],
    ['Chin-Up (biceps focus)', 'Bodyweight', 'Intermediate'],
    ['21s (barbell)', 'Barbell', 'Intermediate'],
    ['Bayesian Cable Curl', 'Cable', 'Advanced'],
  ] },
  { muscle: 'Triceps', items: [
    ['Triceps Pushdown (rope)', 'Cable', 'Beginner'],
    ['Triceps Pushdown (bar)', 'Cable', 'Beginner'],
    ['Overhead Cable Extension', 'Cable', 'Intermediate'],
    ['Overhead Dumbbell Extension', 'Dumbbell', 'Beginner'],
    ['Skull Crusher (EZ-bar)', 'Barbell', 'Intermediate'],
    ['Close-Grip Bench Press', 'Barbell', 'Intermediate'],
    ['Triceps Dip (bench)', 'Bodyweight', 'Beginner'],
    ['Parallel Bar Dip', 'Bodyweight', 'Intermediate'],
    ['Dumbbell Kickback', 'Dumbbell', 'Beginner'],
    ['Cable Kickback', 'Cable', 'Beginner'],
    ['Diamond Push-Up', 'Bodyweight', 'Intermediate'],
    ['Machine Triceps Extension', 'Machine', 'Beginner'],
    ['JM Press', 'Barbell', 'Advanced'],
    ['Tate Press', 'Dumbbell', 'Intermediate'],
  ] },
  { muscle: 'Forearms & Grip', items: [
    ['Wrist Curl (barbell)', 'Barbell', 'Beginner'],
    ['Reverse Wrist Curl', 'Barbell', 'Beginner'],
    ['Reverse Barbell Curl', 'Barbell', 'Intermediate'],
    ["Farmer's Carry", 'Dumbbell', 'Beginner'],
    ['Dead Hang', 'Bodyweight', 'Beginner'],
    ['Plate Pinch', 'Plate', 'Intermediate'],
    ['Wrist Roller', 'Machine', 'Intermediate'],
    ['Cable Wrist Curl', 'Cable', 'Beginner'],
  ] },
  { muscle: 'Quadriceps', items: [
    ['Bodyweight Squat', 'Bodyweight', 'Beginner'],
    ['Barbell Back Squat', 'Barbell', 'Intermediate'],
    ['Barbell Front Squat', 'Barbell', 'Advanced'],
    ['Goblet Squat', 'Dumbbell', 'Beginner'],
    ['Hack Squat (machine)', 'Machine', 'Beginner'],
    ['Leg Press', 'Machine', 'Beginner'],
    ['Leg Extension', 'Machine', 'Beginner'],
    ['Bulgarian Split Squat', 'Dumbbell', 'Intermediate'],
    ['Walking Lunge', 'Dumbbell', 'Beginner'],
    ['Reverse Lunge', 'Dumbbell', 'Beginner'],
    ['Step-Up', 'Dumbbell', 'Beginner'],
    ['Smith Machine Squat', 'Machine', 'Beginner'],
    ['Sissy Squat', 'Bodyweight', 'Advanced'],
    ['Box Squat', 'Barbell', 'Intermediate'],
    ['Pause Squat', 'Barbell', 'Advanced'],
  ] },
  { muscle: 'Hamstrings', items: [
    ['Romanian Deadlift (barbell)', 'Barbell', 'Intermediate'],
    ['Dumbbell RDL', 'Dumbbell', 'Beginner'],
    ['Stiff-Leg Deadlift', 'Barbell', 'Intermediate'],
    ['Lying Leg Curl', 'Machine', 'Beginner'],
    ['Seated Leg Curl', 'Machine', 'Beginner'],
    ['Nordic Hamstring Curl', 'Bodyweight', 'Advanced'],
    ['Good Morning', 'Barbell', 'Intermediate'],
    ['Glute-Ham Raise', 'Machine', 'Advanced'],
    ['Cable Pull-Through', 'Cable', 'Beginner'],
    ['Single-Leg RDL', 'Dumbbell', 'Intermediate'],
    ['Kettlebell Swing', 'Kettlebell', 'Intermediate'],
  ] },
  { muscle: 'Glutes', items: [
    ['Barbell Hip Thrust', 'Barbell', 'Intermediate'],
    ['Glute Bridge (bodyweight)', 'Bodyweight', 'Beginner'],
    ['Machine Hip Thrust', 'Machine', 'Beginner'],
    ['Cable Kickback (glute)', 'Cable', 'Beginner'],
    ['Hip Abduction Machine', 'Machine', 'Beginner'],
    ['Banded Lateral Walk', 'Band', 'Beginner'],
    ['Sumo Deadlift', 'Barbell', 'Intermediate'],
    ['Curtsy Lunge', 'Dumbbell', 'Intermediate'],
    ['Frog Pump', 'Bodyweight', 'Beginner'],
    ['Single-Leg Hip Thrust', 'Bodyweight', 'Intermediate'],
    ['Step-Up (glute focus)', 'Dumbbell', 'Beginner'],
  ] },
  { muscle: 'Calves', items: [
    ['Standing Calf Raise (machine)', 'Machine', 'Beginner'],
    ['Seated Calf Raise', 'Machine', 'Beginner'],
    ['Standing Calf Raise (dumbbell)', 'Dumbbell', 'Beginner'],
    ['Leg Press Calf Raise', 'Machine', 'Beginner'],
    ['Single-Leg Calf Raise', 'Bodyweight', 'Beginner'],
    ['Smith Machine Calf Raise', 'Machine', 'Beginner'],
    ['Donkey Calf Raise', 'Machine', 'Intermediate'],
    ['Tibialis Raise', 'Bodyweight', 'Beginner'],
  ] },
  { muscle: 'Abs & Core', items: [
    ['Plank', 'Bodyweight', 'Beginner'],
    ['Side Plank', 'Bodyweight', 'Beginner'],
    ['Crunch', 'Bodyweight', 'Beginner'],
    ['Bicycle Crunch', 'Bodyweight', 'Beginner'],
    ['Hanging Leg Raise', 'Bodyweight', 'Intermediate'],
    ['Hanging Knee Raise', 'Bodyweight', 'Beginner'],
    ['Cable Crunch', 'Cable', 'Beginner'],
    ['Ab Wheel Rollout', 'Wheel', 'Advanced'],
    ['Russian Twist', 'Plate', 'Beginner'],
    ['Mountain Climber', 'Bodyweight', 'Beginner'],
    ['Dead Bug', 'Bodyweight', 'Beginner'],
    ['Leg Raise (lying)', 'Bodyweight', 'Beginner'],
    ['Decline Sit-Up', 'Bodyweight', 'Intermediate'],
    ['Pallof Press', 'Cable', 'Intermediate'],
    ['Toes-to-Bar', 'Bodyweight', 'Advanced'],
    ['Woodchopper (cable)', 'Cable', 'Intermediate'],
    ['Hollow Body Hold', 'Bodyweight', 'Intermediate'],
  ] },
  { muscle: 'Traps & Neck', items: [
    ['Barbell Shrug', 'Barbell', 'Beginner'],
    ['Dumbbell Shrug', 'Dumbbell', 'Beginner'],
    ['Cable Shrug', 'Cable', 'Beginner'],
    ['Trap Bar Shrug', 'Barbell', 'Intermediate'],
    ["Farmer's Carry (trap)", 'Dumbbell', 'Beginner'],
    ['Neck Curl (plate)', 'Plate', 'Intermediate'],
    ['Neck Extension', 'Plate', 'Intermediate'],
  ] },
  { muscle: 'Full-Body & Compound', items: [
    ['Clean & Press', 'Barbell', 'Advanced'],
    ['Power Clean', 'Barbell', 'Advanced'],
    ['Snatch', 'Barbell', 'Advanced'],
    ['Thruster', 'Barbell', 'Intermediate'],
    ['Turkish Get-Up', 'Kettlebell', 'Advanced'],
    ['Burpee', 'Bodyweight', 'Beginner'],
    ['Kettlebell Clean', 'Kettlebell', 'Intermediate'],
    ["Devil's Press", 'Dumbbell', 'Advanced'],
    ['Trap Bar Deadlift', 'Barbell', 'Beginner'],
    ['Bear Crawl', 'Bodyweight', 'Beginner'],
    ['Wall Ball', 'Med Ball', 'Intermediate'],
  ] },
];

function buildLibraryFromDatabase() {
  const entries = [];
  EXERCISE_DATABASE.forEach(({ muscle, items }) => {
    items.forEach(([name, equipment, level]) => {
      entries.push({
        id: uid('lib'),
        name,
        muscle,
        equipment,
        level,
        group: `${muscle} · ${equipment}`,
        cues: CUES_BY_NAME[name] ? [...CUES_BY_NAME[name]] : [],
        image: null,
      });
    });
  });
  return entries;
}

function parseNum(str) {
  const m = String(str).match(/-?\d+(\.\d+)?/);
  return m ? parseFloat(m[0]) : 0;
}

function repsCount(str) {
  const m = String(str).match(/\d+(\.\d+)?/g);
  if (!m) return 8;
  const nums = m.map(Number);
  return Math.round(nums.reduce((a, b) => a + b, 0) / nums.length);
}

/** Backfills ~4 weeks of plausible completed sessions before the program start
 *  so Dashboard/Measurements have real history on first load. */
function buildHistorySessions(days, programStart) {
  const sessions = {};
  for (let offset = -28; offset < 0; offset++) {
    const d = new Date(programStart);
    d.setDate(d.getDate() + offset);
    const wd = (d.getDay() + 6) % 7;
    const day = days[wd];
    if (day.off) continue;
    const dateIso = isoDate(d);
    const isRecent = offset >= -4;
    sessions[dateIso] = {
      date: dateIso,
      tag: day.tag,
      focus: day.focus,
      off: false,
      exercises: day.exercises.map((ex, idx) => {
        const baseWeight = parseNum(ex.weight);
        const repsTarget = repsCount(ex.reps);
        const setsCount = Math.max(1, parseInt(ex.sets, 10) || 3);
        const progressFactor = 1 - (Math.abs(offset) / 28) * 0.08;
        const weight = Math.round(baseWeight * progressFactor);
        const setLogs = Array.from({ length: setsCount }, (_, i) => ({
          set: String(i + 1),
          weight,
          reps: repsTarget,
          rpe: ex.rpe,
          done: !(isRecent && idx >= day.exercises.length - 2 && i === setsCount - 1),
        }));
        return {
          exerciseId: ex.id, name: ex.name, target: ex.target, weight: ex.weight,
          sets: ex.sets, reps: ex.reps, rpe: ex.rpe, notes: '', setLogs,
        };
      }),
    };
  }
  return sessions;
}

function seed() {
  const today = new Date();
  const programStart = startOfWeek(today);
  const days = defaultProgramDays();
  const firstProgramId = 'prog_default';
  return {
    settings: { accent: '#b6f24a', density: 'Standard', energy: 'Charged' },
    client: { name: 'Marcus T.' },
    seededStart: isoDate(programStart),
    programs: [
      { id: firstProgramId, name: 'Push · Pull · Legs', days },
    ],
    activeProgramId: firstProgramId,
    sessions: buildHistorySessions(days, programStart),
    bodyweightLog: [
      { date: isoDate(new Date(today - 42 * 86400000)), weight: 193.0 },
      { date: isoDate(new Date(today - 28 * 86400000)), weight: 189.4 },
      { date: isoDate(new Date(today - 14 * 86400000)), weight: 186.2 },
      { date: isoDate(new Date(today - 7 * 86400000)), weight: 184.7 },
      { date: isoDate(today), weight: 183.5 },
    ],
    bodyweightTarget: 170,
    measurements: [
      { date: isoDate(new Date(today - 49 * 86400000)), weight: 193.0, bodyFat: 17.3, waist: 34.0, chest: 43.2, arms: 15.6, thighs: 23.8 },
      { date: isoDate(new Date(today - 28 * 86400000)), weight: 189.4, bodyFat: 16.1, waist: 33.4, chest: 43.5, arms: 15.8, thighs: 24.0 },
      { date: isoDate(new Date(today - 14 * 86400000)), weight: 186.2, bodyFat: 15.0, waist: 33.0, chest: 43.8, arms: 16.0, thighs: 24.2 },
      { date: isoDate(today), weight: 183.5, bodyFat: 14.2, waist: 32.5, chest: 44.0, arms: 16.2, thighs: 24.5 },
    ],
    library: buildLibraryFromDatabase(),
    photos: [
      { id: uid('ph'), date: isoDate(new Date(today - 49 * 86400000)), image: null },
      { id: uid('ph'), date: isoDate(new Date(today - 35 * 86400000)), image: null },
      { id: uid('ph'), date: isoDate(new Date(today - 21 * 86400000)), image: null },
      { id: uid('ph'), date: isoDate(new Date(today - 7 * 86400000)), image: null },
      { id: uid('ph'), date: isoDate(today), image: null },
    ],
    calendarOverrides: {},
    ui: { activeExerciseGroup: 'All', logCursor: null },
  };
}

let state = load();

/** Merges any exercises added to EXERCISE_DATABASE since a browser's
 *  localStorage was first seeded into its existing library, and backfills
 *  coaching cues for entries that have none — without touching entries the
 *  user already customized (their own cues, uploaded images). */
function reconcileLibrary(existingLibrary) {
  const existing = Array.isArray(existingLibrary) ? existingLibrary : [];
  let changed = false;
  existing.forEach((ex) => {
    if ((!ex.cues || ex.cues.length === 0) && CUES_BY_NAME[ex.name]) {
      ex.cues = [...CUES_BY_NAME[ex.name]];
      changed = true;
    }
  });
  const known = new Set(existing.map((ex) => ex.name.trim().toLowerCase()));
  const missing = buildLibraryFromDatabase().filter((ex) => !known.has(ex.name.trim().toLowerCase()));
  return { list: [...existing, ...missing], changed: changed || missing.length > 0 };
}

/** Migrates a pre-multiprogram store (single `program`) to the `programs`
 *  list model, preserving the user's schedule. */
function migratePrograms(parsed) {
  if (parsed.programs && parsed.activeProgramId) return false;
  const id = 'prog_' + Math.random().toString(36).slice(2, 8);
  const old = parsed.program;
  parsed.programs = [{ id, name: (old && old.name) || 'My Program', days: (old && old.days) || defaultProgramDays() }];
  parsed.activeProgramId = id;
  delete parsed.program;
  return true;
}

function load() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (!raw) return seed();
    const parsed = JSON.parse(raw);
    const migrated = migratePrograms(parsed);
    const merged = { ...seed(), ...parsed };
    const { list, changed } = reconcileLibrary(parsed.library);
    merged.library = list;
    if (changed || migrated) {
      localStorage.setItem(STORAGE_KEY, JSON.stringify(merged));
    }
    return merged;
  } catch (e) {
    console.warn('Failed to load store, reseeding', e);
    return seed();
  }
}

function persist() {
  try {
    localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
  } catch (e) {
    console.warn('Failed to persist store', e);
  }
}

const listeners = new Set();

function notify() {
  persist();
  listeners.forEach((fn) => fn(state));
}

function subscribe(fn) {
  listeners.add(fn);
  return () => listeners.delete(fn);
}

function getState() {
  return state;
}

/** Mutate state via a producer function `(draft) => void`, then persist + notify. */
function update(mutator) {
  mutator(state);
  notify();
}

function resetAll() {
  state = seed();
  notify();
}

/** The currently-selected training program (its `days` drive the whole app). */
function activeProgram(s = state) {
  return s.programs.find((p) => p.id === s.activeProgramId) || s.programs[0];
}

/** A fresh 7-day (Mon–Sun) blank schedule for a newly created program. */
function newProgramDays() {
  return WEEKDAYS.map((d) => ({ day: d, focus: 'Rest', tag: 'REST', off: true, exercises: [] }));
}

const helpers = { uid, isoDate, startOfWeek, WEEKDAYS };

exports.subscribe = subscribe;
exports.getState = getState;
exports.update = update;
exports.resetAll = resetAll;
exports.activeProgram = activeProgram;
exports.newProgramDays = newProgramDays;
exports.helpers = helpers;
});

__def("scripts/theme.js", function(module, exports, __require){
const ACCENT_OPTIONS = ['#b6f24a', '#ff8a3d', '#3d9bff', '#b45cff', '#ff4d4d'];
const DENSITY_OPTIONS = ['Compact', 'Standard', 'Roomy'];
const ENERGY_OPTIONS = ['Focused', 'Charged'];

function densityToPad(density) {
  return density === 'Compact' ? 0.6 : density === 'Roomy' ? 1.45 : 1;
}

function applyTheme(settings) {
  const root = document.documentElement;
  const accent = settings.accent || '#b6f24a';
  root.style.setProperty('--accent', accent);
  root.style.setProperty('--pad', String(densityToPad(settings.density)));
  root.style.setProperty('--glow', settings.energy === 'Charged'
    ? `0 0 18px ${accent}80, 0 0 5px ${accent}`
    : 'none');
}

exports.ACCENT_OPTIONS = ACCENT_OPTIONS;
exports.DENSITY_OPTIONS = DENSITY_OPTIONS;
exports.ENERGY_OPTIONS = ENERGY_OPTIONS;
exports.densityToPad = densityToPad;
exports.applyTheme = applyTheme;
});

__def("scripts/utils.js", function(module, exports, __require){
const { getState, update, helpers, activeProgram } = __require("scripts/store.js");

const { uid, isoDate } = helpers;

function parseWeight(str) {
  if (str == null) return null;
  const m = String(str).match(/-?\d+(\.\d+)?/);
  return m ? parseFloat(m[0]) : null;
}

function repsToCount(str) {
  if (str == null) return 0;
  const m = String(str).match(/\d+(\.\d+)?/g);
  if (!m) return 0;
  const nums = m.map(Number);
  return Math.round(nums.reduce((a, b) => a + b, 0) / nums.length);
}

function epley1RM(weight, reps) {
  if (!weight || !reps) return 0;
  if (reps <= 1) return weight;
  return weight * (1 + reps / 30);
}

function addDays(date, n) {
  const d = new Date(date);
  d.setDate(d.getDate() + n);
  return d;
}

function weekDates(startIso, weekIndex) {
  const base = addDays(startIso, weekIndex * 7);
  return Array.from({ length: 7 }, (_, i) => isoDate(addDays(base, i)));
}

function dateLabel(dateIso) {
  const d = new Date(dateIso + 'T00:00:00');
  return d.toLocaleDateString('en-US', { weekday: 'short', month: 'short', day: 'numeric' });
}

function weekdayIndexOf(dateIso) {
  const d = new Date(dateIso + 'T00:00:00');
  return (d.getDay() + 6) % 7; // Mon=0
}

function programDayFor(dateIso) {
  const s = getState();
  return activeProgram(s).days[weekdayIndexOf(dateIso)];
}

/** The 7 ISO dates (Mon–Sun) of the calendar week containing `dateIso`. */
function currentWeekDates(dateIso = isoDate(new Date())) {
  const start = helpers.startOfWeek(dateIso + 'T00:00:00');
  return Array.from({ length: 7 }, (_, i) => isoDate(addDays(start, i)));
}

function buildSetLogsFromTemplate(ex) {
  const count = Math.max(1, parseInt(ex.sets, 10) || 1);
  const targetWeight = parseWeight(ex.weight) ?? 0;
  const targetReps = repsToCount(ex.reps) || 8;
  return Array.from({ length: count }, (_, i) => ({
    set: String(i + 1),
    weight: targetWeight,
    reps: targetReps,
    rpe: '',
    done: false,
  }));
}

/** Returns the persisted session for a date, materializing it from the program
 *  template on first access (writes through to the store). */
function ensureSession(dateIso) {
  const s = getState();
  if (s.sessions[dateIso]) return s.sessions[dateIso];
  const day = programDayFor(dateIso);
  const override = s.calendarOverrides[dateIso];
  const tag = override ?? day.tag;
  const focus = override ? overrideFocus(override) : day.focus;
  const off = tag === 'REST';
  const session = {
    date: dateIso,
    tag,
    focus,
    off,
    exercises: off ? [] : day.exercises.map((ex) => ({
      exerciseId: ex.id,
      name: ex.name,
      target: ex.target,
      weight: ex.weight,
      sets: ex.sets,
      reps: ex.reps,
      rpe: ex.rpe,
      notes: '',
      setLogs: buildSetLogsFromTemplate(ex),
    })),
  };
  update((draft) => { draft.sessions[dateIso] = session; });
  return getState().sessions[dateIso];
}

/** Read-only peek at a session without persisting a materialized copy. */
function peekSession(dateIso) {
  const s = getState();
  if (s.sessions[dateIso]) return s.sessions[dateIso];
  const day = programDayFor(dateIso);
  const override = s.calendarOverrides[dateIso];
  const tag = override ?? day.tag;
  return { date: dateIso, tag, focus: override ? overrideFocus(override) : day.focus, off: tag === 'REST', exercises: [], virtual: true };
}

function overrideFocus(tag) {
  return { PUSH: 'PUSH', PULL: 'PULL', LEGS: 'LEGS', CARDIO: 'CARDIO', REST: 'REST' }[tag] ?? tag;
}

function exerciseDone(ex) {
  return ex.setLogs.length > 0 && ex.setLogs.every((s) => s.done);
}

function setVolume(setLog) {
  return (setLog.done ? (setLog.weight || 0) * (setLog.reps || 0) : 0);
}

function sessionVolume(session) {
  return session.exercises.reduce((sum, ex) => sum + ex.setLogs.reduce((a, s) => a + setVolume(s), 0), 0);
}

function sessionCompletionPct(session) {
  const total = session.exercises.reduce((a, ex) => a + ex.setLogs.length, 0);
  if (!total) return 0;
  const done = session.exercises.reduce((a, ex) => a + ex.setLogs.filter((s) => s.done).length, 0);
  return done / total;
}

function isSessionFullyDone(session) {
  return session.exercises.length > 0 && session.exercises.every(exerciseDone);
}

/** All persisted (materialized) sessions between two ISO dates inclusive. */
function sessionsInRange(startIso, endIso) {
  const s = getState();
  return Object.values(s.sessions).filter((sess) => sess.date >= startIso && sess.date <= endIso);
}

function bestEst1RM(exerciseName) {
  const s = getState();
  let best = 0;
  Object.values(s.sessions).forEach((sess) => {
    sess.exercises.forEach((ex) => {
      if (ex.name !== exerciseName) return;
      ex.setLogs.forEach((set) => {
        if (!set.done) return;
        const est = epley1RM(set.weight, set.reps);
        if (est > best) best = est;
      });
    });
  });
  return Math.round(best);
}

function bestSetFor(exerciseName) {
  const s = getState();
  let best = null;
  let bestEst = 0;
  Object.values(s.sessions).forEach((sess) => {
    sess.exercises.forEach((ex) => {
      if (ex.name !== exerciseName) return;
      ex.setLogs.forEach((set) => {
        if (!set.done) return;
        const est = epley1RM(set.weight, set.reps);
        if (est > bestEst) { bestEst = est; best = set; }
      });
    });
  });
  return best;
}

function lastDoneFor(exerciseName) {
  const s = getState();
  const dates = Object.values(s.sessions)
    .filter((sess) => sess.exercises.some((ex) => ex.name === exerciseName && ex.setLogs.some((set) => set.done)))
    .map((sess) => sess.date)
    .sort();
  return dates.length ? dates[dates.length - 1] : null;
}

function relativeDateLabel(dateIso) {
  const today = new Date(isoDate(new Date()) + 'T00:00:00');
  const d = new Date(dateIso + 'T00:00:00');
  const days = Math.round((today - d) / 86400000);
  if (days === 0) return 'Today';
  if (days === 1) return 'Yesterday';
  if (days > 1) return `${days} days ago`;
  return dateLabel(dateIso);
}

function previousExerciseInstance(beforeDateIso, exerciseName) {
  const s = getState();
  const dates = Object.keys(s.sessions).filter((d) => d < beforeDateIso).sort().reverse();
  for (const d of dates) {
    const found = s.sessions[d].exercises.find((ex) => ex.name === exerciseName);
    if (found) return found;
  }
  return null;
}

function earliestEst1RM(exerciseName) {
  const s = getState();
  let earliest = null;
  Object.values(s.sessions)
    .sort((a, b) => a.date.localeCompare(b.date))
    .forEach((sess) => {
      if (earliest != null) return;
      sess.exercises.forEach((ex) => {
        if (ex.name !== exerciseName || earliest != null) return;
        const doneSet = ex.setLogs.find((set) => set.done);
        if (doneSet) earliest = epley1RM(doneSet.weight, doneSet.reps);
      });
    });
  return earliest ? Math.round(earliest) : null;
}

/** Weekly-bucketed training volume for the last `windowWeeks` weeks (or all history). */
function weeklyVolumeBuckets(windowWeeks) {
  const s = getState();
  const buckets = new Map();
  Object.values(s.sessions).forEach((sess) => {
    const weekStart = isoDate(helpers.startOfWeek(sess.date + 'T00:00:00'));
    buckets.set(weekStart, (buckets.get(weekStart) || 0) + sessionVolume(sess));
  });
  const sorted = [...buckets.entries()].sort((a, b) => a[0].localeCompare(b[0]));
  const sliced = windowWeeks ? sorted.slice(-windowWeeks) : sorted;
  return sliced.map(([weekStart, volume]) => ({
    label: new Date(weekStart + 'T00:00:00').toLocaleDateString('en-US', { month: 'short', day: 'numeric' }),
    volume,
  }));
}

function adherenceInWindow(windowWeeks) {
  const s = getState();
  const today = new Date();
  const daysBack = windowWeeks ? windowWeeks * 7 : 365;
  const windowStart = addDays(today, -daysBack);
  let expected = 0;
  let done = 0;
  const days = activeProgram(s).days;
  for (let d = new Date(windowStart); d <= today; d = addDays(d, 1)) {
    const wd = (d.getDay() + 6) % 7;
    const day = days[wd];
    if (day.off) continue;
    const dIso = isoDate(d);
    if (dIso > isoDate(today)) continue;
    expected += 1;
    const sess = s.sessions[dIso];
    if (sess && isSessionFullyDone(sess)) done += 1;
  }
  return { done, expected, pct: expected ? Math.round((done / expected) * 100) : 0 };
}

function bodyweightNear(dateIso) {
  const s = getState();
  const sorted = [...s.bodyweightLog].sort((a, b) => a.date.localeCompare(b.date));
  if (!sorted.length) return null;
  const onOrBefore = [...sorted].reverse().find((e) => e.date <= dateIso);
  return (onOrBefore ?? sorted[0]).weight;
}



exports.uid = uid;
exports.isoDate = isoDate;
exports.parseWeight = parseWeight;
exports.repsToCount = repsToCount;
exports.epley1RM = epley1RM;
exports.addDays = addDays;
exports.weekDates = weekDates;
exports.dateLabel = dateLabel;
exports.weekdayIndexOf = weekdayIndexOf;
exports.programDayFor = programDayFor;
exports.currentWeekDates = currentWeekDates;
exports.ensureSession = ensureSession;
exports.peekSession = peekSession;
exports.exerciseDone = exerciseDone;
exports.setVolume = setVolume;
exports.sessionVolume = sessionVolume;
exports.sessionCompletionPct = sessionCompletionPct;
exports.isSessionFullyDone = isSessionFullyDone;
exports.sessionsInRange = sessionsInRange;
exports.bestEst1RM = bestEst1RM;
exports.bestSetFor = bestSetFor;
exports.lastDoneFor = lastDoneFor;
exports.relativeDateLabel = relativeDateLabel;
exports.previousExerciseInstance = previousExerciseInstance;
exports.earliestEst1RM = earliestEst1RM;
exports.weeklyVolumeBuckets = weeklyVolumeBuckets;
exports.adherenceInWindow = adherenceInWindow;
exports.bodyweightNear = bodyweightNear;
});
  __require("scripts/app.js");
})();
